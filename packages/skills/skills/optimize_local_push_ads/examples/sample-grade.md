用户输入：

客户A，广告账户 123456。搜索线索计划消耗高，只来了两条线索且销售说不精准；当前只有消耗和线索数，没有有效线索、预约和成交回传。请给调整方案。

样板输出：

投流结论

当前不能把“关键词不准”定为唯一根因。先保持计划暂停，优先补齐搜索词、线索有效性和承接记录；本轮只预览一个变量的调整，不直接提交。

账户身份

client_id：客户A；account_id：123456。计划名称、门店、抖音号和当前状态待页面核验。

证据与数据口径

已知：有消耗、2条原始线索、销售主观反馈不精准。缺少时间范围、搜索词报告、有效线索定义、接通率、预约/到店/成交和回传状态，因此不能计算有效获客成本。

根因强度

待验证假设：搜索词意图过宽，或表单/销售筛选不足。证据强度低，必须先读取搜索词和逐条销售标注。

P0动作

导出搜索词与两条线索的销售标签；将明显求职、泛运营学习等无关意图列为候选排除项，其他设置保持不变。

验证指标

同口径比较有效线索率、有效线索成本和预约率，不只看原始线索成本。

观察条件

在用户确认的预算上限内，跑满一个完整观察周期或达到预先约定的最小有效样本；不同时调整出价、地域和素材。

止损

账户身份不符、审核异常、预算超限或回传失效时停止本轮；无有效线索时不继续扩词或加价。

回退方案

保留变更前关键词与截图；验证失败时恢复原词包，并改做素材/表单筛选验证。

PREVIEW_ONLY变更单

status: PREVIEW_ONLY；requires_confirmation: true。账户：123456；计划：待页面核验；字段：关键词排除项；原值：待读取；新值：待搜索词报告核验后列出；原因：验证搜索意图假设；不提交。

风险与待补信息

需补：官方当前搜索线索能力、账户开放状态、搜索词报告、销售逐条标签和回传状态。任何实际改词、暂停或启动均需要在页面预览后再次确认。
