## 用户输入：

合成招商团队希望获取有效加盟咨询，给出每周预算上限，但尚未确定投放产品、账户身份和线索承接口径。

## 样板输出：

# 场景与推荐专项

已确认：客户 `tenant-demo-a` 想为招商内容获取有效咨询；预算上限为每周 3,000 元，尚未说明使用本地推还是 DOU+，也未提供 `client_id`、`account_id`、素材、线索定义或承接数据。

本轮专项路由或最小事实追问：招商团队的目标是有效加盟咨询；用户只给出每周预算上限，尚未确认投放产品、账户身份和线索承接口径。因此先核对，不进入执行层。

当前不能判断平台能力、ROI 或线索成本。若目标是单条视频的内容加热，进入 `dou_plus_ads`；若已具备本地生活/线索账户、需看账户漏斗或回传，进入 `optimize_local_push_ads`。本例先不输出任何专项大方案。

# 已确认事实与待补信息

- 已确认：招商目标、每周预算上限、需要获得有效咨询。
- 待补：`client_id`、`account_id`、投放产品/平台、地域、目标加盟商、素材状态、线索有效定义、承接人和回传周期。
- 租户边界：只使用 `tenant-demo-a` 的后续资料，不读取或引用其他客户预算、素材或结果。

# 证据与数据口径

当前没有账户消耗、曝光、点击、咨询、有效线索、预约或签约回传。有效线索定义和观察周期待业务方确认；没有这些证据，不计算 ROI、不报预计线索成本，也不把内容指标写成招商结果。

# 预算预览与测试顺序

预算仅为上限预览，不是平台建议或已创建计划：先保留 70% 用于一个已确认的平台/产品与单一主变量测试，剩余 30% 仅在首轮数据口径可读、承接与回传可核验后再决定是否测试另一渠道。先确认平台：DOU+ 进入内容加热专项，本地推/线索账户进入本地推专项。

# 观察条件与止损

观察条件：每轮只改变一个主变量，记录相同周期内的消耗、咨询、有效线索和承接状态。止损：账户身份、素材授权、线索定义、承接或回传任一项缺失时，不进入可执行设置；若数据不可读或工具失败，停止调整并转人工核验。

# PREVIEW_ONLY变更草案

```yaml
status: PREVIEW_ONLY
requires_confirmation: true
client_id: 待补
account_id: 待补
action: 确认平台与专项路由后，建立单变量测试草案
no_external_execution: true
```

# 风险与下一步确认

未读取真实后台，未充值、未付款、未创建或启动投放。请确认平台/产品、账户身份、地域、预算周期、有效线索定义和承接回传；确认后只进入对应专项完成深度预览。

## 下一步核对清单

- [ ] 确认投放产品：本地推则转 `optimize_local_push_ads`；DOU+ 则转 `dou_plus_ads`。
- [ ] 确认 `client_id + account_id`、地域、素材授权和招商线索承接口径。
- [ ] 确认每周预算上限、观察周期和有效加盟咨询定义。

以上是安全投流预览，不是已创建、已充值或已启动的投放计划。
