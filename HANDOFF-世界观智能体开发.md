# 世界观智能体（人设生成引擎）· 开发交接文档

> 用途：发给技术合伙人周凯，落地「商户测试团 · 视频测试智能体」中的"世界观 / 人设生成"子能力
> 版本：V1.0（开发交接稿）
> 发起人：保禄（产品 / 决策）｜ 起草：WorkBuddy（原型设计，不写生产代码）
> 落地责任：周凯 / Codex → 思潼 AI 平台（api.lcppch.top）
> 关联附档：本仓库 `agent-specs/` 目录（人设生成引擎子模块规格、整体 skill 规格、技术合伙人交接、人设卡、示例报告）

---

## 0. 一句话目标

把"目标用户描述 → 自动批量生成一群有血有肉的虚拟观众（世界观人设）→ 代入给视频打分挑刺"这套能力，做成思潼里**可调用、可复现的人设生成 API（模块 B）**，而不是写死 5 个固定角色。

这是整个产品的差异化引擎：别人做"AI 点评"，我们做"一群真实目标客户替你测片"。

---

## 1. 世界观 skill 是什么（起源 + 现状）

**起源 / 方法论**
- 底层框架来自 novel-creator 专家的角色设计框架：**Lajos Egri 三维角色（生理 / 社会 / 心理）+ K.M. Weiland 角色弧线 + 声线卡（口癖 / 句式 / 比喻 / 禁忌）**。
- 该框架原用于小说角色塑造，被我们**迁移**到短视频场景：把"目标客户"做成有独立动机、偏见、声纹的虚拟观众（surrogate 测试观众），让他们从各自立场给视频打分挑刺。
- 本轮已用该框架**手动**生成 5 位商户人设（老周 / 李姐 / 王总 / 陈先生 / 赵经理），并跑通"人设代入 → 内容诊断"链路，验证方法论成立。

**现状（诚实交代）**
- ✅ 方法论已在 WorkBuddy 的 novel-creator 专家态跑通（生成了 5 位商户，见附档 `商户测试团_虚拟形象设定.md`）。
- ❌ **尚未工程化**为可调用的"人设生成 API"。当前是"人工在 WB 里跑框架出人设"，无法被视频测试 skill 程序化调用。
- 📌 本交接要周凯落地的，就是把这套框架做成思潼里的独立子能力（模块 B），供视频测试 skill（模块 D 代入推理）调用。
- ⚠️ 诚实更正：V1 测试报告（附档 `商户测试团_第一轮测试报告.md`）的具体视觉细节是**推断非实测**（当时环境图片读取被过滤），仅评估方法论可信。具体视频的逐帧结论需用口播逐字稿或切多模态模型重测，不可作为真实基线。

---

## 2. 它在整体架构里的位置

```
思潼 AI 平台（唯一事实源 / 核心底座）
  [A] 目标用户解析  → 结构化 Brief（行业/地域/规模/痛点/决策风格…）
  [B] 人设生成引擎  ← 本交接主体（世界观智能体 / 模块 B）        ★
  [C] 视频 ingestion（ffmpeg 抽帧 + STT 转写 + 百炼 Qwen-VL 读画面）
  [D] 代入推理（每个虚拟观众独立调用，打分 + 原话式刺点）
  [E] 报告聚合（总分表 + 共性诊断 + P0/P1/P2 调整清单）
  [F] 权限 / 计费 / 落盘（接 api.lcppch.top:3007，按次付费）
```

- B 上游接 A（Brief），下游供 D（`personas[]` + `registry_id`）。
- 模型选型遵循既定架构：推理主模型 = 思潼默认 DeepSeek 经阿里百炼；视频理解必须补**百炼 Qwen-VL**（纯文本 DeepSeek 看不了视频）；STT 百炼 / 讯飞二选一。

---

## 3. 模块 B 核心规格（精炼，完整版见 `agent-specs/商户测试团_人设生成引擎_子模块规格.md`）

### 3.1 输入：结构化 Brief（来自模块 A）
| 字段 | 必填 | 说明 |
|------|:---:|------|
| industry | ✅ | 行业：餐饮/美业/实业/大健康/招商/教培… |
| pain_points | ✅ | 核心痛点列表（至少 1 个） |
| count | ❌ | 生成人数，默认 5，范围 3–7 |
| region / scale / decision_maker / decision_style / focus_axis / seed_personas | ❌ | 地域/规模/决策者/主导风格/重点轴/自有客户种子 |

自由文本兜底：模块 A 收到自由文本时先解析为上述结构；解析失败则向用户追问必填项（industry / pain_points / count）。

### 3.2 输出：Persona Schema（存入人设注册表）
每位人设必含：`id, name, title, dimension_tags(5 轴), physiology, sociology, psychology, core(desire/fear/lie), voice(声线卡), criteria[], stance`。
完整 JSON Schema 与示例见附档第 3 节。

### 3.3 生成管线（6 步）
```
解析 Brief → 维度正交分配(代码层) → 模板匹配(可选) → LLM 填细节 → 一致性校验 → 注册表落盘
```
建议**两阶段实现**：Step 2（控轴）+ Step 5（校验）用代码 deterministic 执行；Step 4（填细节）用一次 LLM 调用批量产出 count 个。既可控又省 token。

### 3.4 维度正交防同质化（核心，否则测试失效）
定义 5 条正交轴：
`decision_style 决策风格` / `price_sensitivity 价格敏感` / `trust_threshold 信任门槛` / `digital_level 数字化程度` / `risk_appetite 风险偏好`。
- 以 `decision_style` 为主轴，count 个人轮流取不同值（如 5 人 → 结果导向/怕被坑/面子型/合规型/价格敏感 各一）。
- 其余轴在 ±1 档内互斥，保证任意两人 `dimension_tags` 签名不完全相同；完全相同则扰动重排。
- 若 `focus_axis` 指定（如"价格敏感度"），该轴升为主轴优先拉开。
目的：生成出来的 N 个人"像不同真实客户"，而不是同一模板换名字。

### 3.5 声线卡（防 OOC 关键）
批次内口癖互不重叠，遮名可辨是谁在说话；句式/比喻/禁忌与身份自洽。

### 3.6 可复现
- 稳定 id：`P_{日期}_{Brief哈希前4位}_{序号}`，相同 Brief 产出相同 id 序列。
- 种子固定：LLM 调用携带 `seed = hash(Brief + index)`；不支持 seed 则用 `temperature=0 + 固定 few-shot` 近似。
- 注册表缓存：同一 `registry_id` 二次测试直接复用，跨轮对比有效、零成本。

### 3.7 红线（必须写进实现 / 系统提示词）
- 不编造行业 / 画像不存在的痛点或能力。
- `criteria / stance` 不得夹带"私信/找我/留电话"等平台违规引导语。
- `industry` 缺失必须追问，不默认、不替用户决定行业。

---

## 4. 开发任务分解（周凯要做的）

1. **实现模块 B 人设生成 API**：输入 Brief → 输出 `personas[] + registry_id`，严格按第 3 节规格。
2. **维度正交分配器（代码层）**：deterministic 控轴，保证批次差异、签名不重复。
3. **人设注册表存储**：思潼存储，`registry_id → personas[]` 索引；支持复用与跨轮比对。
4. **可复现机制**：seed / temperature=0 + 固定 few-shot；确认百炼是否支持 seed 参数。
5. **行业预设 archetype 库**：先人工沉淀 5 类（餐饮/美业/实业/大健康/招商）底稿，命中则载入。
6. **接入视频测试 skill**：B 输出喂给 D（代入推理），并接 F 计费（按次）。
7. **单元测试**：按附档第 8 节验收 7 项自测。

---

## 5. 里程碑（讨论用，整体视频测试智能体）

- **M0 原型验证**：固定 5 人 + 真实视频 ingestion（ffmpeg + STT + Qwen-VL）跑通端到端，验证"真能看视频"。
- **M1 人设生成引擎（模块 B）**：输入 Brief → 批量生成人设（含防同质化 + 注册表）。**本交接的主体**。
- **M2 通用版 SKU**：开放目标用户输入，动态生成 + 测试，上思潼 AI 商城。
- **M3 计费 / 权限 / 可视化**：接 api.lcppch.top:3007，报告可视化（雷达图 / 热力图）。

---

## 6. 待周凯确认的议题

| # | 议题 | 需要拍板的点 |
|---|------|------------|
| 1 | **seed 支持** | 百炼 DeepSeek / Qwen 是否支持 `seed`？不支持则用 `temperature=0 + 固定示例` 近似可复现 |
| 2 | **生成实现方式** | 代码 deterministic 控轴 + LLM 填细节（建议），还是纯 LLM 一次性？ |
| 3 | **archetype 库** | 是否先人工沉淀 5 类行业底稿？建议 M1 先做 |
| 4 | **注册表 key 设计** | `registry_id → personas[]` 的存储结构 |
| 5 | **成本测算** | 人设生成成本可忽略（单次 1 次 LLM ~3–4k token）；主成本在视频理解（多模态图 + 长 transcript），需先估单次诊断总成本以定档位 |

---

## 7. 已有资产（本仓库 `agent-specs/` 附档）

| 文件 | 内容 |
|------|------|
| 商户测试团_人设生成引擎_子模块规格.md | 模块 B 完整规格（IO 契约 / 管线 / 维度正交算法 / 提示词 / 验收 / 成本 / 红线） |
| 商户测试团智能体_skill规格.md | 整体 skill（系统提示词 + 5 人设 + IO + 状态机 + 验收 + 计费） |
| 商户测试团智能体_技术合伙人交接.md | 整体视频测试智能体的讨论稿（含世界观现状、架构、议题） |
| 商户测试团_虚拟形象设定.md | 5 位商户人设卡（Egri 三维 + 声线卡 + 评判标准） |
| 商户测试团_第一轮测试报告.md | 示例输出 + V1 诚实更正 |

---

## 8. 全局红线（视频测试智能体，必须写进系统提示词）

- ❌ 不编造视频里没有的功能 / 数据（如敏感词过滤，有就讲没有别编）。
- ❌ 生成 CTA 建议禁用"私信/找我/留电话"等违规引导语，改用"评论区扣关键词"等安全说法。
- ❌ 输出开头必须声明"评估为内容层受欢迎度，非平台真实播放数据"。
- ❌ 尊重创作者主权：智能体只诊断建议，不替用户改视频、不署名。

---

## 9. 给周凯的下一步

1. 过一遍第 6 节议题，逐项给判断 / 替代方案。
2. 确认 M0 是否先解决"真能看视频"（Qwen-VL + STT）再谈人设生成。
3. 确认模块 B 是做成**通用子能力**，还是只内嵌在视频测试 skill 里。
4. 评估成本后回定档位（建议诊断一次 ¥9.9，预览免费）。
5. 确认后由 Codex 按 `agent-specs/` 规格工程化落地到思潼后端。
