# 枕水江南外卖增长智能体｜任务交接清单

更新时间：2026-08-10  
项目目录：`F:\思潼AI增长os\baolu-os-v2-source`  
线上地址：<https://api.lcppch.top/os-v2/agents/takeaway-growth>

## 一、项目目标

为枕水江南单独开发一套外卖增长智能体，第一阶段先供枕水江南总部和门店测试。系统不是自由聊天框，而是按照固定任务地图和工作台完成：

1. 导入真实经营数据；
2. 数据口径审计；
3. 老店增长或新店突破诊断；
4. 生成待审批的单变量实验；
5. 门店执行并回填；
6. 复盘后决定继续、调整或停止。

核心要求：所有诊断必须由真实数据驱动，明确区分已确认事实、方向性信号和待验证假设，不能凭模型自由发挥。

## 二、已经完成并上线的内容

### 1. 枕水江南专属版本

- 品牌名称固定为“枕水江南外卖增长智能体”。
- 已有独立线上入口。
- 已接入枕水江南品牌工作台和右侧外卖经营数据看板。

### 2. 任务地图与五步工作台

- 已实现外卖增长任务地图。
- 已包含数据口径审计、老店增长、新店业绩突破、增长总诊断、菜单货盘与利润、活动投放诊断、流失竞品诊断、单变量实验、周期复盘等任务。
- 从任务地图进入任务后，只运行对应模块。
- 默认工作区使用五步工作台，避免自由对话误判用户语义。

主要代码：

- `apps/web/src/components/takeaway/TakeawayGrowthWorkbench.tsx`
- `apps/web/src/components/work-map/AgentWorkMap.tsx`
- `apps/web/src/pages/AgentProductsApp.tsx`
- `apps/web/src/styles/agent-products.css`

### 3. 数据智能导入

- 支持多文件选择。
- 支持 XLSX、XLS、CSV、TSV、JSON、TXT。
- 支持表头前有标题或说明行。
- 已增加枕水江南菜品货盘模板的专门识别规则。
- 已实现订单号单向哈希，顾客姓名、电话、地址不进入经营看板。
- 当前页面显示已成功导入 16/16 份文件，共约 12,792 行。

主要代码：

- `apps/api/src/services/takeaway-growth-data.ts`
- `apps/web/src/components/takeaway/TakeawayGrowthDataPanel.tsx`

### 4. 当前已识别的数据范围

从最新页面截图看，系统当前识别为：

- 已识别：订单、退款与实付；
- 已识别：菜品价格与货盘；
- 未识别：菜品销量；
- 未识别：活动与投放；
- 数据可信度：61/100；
- 主要缺失字段：`costAmount`、`orderId`、`refundAmount`。

注意：页面显示“未识别菜品销量”和“未识别活动与投放”，并不等于上传失败。当前 16 份文件已入库，但文件中是否存在可结构化识别的销量、活动名称、消耗、曝光、点击、进店、下单、成交额等字段，还需要下一任务继续核查。

### 5. 诊断数据传递逻辑

当前诊断会使用企业当前已成功导入的文件数据，但不会把 16 个原始文件全文直接塞给大模型。流程是：

`原始文件 → 字段识别与标准化 → 去重和口径检查 → 经营看板汇总 → 生成 dataContext/contextPrompt → 固定诊断模块调用模型`

因此，模型收到的是这 16 份文件汇总后的订单、漏斗、菜品、货盘、投放、数据缺口和口径警告。未被解析器识别的内容不会自动进入结构化诊断上下文。

相关代码：

- `apps/web/src/components/takeaway/TakeawayGrowthDataPanel.tsx` 中的 `diagnosisPrompt` 和 `onDashboardChange`。
- `apps/web/src/components/takeaway/takeaway-workbench-config.ts` 中的 `buildTakeawayWorkbenchPrompt`。
- `apps/api/src/services/takeaway-growth-data.ts` 中的 `buildTakeawayDashboard`。

### 6. 已去掉独立长文结果页

- 已删除工作台下方独立的“资料卡｜外卖增长第一版”长文结果页。
- 诊断、实验和复盘结果只显示在当前工作台任务内。
- 生成失败和待补信息也在当前任务内提示。
- 已移除“查看下方最新结果”入口和旧结果页样式。

最后一次相关生产发布：

- Release ID：`zhenshui-takeaway-clean-result-page-20260810-175000`
- 服务状态验证：`ActiveState=active`、`NRestarts=0`
- 页面验证：HTTP 200

### 7. 已完成的测试

- Web TypeScript 类型检查通过。
- Web 生产构建通过。
- `pnpm.cmd takeaway:workbench-smoke` 通过。
- WorkBuddy 已进行 V5、V6、V7、V8 多轮验证。
- V7 已验证核心链路可以创建 run、返回诊断和生成实验卡。
- V8 已验证生成实验卡后停留在第 4 步，需用户确认后才能进入第 5 步。

## 三、尚未完成、下一任务必须继续的事项

### P0-1：核查“菜品销量”为何未识别

待做：

1. 使用枕水江南本地 16 份原始文件逐一运行解析器；
2. 输出每个文件、每个工作表识别到的表头、字段映射和 `sourceKind`；
3. 检查订单明细中是否实际包含菜品名称、数量、商品实付；
4. 如果订单明细存在菜品字段，应允许从订单商品行聚合菜品销量，而不是只依赖独立的“销量”字段；
5. 如果只有菜品销量截图而没有表格，需要明确标为“图片证据未结构化”，不要显示成文件未上传。

初步代码风险：

- `buildTakeawayDashboard` 已支持在没有独立 products 表时从 orders 中聚合菜品；
- 但页面 `coverage.products` 目前仅判断 `dashboard.products.length > 0`；
- 若订单表被解析成订单级而没有 `productName/quantity`，则无法生成菜品销量；
- 需要核对美团、淘宝闪购实际订单明细表头是否被现有 alias 命中。

### P0-2：核查“活动与投放”为何未识别

待做：

1. 检查“每天总单总数据”是否包含推广消耗，但缺少活动名称；
2. 检查字段是否叫“推广时段消耗”“推广花费”“营销费用”等现有规则尚未覆盖的名称；
3. 如果文件只有总推广消耗而没有活动名称，应识别为“投放汇总”，而不是完全未识别；
4. 活动诊断需要区分：完整活动明细、仅有投放汇总、仅有平台截图三种可信度；
5. 不得把普通平台补贴自动当成广告投放消耗。

现有活动识别的关键字段：活动名称、消耗、曝光、点击、下单、成交额。代码在 `takeaway-growth-data.ts` 的 `fieldAliases`、`reliableChineseFieldAliases` 和 `inferSourceKind`。

### P0-3：增加“进入菜单货盘与利润诊断”按钮

用户已明确要求：数据口径审计结论中的“下一步：可进入菜单货盘与利润诊断”不能只是文字，必须有直接按钮。

建议实现：

- 在审计结果卡底部增加主按钮“进入菜单货盘与利润诊断”；
- 点击后：
  - 普通五步工作台：设置 `moduleId="menu"`，如尚未选择阶段则先进入阶段选择；已选择阶段则进入 Step 3；
  - 任务地图审计页：退出当前审计任务并直接打开 `takeaway_menu_profit` 任务；
- 如果只有货盘价格、没有成本，按钮仍可进入，但页面必须标注“可做货盘与价格结构分析，利润结论需补成本”；
- 如果有成本和价格，则显示“可进行利润诊断”。

### P0-4：测试积分不足

用户要求为当前测试账号提供充足测试积分，避免反复提示积分不足。

当前状态：**尚未完成充值或积分调整。**

原因：上一轮只完成了积分表和代码位置检索；数据库查询命令的引号转义失败，随后任务被中断。没有任何积分数据被修改。

下一任务建议：

1. 先在生产数据库只读查询当前登录用户所属 Tenant 和 `CreditAccount.balance`；
2. 通过有审计记录的方式增加测试积分，例如 1,000,000；
3. 同时写入 `CreditTransaction`，`direction=grant` 或 `adjust`，备注“枕水江南外卖智能体测试额度”；
4. 不建议只改 balance 而不留交易记录；
5. 修改后通过账户 API 和页面顶栏确认余额；
6. 如需真正“无限”，建议增加白名单测试租户免扣费配置，而不是使用数据库超大数值。第一阶段直接给 1,000,000 测试积分更安全。

相关数据表：

- `CreditAccount`
- `CreditTransaction`
- Prisma 定义：`packages/db/prisma/schema.prisma`

### P1：验证本轮诊断确实携带已导入数据

虽然从代码链路可以确认会传入 `dataStatus.contextPrompt`，仍建议做一次可审计验证：

1. 在 run 创建前记录非敏感上下文摘要，例如导入文件数、行数、数据日期、coverage 和质量分；
2. 不记录原始顾客信息；
3. 运行菜单货盘诊断；
4. 检查 AgentRun 输入快照或运行步骤，确认包含 16 份文件汇总后的 data context；
5. 前端结果卡增加“本次使用数据：16 份文件 / 12,792 行 / 更新至 2026-07-31”的证据标签，让测试者不用猜。

## 四、本地枕水数据位置（涉密，禁止外发）

仅限授权查阅：

- `C:\Users\book\Desktop\枕水江南AI运营项目\枕水江南中街店数据导出（涉密内部数据，仅限授权查阅，禁止流转外泄！）.zip`
- `C:\Users\book\Desktop\枕水江南AI运营项目\中街店一个月内双平台数据（涉密内部数据，仅限授权查阅，禁止流转外泄！）.zip`
- 已解压补充数据：`C:\Users\book\Desktop\枕水江南AI运营项目\AI分析结果_仅限授权查阅\supplement_20260807_095335\`

不要在报告、聊天回复、日志或测试截图中输出订单级原始数据和顾客信息。

## 五、开发与发布方法

### 本地验证

```powershell
pnpm.cmd --filter @baolu/web typecheck
pnpm.cmd takeaway:workbench-smoke
$env:VITE_API_BASE_URL='https://api.lcppch.top/os-v2/api'
$env:VITE_BASE_PATH='/os-v2/'
pnpm.cmd --filter @baolu/web build
```

### 生产服务器

- SSH：`admin@api.lcppch.top`
- 本机密钥：`C:\Users\book\.ssh\sales_copilot_20260618`
- 部署脚本：`/opt/baolu-os-v2/scripts/deploy-zhenshui-takeaway-production.sh`
- 服务：`baolu-os-v2`
- 生产目录：`/opt/baolu-os-v2`

每次使用新的 Release ID。部署完成后检查：

```bash
systemctl show baolu-os-v2 -p ActiveState -p SubState -p MainPID -p NRestarts
curl -fsS http://127.0.0.1:3002/ready
curl -sS -o /dev/null -w '%{http_code}' https://api.lcppch.top/os-v2/agents/takeaway-growth
```

部署脚本切换服务后有时会短暂出现 `curl: (7) connection refused` 重试输出；只要脚本已输出 `ZHENSHUI-TAKEAWAY-DEPLOYMENT-SUCCEEDED`，随后再次检查 `/ready`、HTTP 200、服务状态和重启次数即可。

## 六、新任务窗口建议直接使用的开场指令

请继续开发“枕水江南外卖增长智能体”。先完整阅读：

`F:\思潼AI增长os\baolu-os-v2-source\枕水江南外卖增长智能体-任务交接清单-20260810.md`

按优先级完成：

1. 给当前测试租户增加有审计记录的 1,000,000 测试积分，并验证页面不再提示积分不足；
2. 用本地 16 份涉密文件逐项复现并定位“菜品销量、活动与投放未识别”的原因，修复字段识别和 coverage 判定；
3. 在数据审计结果中增加“进入菜单货盘与利润诊断”按钮，并确保普通工作台和任务地图两种入口都能正确跳转；
4. 在诊断页面显示本次实际使用的数据证据摘要；
5. 完成类型检查、解析器测试、工作台冒烟测试、生产构建与上线部署；
6. 最后给出测试步骤和修复结果，不得泄露枕水原始数据。

