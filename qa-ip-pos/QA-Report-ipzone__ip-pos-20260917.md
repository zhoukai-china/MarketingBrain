# OS-v2 IP定位智能体 QA 报告（修正版）
**智能体：** ipzone__ip-pos（IP定位智能体 · 创始人IP专区）
**日期：** 2026-09-17
**实例：** 测试 `lanqi-test/agent/ipzone__ip-pos/chat`（免登录）｜生产 `os-v2/agent/ipzone__ip-pos/chat`（微信扫码）
**方法：** Puppeteer 驱动内置 Chromium，真实账号扫码走查 + DOM 进度条抓取

---

## 一、总体结论

✅ **生产实例（真实账号）全流程通过**：6 步访谈 → 确认卡 → 生成全案（8 章完整）→ 0 console/网络错误，会话持久化正常，两次走查各消耗 400 积分（共 800）。

⚠️ **上一版报告的 2 项判定已证伪（误判）**，本次真实账号复测修正：
- 「P1 进度条不推进」→ **实际每步正确推进**（误判源于测试账号在生成阶段被 402 截断，截到的只是中断残留快照）
- 「P3 第6步按钮文案提前变『确认需求』」→ **实际仅最后一步（第6步）变『确认需求』，前5步均为『下一步』**，符合设计

🔴 **唯一真问题（低优先级）**：低积分账号触发 402 时，报错文案自相矛盾（"本次不消耗积分" + "当前积分不足"）。真实账号不触发，不阻塞上线。

---

## 二、真实账号补测详情

### 2.1 走查路径（6 步全部走通）
1. 角色适配 → 2. 项目基础 → 3. 竞争格局 → 4. 目标用户 → 5. 创始人 + 目标 → 6. IP 现状与能力 → 确认卡「确认需求」→「✓ 确认，开始生成」→ 全案产出

### 2.2 进度条推进验证（核心证据，每步点击后抓取 DOM）
判定规则：已完成步骤的 DOM 祖先含 `chat-bubble ai`（转为聊天气泡）；未完成步骤为 `chat-prog todo`。

| 走完第 N 步后 | 已完成（转气泡） | 待办（todo） | 结论 |
|---|---|---|---|
| step 0 | 角色适配、项目基础 | 竞争格局、目标用户、创始人+目标、IP现状 | ✅ |
| step 1 | + 竞争格局 | 目标用户、创始人+目标、IP现状 | ✅ |
| step 2 | + 目标用户 | 创始人+目标、IP现状 | ✅ |
| step 3 | + 创始人+目标 | IP现状 | ✅ |
| step 4 | 全部 6 步 | 无 | ✅ 全部完成 |
| step 5 | 全部 6 步（含 report-table） | 无 | ✅ 进入确认卡 |

**结论：进度条逐步推进完全正常。**

### 2.3 全案生成结果
- 0 报错，耗时约 50s 流式生成
- 实际消耗 **400 积分**（顶部显示「本次实际消耗 400 积分」，账号余额 3040 → 2640）
- 全案结构完整（8 章）：1分钟速览 / 项目定位 / 目标用户定位 / IP人设定位（五维模型+IP原型+视觉锤+声音钉）/ 主页四件套 / 语言风格 / 内容定位（四型矩阵）/ 选题方向（80条+TOP10+月历）/ 投流建议（DOU+本地推+预算）/ IP发展规划（能力评估+三阶段）/ 执行建议
- 多处「待补充：需用户提供…」占位，属设计预期（首轮 6 步信息有限，全案为初稿需迭代）

### 2.4 会话持久化
重新打开 chat URL（带登录态）直接恢复上次完整对话 + 全案，底部有「再问一次 / 重新开始」按钮。正常。

---

## 三、测试实例 vs 生产实例（复核）

| 项 | 测试实例（免登录） | 生产实例（真实账号） |
|---|---|---|
| 匿名访问 | 直接进对话（测试账号） | 第 0 步登录墙拦截 ✅（正确） |
| 6 步访谈 | 走通 | 走通 |
| 生成全案 | ❌ 402（测试账号仅 100 积分 < 400） | ✅ 成功（消耗 400 积分） |
| 全案内容 | 未产出（被 402 截断） | 8 章完整产出 |
| console/网络错误 | 无 | 无 |

**结论：测试实例的 402 是测试账号积分配置不足，非生产逻辑缺陷。生产实例可用。**

---

## 四、修正后的 Bug 清单（给 Codex）

### 已排除（误判，无需修复）
| 原判定 | 修正 |
|---|---|
| P1 进度条不推进 | ❌ 误判。真实账号复测每步正确推进（见 §2.2） |
| P3 第6步按钮文案提前变「确认需求」 | ❌ 误判。仅第6步变「确认需求」，前5步「下一步」，符合设计 |

### 真实待处理（低优先级）
| 优先级 | 问题 | 现象 | 建议 |
|---|---|---|---|
| P2 | 低积分 402 文案自相矛盾 | 报错同时写「本次不消耗积分」和「当前积分不足」 | 统一文案：明确「积分不足，本次未消耗，请充值后重试」 |
| P3 | 开场白步数与 UI 不一致 | 智能体说「走 5 轮：项目→竞争→用户→创始人/目标→IP现状」，但进度条是 6 步（含「角色适配」前置） | 开场白改为「6 步访谈」或把角色适配并入口径，避免用户数出来是 6 步却听说是 5 轮 |

---

## 五、给 Codex 的最终建议

1. **生产实例可放心交付**：真实账号端到端跑通，全案质量高，无需因本 QA 阻塞上线。
2. **仅建议优化 2 处文案**（P2 低积分提示、P3 开场白步数），均为体验层，不阻塞。
3. **测试实例**：如需在测试环境复现「全案生成」验证，请给免登录测试账号充值 ≥500 积分，或在 `lanqi-test` 环境临时跳过计费，否则只能走到 402。
4. **进度条 / 按钮文案无需改动**：上一版报告的两项 P1/P3 已证伪。

---

## 六、证据文件
- `progress_log.json` — 每步进度条 DOM 状态（证伪「不推进」）
- `final_real.txt` — 真实账号生成的全案纯文本（8 章完整）
- `final_real.png` — 全案截图
- `after_step0.png` ~ `after_step5.png` — 每步截图
- `qr_full.png` — 登录二维码
- `login_real.log` — 走查完整日志（0 错误）
- `dom_check.json` — 重开页面DOM（证会话持久化）

---

## 七、给 Codex 的一句话交接话术（HANDOFF）

> OS-v2 IP定位智能体（ipzone__ip-pos）生产实例端到端 QA 已通过：真实账号走完 6 步访谈→确认卡→8 章完整全案，0 报错，单次消耗 400 积分，会话持久化正常；此前 v1 报告的「进度条不推进」与「第6步按钮文案提前变化」两项已用 DOM 证据（progress_log.json）证伪，无需改动；仅 2 处低优先级文案待优化——①低积分 402 提示同时写「本次不消耗积分」与「当前积分不足」自相矛盾；②开场白说「5轮」但 UI 为 6 步——均不阻塞上线；测试实例免登录账号仅 100 积分，复现全案生成需充 ≥500 积分或临时跳过计费。完整报告见 `QA-Report-ipzone__ip-pos-20260917.md`。
