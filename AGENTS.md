# 思潼 AI 增长 OS：开发与质量规则

本文件适用于整个 `baolu-os-v2-source`。目标是让 Codex 独立完成开发、测试、验收、修复和回归，把人工工作缩减为高价值产品验收。

## 一、每个任务的必做流程

1. 先阅读 `docs/QUALITY_WORKFLOW.md`、`docs/TEST_PLAN.md` 和 `docs/BUG_REGRESSIONS.md`。
2. 检查当前实现、相关文档、已有测试和可用脚本；不要凭旧交接内容猜测当前状态。
3. 把需求转换为可验证的验收条件，至少包括正常路径、失败路径和“不应发生”的结果。
4. 修改前运行最小相关基线检查。若基线已经失败，记录失败证据并区分“既有失败”和“本次引入失败”。
5. 做最小、可回滚的修改；不要顺手重构无关模块。
6. 修改后先运行受影响测试，再运行与风险等级匹配的质量门禁。
7. 对页面或真实用户流程，启动应用并用浏览器或 Computer Use 实际操作；不能只依赖类型检查。
8. 最终报告必须列出修改文件、验收条件、执行命令、结果、未运行项和残余风险。

## 二、开发规则

- 生产路径是 `apps/web`、`apps/api`、`packages/*`、`prisma`、`mcp-skills` 和明确的运行脚本。不要直接修改 `dist`、发布压缩包、线上抓取的 JS、临时目录或备份副本来代替源代码修复。
- 保持现有 pnpm monorepo、TypeScript 和项目命名约定。新增依赖前先确认现有依赖或原生能力不能满足需求。
- 所有身份、租户、订单、积分、文件、会话和 Agent 运行数据必须维持租户隔离。任何相关改动都要测试“租户 A 无法读取或影响租户 B”。
- 外部动作默认只生成预览或草稿。没有用户确认，不自动投放、付款、发布、发消息或改生产配置。
- 不在代码、夹具、日志、截图或文档中写入真实密钥和客户敏感数据。
- 修复根因，不用吞异常、放宽断言、删除测试或硬编码假结果来获得通过。

## 三、分层测试门禁

### 开发中快速检查

运行：

```powershell
pnpm qa:fast
```

它覆盖结构检查、Skill 质量资产、Eval 结构、全仓类型检查。对小改动可先运行更窄的对应命令，但交付代码前至少运行 `pnpm qa:fast`。

### 智能体和业务回归

涉及 Agent 路由、提示词、Skill、工具调用、质量门禁、数据合成或核心业务逻辑时，运行：

```powershell
pnpm qa:regression
```

还必须按 `docs/TEST_PLAN.md` 选择受影响领域的专项 smoke/acceptance 命令。

### 完整本地质量门禁

跨模块改动、Bug 修复完成、准备交付或准备发布时，运行：

```powershell
pnpm qa:full
```

`qa:full` 通过不代表可以直接上线。上线前仍需测试环境 E2E、生产配置预检、关键接口 smoke、页面验收、备份和回滚验证。

Windows PowerShell 若因执行策略拦截 `pnpm.ps1`，使用完全等价的 `pnpm.cmd <命令>`；这属于命令入口问题，不得误报为项目测试失败。

### 文档或纯素材改动

只改文档或素材时可不运行全量代码门禁，但必须验证文件可读、链接/路径正确；文档、PDF、Word、表格或图片还要做对应的渲染和视觉检查。最终报告说明为什么没有运行代码测试。

## 四、Bug 修复强制闭环

任何 Bug 都严格执行以下顺序：

1. 独立复现并保存最小证据：输入、环境、步骤、预期、实际、日志或截图。
2. 定位根因，区分现象、根因和连带影响。
3. 先新增一个能在修复前失败的自动回归测试或 Eval；如果技术上无法自动化，记录原因并建立可重复的人工验收步骤。
4. 运行新增测试，证明它因该 Bug 失败，而不是测试本身错误。
5. 实施最小修复。
6. 重新运行新增测试、相邻测试和受影响领域测试。
7. 按风险运行 `pnpm qa:fast`、`pnpm qa:regression` 或 `pnpm qa:full`。
8. 在 `docs/BUG_REGRESSIONS.md` 登记 Bug、根因、测试和验证命令。

无法复现时禁止猜测式修改。先补日志、最小复现或可观测性。

## 五、智能体与 AI 行为验收

智能体行为不能只靠“看起来不错”。每次相关变更至少评估：

- 路由是否选择正确 Agent、Skill 和工具；工具参数是否有效。
- 已知事实是否准确，缺失数据是否明确标记为待补，是否编造案例、数字、效果或已执行动作。
- 输出结构、必填章节、业务规则、权限边界和禁止事项是否满足契约。
- 工具失败、超时、取消、部分失败和重复请求是否有正确降级、幂等和扣费行为。
- 上下文、客户、租户和 Agent 之间是否串数据。
- 同一高风险样例至少重复运行 3 次；出现偶发失败时不得以单次通过放行。

新能力先建立 Eval 基线，再调整提示词、模型、检索或路由。优先测试用户实际经过的应用路径，不只测试裸提示词。每个真实 Bug 和客户反馈都应转成脱敏回归案例。

## 六、页面与 E2E 验收

涉及 UI 时必须在实际页面验证：

- 桌面和移动端关键尺寸。
- 加载、空状态、错误、重试、重复点击、刷新、返回和网络失败。
- 核心路径可完成，按钮状态、结果显示、下载/导出和导航正确。
- 浏览器控制台无新增错误，关键请求状态和错误提示正确。
- 不泄露跨租户数据、内部提示词、密钥、堆栈或客户隐私。

使用 `docs/TEST_PLAN.md` 中的用户旅程。自动化暂未覆盖的页面路径，可以先用 Browser/Computer Use 验收，但稳定后必须沉淀为可重复 E2E。

## 七、缺陷等级和放行标准

- P0：数据泄露/破坏、付费错误、严重安全问题、核心系统不可用。发现即停止发布。
- P1：核心路径失败、主要智能体不可用、严重错误输出或无法恢复。修复并回归后才能交付。
- P2：非核心功能错误、有替代路径。记录负责人和后续计划。
- P3：轻微 UI、文案或低频问题。可进入需求池，但不能伪装成已修复。

放行条件：无未解决 P0/P1；相关验收条件全部通过；要求的自动检查通过；残余 P2/P3 已明确记录；发布时具备回滚方案。

## 八、最终回复格式

最终回复至少包含：

```text
结果：完成 / 部分完成 / 阻塞
修改：文件和行为
验收：逐条验收条件及结果
测试：实际执行的命令及 PASS/FAIL
未运行：命令、原因和影响
风险：残余问题、P2/P3、环境限制
下一步：仅列确实需要用户处理的事项
```

禁止使用“应该可以”“大概没问题”代替证据。命令没有执行就不能写“测试通过”，页面没有实际操作就不能写“页面正常”。

## 九、智能体持续学习与受控自我迭代

涉及 Agent、Skill、提示词、路由、工具、知识库或质量门禁时，必须同时阅读并执行 `docs/CONTINUOUS_IMPROVEMENT.md`。

- 新能力必须定义可观测的成功、失败、用户结果事件和硬门禁，不能只记录最终文本。
- 稳定用户反馈和真实 Bug 必须先沉淀为脱敏 Eval/回归，再修改实现。
- 优化前保存 Champion 基线；Challenger 一次只改变一个主要变量，并用同一批 Eval 对比。
- 跨租户、泄露、编造、越权和错误执行是不可被平均分掩盖的硬失败。
- 每日学习任务只允许生成评分、聚类、Eval 草稿和改进候选；禁止自动修改或激活生产 Prompt、Skill、路由、模型、知识或代码。
- 候选必须经过人工根因复核、离线 Eval、`pnpm qa:full`、人工批准、小流量灰度和可验证回滚。
- 报告禁止包含客户原始输入、模型原始输出、反馈备注、密钥或客户标识；租户偏好不得未经确认提升为全局规则。

## 十、单人开发自动分流与任务交接

用户不需要判断需求属于公共平台还是某个智能体。收到涉及外卖增长、创始人 IP 获客或兰琪美业的开发请求后，Codex 必须先执行 `docs/agents/AGENTS.md`：

1. 自动识别产品归属和变更层级；无法唯一判断时，先做只读检查，只有会改变产品方向、知识授权或计费规则时才询问用户。
2. 一个 Codex 任务只交付一个可独立验收的主要用户结果；同一智能体不得同时存在两个编码任务。
3. 开发前读取对应产品目录的 `PRODUCT.md`、`WORKFLOW.md`、`CONTRACTS.md`、`TEST_MATRIX.md`、`STATUS.md` 和当前任务卡。
4. 若当前任务卡不存在，由 Codex 按 `docs/agents/_templates/TASK_TEMPLATE.md` 创建；用户只需描述想实现的结果。
5. 完成或中止时由 Codex 更新任务卡和 `STATUS.md`，新对话从仓库文档恢复上下文，不要求用户复制旧聊天。
6. 不得把客户原始资料、密钥或敏感经营数据写入 `docs/agents`；客户资料只放受控客户项目区，测试使用脱敏或合成数据。
7. 不允许为了“目录整齐”一次性搬迁生产代码。模块拆分必须作为独立任务，先有回归再移动。

当前产品目录：

- `docs/agents/takeaway-growth/`：外卖增长智能体。
- `docs/agents/founder-ip-acquisition/`：创始人 IP 获客系统，覆盖招商加盟、C 端团购到店、学员招募和合作方招募。
- `docs/agents/lanqi-beauty/`：兰琪美业经营增长智能体平台。

跨产品并行仅允许在独立 Git worktree/分支中进行，且最多两个编码任务。若两个任务都要修改公共热点文件，Codex 必须将其串行化或先完成独立的公共平台抽取任务。

## 十一、WorkBuddy Skill 候选与正式运行资产隔离

- WorkBuddy 生成或维护的 Skill 只能作为候选输入/样本，保存在 `mcp-skills/candidates/workbuddy-*` 或明确的 `intake/quarantine` 区；生产和验收运行时不得直接调用、软链、动态加载或隐式依赖候选原件。
- 候选进入正式目录前，Codex 必须独立验收正式 Schema、事实保留、行业污染、合规边界、失败关闭、稳定性/可复现性和老板可直接使用性，并保留脱敏 Eval 与来源 provenance。
- 只有验收通过的候选才可复制并重新归属到仓库 `mcp-skills/skills/<skill>`；正式副本必须有独立版本、合同、Eval、owner/provenance 和升级记录。运行时只引用正式副本，绝不回调 WorkBuddy 原件；WorkBuddy 后续产物也不得自动覆盖正式 Skill。
- 产品运行 Skill 不得放在个人级 `C:\Users\book\.codex\skills`。该目录属于 Codex 客户端能力，不是本产品仓库的版本化运行资产。
- Web 与 WorkBuddy 可以调用同一份已经验收的产品正式 Skill 合同，但候选来源、开发区和运行依赖必须隔离。复制或升级正式 Skill 若改变产品输出或公开能力，仍须先红灯、专项 Eval 和完整放行门禁。
