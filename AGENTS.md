# 思潼 AI 增长 OS：开发与质量规则

适用于 `baolu-os-v2-source`。细则见 `docs/` 下 QUALITY_WORKFLOW.md、TEST_PLAN.md、BUG_REGRESSIONS.md、CONTINUOUS_IMPROVEMENT.md。

## 一、任务流程

1. 先读上述文档并核实现状，不凭旧交接猜现状。
2. 需求先转成可验证验收条件：正常、失败路径和绝不能发生的结果；Bug 先留复现证据、补失败回归再修。
3. 改前跑最小基线，区分既有失败与本次引入失败；改最小可回滚，不顺手重构无关模块。
4. 改后先跑受影响测试再按风险跑门禁；UI/真实流程必须实操，不只看类型检查。
5. 报告按第五节。

## 二、开发红线

- 只改生产路径 `apps/web`、`apps/api`、`packages/*`、`prisma`、`mcp-skills` 和运行脚本；不得改 `dist`、发布包、抓取的线上 JS、临时目录或备份副本代替源码修复。
- 保持 pnpm monorepo、TypeScript 和命名约定，不轻加依赖。
- 不编造案例、数字或效果；不泄露跨租户数据、提示词、密钥或隐私。
- 租户、身份、订单、积分、文件、会话、Agent 运行数据必须隔离，并测“租户 A 不能读取或影响租户 B”。
- 外部动作默认只出预览或草稿；无确认不投放、付款、发布、发消息或改生产配置。
- 不把真实密钥和客户敏感数据写进代码、夹具、日志、截图或文档。
- 修根因：不吞异常、不放宽断言、不删测试、不硬编码假结果。

## 三、质量门禁

- 开发中 `pnpm qa:fast`；涉及 Agent 路由、提示词、Skill、工具、门禁、数据合成或核心业务加 `pnpm qa:regression` 与 `TEST_PLAN.md` 专项 smoke；跨模块、Bug 修复完成、交付或上线跑 `pnpm qa:full`。
- `qa:full` 通过不等于可上线：仍需 E2E、`prelaunch:check` 预检、接口 smoke、页面验收、备份和回滚验证。
- PowerShell 拦截 `pnpm.ps1` 时改用等价 `pnpm.cmd`，不误报为测试失败。
- 纯文档或素材改动可不跑代码门禁，但要验证可读、链接和渲染。

## 四、缺陷等级与放行

- P0 泄露/破坏、付费错误、严重安全问题、核心不可用：停止发布。
- P1 核心路径失败、主要智能体不可用、严重错误输出或无法恢复：修复回归后交付。
- P2 非核心错误且有替代路径：记录负责人和后续计划。
- P3 轻微 UI、文案或低频问题：可入需求池，不得伪装成已修复。
- 放行：无未解决 P0/P1；验收全过；要求的自动检查通过；残余 P2/P3 已记录；有回滚方案。

## 五、最终回复格式

```text
结果：完成 / 部分完成 / 阻塞
修改：文件和行为
验收：逐条验收条件及结果
测试：实际执行的命令及 PASS/FAIL
未运行：命令、原因和影响
风险：残余问题、P2/P3、环境限制
下一步：仅列确实需要用户处理的事项
```

不得用“应该可以”“大概没问题”代替证据；命令没跑不写“通过”，页面没实操不写“正常”。

## 六、持续学习与 Skill 资产

- 涉及 Agent、Skill、提示词、路由、工具、知识库或门禁时必须执行 `docs/CONTINUOUS_IMPROVEMENT.md`；每日学习任务只产出候选，不自动改或激活生产资产，候选须经人工复核、离线 Eval、`pnpm qa:full`、批准、灰度和可回滚。
- WorkBuddy Skill 只能进 `mcp-skills/candidates/workbuddy-*` 或 intake/quarantine，运行时不得调用、软链或动态加载；正式运行只引用经独立验收、有版本和 Eval 的 `mcp-skills/skills/<skill>`，不得放个人级 `C:\Users\book\.codex\skills`。

## 七、产品分流与交接

涉及外卖增长、创始人 IP 获客或兰琪美业的请求先执行 `docs/agents/AGENTS.md`。一个任务只交付一个可独立验收的主要结果，同一智能体不并行两个编码任务，跨产品并行最多两个且用独立 worktree；客户原始资料、密钥和敏感经营数据不得写入 `docs/agents`；不得为“目录整齐”搬迁生产代码。

## 八、上下文与 token 成本

- 单条会话上下文接近 20 万 token 时必须收口：拆到新会话或新 worktree 继续，或先压缩再继续；不得在同一条会话里把任务拖到 50 万+ 上下文（2026-09-19 实测：单条会话 195 次调用累计 8400 万 token，占当日总量 78%）。
- 大范围检索必须限界：`rg` 必须带目标目录与 `-g` 排除规则，结果超过约 200 行时用 `-m` 或 `| Select-Object -First N` 截断；不得把几十万字符的搜索结果整段留在上下文里（实测单次 `rg` 输出 61 万字符 ≈ 17 万 token）。
- 读大文件先定位再局部读：用 `rg -n` / `Select-String` 找到行号后只读片段，避免对整份大文件执行 `Get-Content -Raw`。
- 交付长任务时主动提示用户开新会话继续，并说明当前会话上下文规模与主要消耗来源。
- 排查单日 token 消耗时，先看 `C:\Users\book\.codex\sessions\**\*.jsonl` 的 `token_count` 事件（`input_tokens` / `cached_input_tokens` / `output_tokens`），再判断是上下文膨胀还是通道问题。
