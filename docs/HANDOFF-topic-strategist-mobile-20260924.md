# 交接：选题策略官工作台 · 手机端优先（2026-09-24）

交给人：周凯（开发 + 上线）
交付人：产品侧（demo 已定稿，用户已确认）
优先级：高

## 1. 交付物与验收基准

- **`docs/prototypes/topic-strategist-demo-20260922.html`（v0.5）— 唯一验收基准**
- 自包含单文件（头像 base64 内嵌），浏览器直接打开即可，可微信转发
- **手机端为第一验收场景**：视口 ≤820px 为基准布局（375–390px 实机验收），≥821px 桌面端自动增强（同一套代码，对照 demo 内 `@media (min-width:821px)` 块）
- 硬性要求：手机端整页**无横向溢出**（`document.documentElement.scrollWidth <= viewport`）；触控目标（按钮）高度 ≥38px；输入框 `font-size ≥16px`（防 iOS 聚焦自动放大）；窄屏表格在卡片内横向滑动

## 2. 产品一句话

私有知识库 + 行业热点 + 对标账号 + 数据复盘 → 四大来源三关筛选出 10 条。

## 3. 已定稿的产品决策（用户确认，勿改口径）

1. **四大来源**：
   - ① 私有知识库 —— 大脑笔记自动同步（记录工作生活日常：客户往来 / 成交案例 / 心得灵感 / 评论私信 / 销售客服记录）
   - ② 行业热点 —— 按行业 / 产品 / 地区自动组合关键词检索，逐条核验来源。**后台只抓抖音平台数据**；前端/文案不得出现「只抓抖音」字样
   - ③ 同行与对标内容 —— 用户粘贴**抖音账号主页链接**，检索该号近期动态（标题角度 / 互动数据 / 更新频率）
   - ④ 自身账号数据复盘 —— 用户上传**抖音 / 视频号**视频数据，由「视频数据复盘智能体」分析，**本模块只显示推荐选题**，不显示复盘报告等其他内容
   - 附加：用户**自填选题**入口（复盘推荐下方输入框，「加入候选」即入池，同样过三关）
2. **三关筛选全自动**（页面上标注「自动完成，无需配置」）：一票否决 → 共识层级 → 账号阶段配比（起号 / 增长 / 变换三档，demo 内 tab 可切）
3. **缺源降级不阻塞**：任一来源缺失或关闭 → 配额自动重分配到其余来源 → 照常出 10 条，缺口如实标注（demo 来源③卡片有开关可直接演示，黄色降级提示条会弹出）
4. **对外命名统一**：私有知识库 / 行业热点 / 对标账号 / 数据复盘。全文不出现「baolu_topics」「大脑笔记」以外的旧称（大脑笔记仅作为来源①的同步载体名称保留）
5. **头像**：选题策略官用真人头像（平台 `/avatars/topic.png`；demo 内嵌的是同一张图的 base64）
6. 流程口径：配置四大来源 → 候选池涌现 16–20 条 → 三关筛选 → 交付 10 条（带证据状态 + 创作建议 + 适用阶段）

## 4. demo 内可点交互（开发时需真实实现）

| 交互 | demo 行为 | 正式版 |
|---|---|---|
| 「搜索抖音行业热点」 | 模拟检索 → 已核验/待核验结果 | 真实检索 + 来源核验 |
| 「搜索近期动态」（对标链接） | 模拟返回近 7 天更新数 | 真实取数（见 §5） |
| 来源③开关 | 关 → 置灰 + 配额重分配 + 降级提示 | 同 demo 逻辑 |
| 自填选题「加入候选」 | 入池，带「自填」标记 | 入候选池过三关 |
| 阶段 tab（起号/增长/变现） | 条形图与建议切换 | 按账号阶段数据驱动 |
| 「一键生成今天选题」CTA | — | 跑完整流水线，约 3–5 分钟 |
| 运行实况面板 | 自动播放演示（约 20s） | 真实运行日志流 + 右侧实时指标 |

## 5. 后端 / 数据需求

- 大脑笔记同步接口（来源①）
- 行业热点检索 + 来源核验：抖音热榜 / 巨量算数等公开数据可行（已验证结论）
- 对标账号取数：抖音官方无主页 API，需第三方数据服务（飞瓜/新抖类，按账号付费）或用户自助粘贴链接解析——**开发前先做可行性验证**，这是本需求唯一的重大技术风险点
- 数据复盘选题读取：复用视频数据复盘智能体的产出，只取选题字段
- 自填选题入池接口

## 6. 验收清单

- [ ] 手机端（375/390px）：对照 demo v0.5 逐区块一致（hero → 流水线 → 运行实况 → 四大来源 → 漏斗 → 三关 → 交付卡片流 → 配比建议 → 页脚）
- [ ] 手机端无横向溢出；按钮/输入框符合 §1 尺寸要求
- [ ] 桌面端（≥1280px）：自动切增强布局，不破版
- [ ] 缺源降级：关来源③照常出题 + 提示条 + 交付区标注
- [ ] 自填选题可入池并出现在交付结果（过三关）
- [ ] 对外文案口径符合 §3.2/§3.4（前端不出现「只抓抖音」）

## 7. 背景参考

- 整体平台上下文：`docs/HANDOFF-tech-partner-20260922.md` 第七节（选题策略官章节）
- 发布纪律 / 环境说明见同文档第二、四节
