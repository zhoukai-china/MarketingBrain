# 创始人 IP 获客系统状态

- 产品决策：原品牌招商独立产品已取消，合并升级为创始人 IP 获客系统。
- 已确认目标：招商加盟、C 端团购到店、学员招募、合作方招募。
- 当前生产状态：创始人 IP 入口已提供统一四目标选题工作台；招商加盟、C 端团购到店、学员招募和合作方招募均可填写身份、目标人群与线索目标，并由选题 Skill 汇总四类来源生成选题。企业知识库/资料库已从该用户主流程隐藏，底层历史资料、连接、旧 Agent ID、会话与权限仍保留。
- 当前编码任务：`FIP-01` 已完成迁移基础、四大来源选题和证据相关性 P1 闭环；`FIP-04` 已完成“选题结果 → 内容草稿”闭环；`FIP-04-02` 已完成内容目标对齐、上下文隔离、真实 Pro 四目标 12/12 和桌面/390px 页面 E2E，无未解决 P0/P1。当前验收任务已正式收口；创始人 IP 独立产品扩展及原排队的 FIP WorkBuddy MCP v1 均暂停，不自动进入编码。
- 兼容要求：迁移期间保留老客户权限、历史会话和旧 Agent ID，完成数据与路由回归后再下线旧外显产品。
- 当前一键检查：`pnpm.cmd qa:founder-ip-acquisition`，已覆盖四目标路由、工作地图直达选题、四源选题契约、问问保禄三次能力分身回归与硬门禁、统一 Brief 的结构与隔离契约；运行态接口验收使用 `SITONG_API_BASE_URL=http://127.0.0.1:3012 pnpm.cmd verify:founder-ip-goal-briefs`，覆盖保存、更新、恢复、目标隔离和租户隔离。
- 本机体验入口：本机开发环境的 `/login/founder-ip` 已提供“本机直接开通并进入创始人 IP 获客”入口，复用仅非生产可用的 `dev-login`，固定带入 `personal_ip + ip_standard`；不修改线上邀请码、微信登录或产品授权。
- 客户主体策略：创始人 IP 获客系统按“一个客户租户一个默认主体”使用。选题系统自动绑定该租户默认主体，不向客户展示主体切换、已有客户选择或新增客户入口；获客目标简报的租户、主体和四目标隔离继续保留在服务端。
- 工作地图：以不可点击的“创始人IP获客系统”为总入口，一级分支固定为“视频分支、直播分支、独立答疑”；视频分支为选题系统→内容系统→投流系统→视频复盘系统，直播分支为直播系统→直播复盘系统，问问保禄是独立答疑终点。四目标只在小 P 选题系统的获客目标简报内选择。
- 工作地图连线：直播与问问保禄都从总入口直接分支；直播连线不再穿过问问保禄卡片，问问保禄为无后继连线的独立答疑终点。
- 用户文案：创始人 IP 获客系统对用户统一使用“获客目标简报”，不再展示英文“Brief”；内部 API、类型与数据库命名保持兼容。
- 招商线索目标去重：招商加盟获客目标简报保留“获取加盟咨询”，移除同义的“获取加盟商留资”；旧获客目标简报和本地缓存中的旧值读取时自动归一为“获取加盟咨询”。
- 学员招募目标收敛：移除“完成报名”；历史获客目标简报和本地缓存中的该值读取时自动归一为“报名说明会”。
- 当前回归：已修复完整获客目标简报的前端生成请求未被等待、错误不能回传、直达选题页重开工作地图，以及“从四大来源生成创始人 IP 选题”请求写入会话但结果定位规则未匹配、页面不回显的问题。桌面与 390px 移动端已实际点击生成、检查单来源、刷新恢复、内容系统入口、控制台与目标边界；均无新增浏览器错误。
- FIP 内容生成运行态：选题进入内容系统已改为 FIP 专属 API，使用空历史、固定 Skill 和不含默认画像/通用合同/样板的专属系统消息。模型正文先过目标、事实和 CTA 语义门禁，再由服务端逐字重建选题、人群、来源与目标关系。共享终态分类确认旧 fallback 是 `finish_reason=length`：4099 个 completion tokens 全为 reasoning、正文为空；FIP 阶段保持 Pro/high，仅把有界输出预算升至 16384。修复后四目标 12/12、fallback 0，演示/未配置/真实失败仍 fail closed 且不落库。
- FIP-04-02 联调准备（2026-08-20）：已建立四目标脱敏 Champion、显式开关保护的单请求和四目标各三次真实 Provider 脚本，以及 browser-skill 桌面/390px E2E。脚本在模型请求前锁定本机 database、configured DeepSeek 与当前 `deepseek-v4-pro`，逐请求记录阶段/延迟/结果并硬检来源证据、目标关系、CTA、固定模板差异度和租户隔离；未获网关交接会在任何 HTTP 请求前拒绝执行。隔离 PostgreSQL 迁移、`/ready` 和 `/mcp/status` 已验证，FIP 原始 Skill 已按现有产品契约补齐。首个真实生成经自 MCP 超过预期仍未完成；网关任务已接管共享 P1，FIP 未修改其运行时热点，也不再重复挂起请求。收到网关已验证的单请求命令后立即执行单请求、4×3、桌面/390px、失败路径和全量门禁；此前不启动 FIP-05。
- 迁移回归（2026-08-20）：`scripts/user-role-assignment-migration-smoke.mjs` 锁定 `MembershipRole @@map("UserRole")` 不变、RBAC 关联表/约束改为 `UserRoleAssignment`、Schema 映射和历史生成器同步；修复前 FAIL，修复后静态 PASS，并已在隔离干净库完整 deploy PASS。
- 共享热点：Provider/Agent/MCP/FIP route 已脱敏区分 HTTP、timeout、cancel、invalid JSON/response、empty final、output limit、content filter、tool call 和 upstream capacity，并只记录阶段时间、finish reason 与 token 计数。仍禁止自动重试、固定模板、Flash、低端模型或百炼文本降级；未改 `packages/shared/src/index.ts`、`packages/skills/src/index.ts`、`apps/api/src/services/agent-definitions.ts`。
- P1 选题质量回归：API 最终交付已修复。根因是本机 `DATA_MODE=demo` 仍经强制 MCP 配置进入旧网关模板；现已限定 demo 使用仓库内当前 Agent，生产仍保持 MCP 强制边界。最终交付边界还会重建缺少“选题/钩子、目标人群、来源依据、与获客目标的关系、下一步生成内容”的旧格式结果。四个单来源、两种组合、缺来源、超时降级、四目标隔离、租户/主体隔离与最终 API 三次重复回归均已通过。2026-08-14 已实跑 `qa:fast`、`qa:regression`、`qa:full` 并全部通过。
- 共享热点交接（2026-08-14）：`packages/skills/src/index.ts`、`packages/agent/src/index.ts` 与 `apps/api/src/services/agent-definitions.ts` 的当前未提交差异均为 FIP（问问保禄、四目标路由/契约、四源最终交付和工作地图）。`packages/shared/src/index.ts` 同时混有 PLAT-01 的三产品登录定义（`PRODUCT_LOGIN_*`、`founder-ip/takeaway/lanqi`）与 FIP 的 `static` 地图节点、四目标能力/路由和问问保禄 Skill；该文件未形成单任务可释放基线，兰琪不得覆盖或重写它。其余三个热点可在保留当前工作树的前提下作为 FIP 已收口基线继续使用。
- 最近更新：2026-08-21。
- 关机安全停点（2026-08-20）：网关共享超时 P1 已解除，真实 Pro 请求在 67.848 秒完成并由 FIP 契约明确拒绝为 422“内容没有保留当前选题/钩子”。产品侧已保存专属 3.6KB Prompt/Skill ID 候选及修复前失败回归，但 `agent-product-smoke` 证明新 ID 未进入被禁止修改的 `packages/agent` 运行时白名单，候选尚未收口、不可验收。当前没有本任务遗留的模型、API、浏览器或测试进程；隔离库和临时 env 保留且未输出凭据。明天第一条恢复命令：`git diff -- packages/shared/src/index.ts packages/skills/src/index.ts apps/api/src/services/agent-definitions.ts scripts/agent-product-smoke.ts scripts/founder-ip-content-context-isolation-smoke.ts packages/skills/skills/founder_ip_content_creator mcp-skills/skills/founder_ip_content_creator`。
- 网关恢复交接（2026-08-21）：共享激活集合已纳入 `founder_ip_content_creator`，`agent:smoke` 已证明 FIP 使用 `agent_founder_ip_acquisition / founder_ip_content_creator`，旧门店内容继续使用 `agent_store_acquisition / baolu_content_creator`；未放宽产品授权、未启用模板或其他模型。self-MCP 超时/取消专项、FIP 专项以及 `qa:fast`、`qa:regression`、`qa:full` 均 PASS。今天没有新增真实 Provider 调用；上一受控单请求仍为 `deepseek-v4-pro + thinking enabled + reasoning_effort=high + max_tokens=4096`，67.848 秒得到明确 422 产品质量拒绝。FIP 任务现可按“受控单请求 → 四目标各 3 次 → 桌面/390px E2E”继续闭环。
- 真实 Pro 复验停点（2026-08-21）：隔离库 26 个迁移 PASS，API `/ready` 为 database/deepseek/deepseek-v4-pro/configured=true。最终字段重建后单请求 48.237 秒 2xx；Prompt 精简批次前 8 次 2xx 后学员第 3 次 503。8192 token 对照前 11 次 2xx 后合作方第 3 次 503，证明加预算无效并恢复 4096。专属系统上下文隔离后单请求 64.708 秒 2xx、招商 3/3 2xx，但到店第 1 次 503。`qa:founder-ip-acquisition`、`qa:fast`、`qa:regression`、`qa:full` 全 PASS；真实稳定性 FAIL，桌面/390px E2E 未启动，仍不放行。
- Provider P1 最终交接（2026-08-21）：安全分类在第 12 个诊断请求捕获 `output_token_limit / finish_reason=length / completion=4099 / reasoning=4099 / final empty`。FIP 阶段 `max_tokens=16384` 后，修复后单请求和四目标各 3 次全部 PASS；最终 12 次延迟 43.534–102.149 秒，实际 model `deepseek-v4-pro`、reasoning `reasoning_high`、fallback 0。Eval 关联 AgentRun/积分交易均为 0；专项、`qa:fast`、`qa:regression`、`qa:full` PASS。下一步只做桌面/390px E2E 和归档。
- FIP-04-02 页面收口（2026-08-21）：系统 Chrome + 工作区现有 Playwright 在 1440×1000 和 390×844 实际操作通过四目标、简报/选题/内容保持、保存、刷新、返回、失败、取消、重试、重复点击、跨租户 404、控制台与网络检查；模型请求 0。跨租户恢复失败不再落入旧通用内容模块或对话区；非预期控制台错误 0。页面专项、`qa:founder-ip-acquisition`、`qa:fast`、`qa:regression`、`qa:full` 全部 PASS，FIP-04-02 已关闭并可验收。
- 本机验收复验（2026-08-21）：隔离 PostgreSQL 继续只绑定 `127.0.0.1:55432`，API `127.0.0.1:3014` 的 `/ready` 为 database、DeepSeek Pro、configured=true；因 `5174` 已被其他任务占用，FIP Web 安全改用 `127.0.0.1:5185`，未停止或修改其他产品进程。Playwright 补实测工作地图三条直连分支和两条复盘回流；另发现并关闭内容编辑器浅色文字 P1，以及生成按钮缺少 AI 调用边界提示 P1。选题页与内容页现在明确说明生成/重新生成会调用 AI，仅查看、编辑、保存现有结果不会调用；最终桌面/390px E2E 模型请求 0。`qa:fast`、`qa:regression`、`qa:full` PASS；当前体验入口依赖本机 55432/3014/5185 三个进程，关机后需按受控隔离 env 重新启动。
- 验收追加闭环（2026-08-21）：工作地图一级标签由错误的“选题分支”改为“视频分支”，内部选题、内容、投流、视频复盘节点和回流不变。用户截图中的美业加盟/AI 选题错配不是账户或跨租户泄露：坏结果是同一隔离验收租户的一次真实旧运行，本机合成入口错误连接了该租户的真实 GetNote 录音，且服务端缺少项目/行业/目标相关性与录音确认门禁。现已由服务端重新读取权威获客目标简报并筛选主体、确认状态、敏感性、类型、行业和目标相关性；无合格证据返回 422 且不创建 AgentRun、不扣积分、不调用 Provider。合成入口持续标识“本机合成验收数据”，禁用真实资料连接和 AI 生成。
- 最终放行（2026-08-21）：美业加盟、AI 咨询与切换项目/目标的脱敏契约回归均不串源；当前页面零模型 E2E 在桌面和 390px 覆盖四目标、中文获客目标简报、视频/直播/独立答疑三分支、选题与内容恢复、失败/取消/重复点击、跨租户 404、控制台和网络。API 证据为无合格来源 422、旧上下文 409、AgentRun 0、积分交易 0、Provider 调用 0；`qa:founder-ip-acquisition`、`qa:fast`、`qa:regression`、`qa:full` 均 PASS，无未解决 P0/P1。
- 行业复用交接（2026-08-21）：已只读盘点选题、文案、小红书图文、投流、直播话术、短视频复盘和直播复盘的 Agent、Skill、契约与 Eval 入口，记录于 `INDUSTRY_REUSE_HANDOFF.md`；未修改共享实现。下一开发最高优先级转为“美业行业通用智能体获客 MVP”，品牌版兰琪排在通用美业之后；本任务保持 idle 等待总调度。
