# BY-26 真实图片客户质量门禁 P1

状态：完成；plan-v2/prompt-v1.2/safety-v2.6 最终真实三图 3/3 客户可用，业务 P1 已关闭（2026-08-27）

## 2026-08-27 plan-v2/prompt-v1.2/safety-v2.6 最终受控真实三图验收（3/3 PASS）

- 调用前门禁：同一 AcceptanceRoot 的旧 runtime 先由正式 status/stop 脚本确认 PID、listener、命令和 runtime 归属，再安全替换；新 runtime 与源码完整指纹均为 `753AC360F937D0D6D139F57A6CC2E54AF13FB1E90755654185FE7EDFAAE6EA3D`，`source_fresh=true`、ready/database=true。进程只从 `apps/api/.env` 读取同值 `sk-` 百炼密钥，不输出或复制；全合成租户、entitlement、300测试积分、人民币硬门禁和 media real/max3 全部通过。
- 一次性授权：grant=`by26-image-quality-live-20260827-once-25cd1ef6e642`，只绑定 `aliyun_bailian/wan2.7-image`、`beauty-xhs-image-plan-v2`、`beauty-image-provider-prompt-v1.2`、`beauty-image-safety-v2.6`，最多3张顺序任务、单图保守¥0.20、总额¥0.60≤服务端¥1；文本/视频/ASR/其他Provider、重试、修复、换模、补图、追加均为0。
- 三图结果：封面 SHA `3e4e59ee…fc3a9`、内容 SHA `c8173368…dbe21`、互动 SHA `1c145b97…b2643`，均为768×1024。每张均依次完成 Provider 技术成功、原子持久化、正式 v2.6 自动质量 PASS 和人工只读可用性 PASS 后才提交下一张；未见文字/乱码、品牌/Logo、二维码/条码、水印/UI、人物或不实美业事实。
- 完整交付与账本：AgentRun=`cmtbg1sqi00jx9bdgqle2tp6g`、batch=`succeeded`、3个唯一Provider task、客户可用/下载3。300测试积分一次预留并一次结算，释放/补偿0；重复确认返回同一3个task，不新增账本交易。owner资产200、对照租户404，刷新/历史恢复三图。
- 页面与 postflight：真实Chrome桌面1440和390px均无横向溢出，三图/三下载可见；刷新、历史、重复确认、跨租户、console=0、浏览器请求失败=0。结束后 grant consumed 文件与 `.env.media-approval` 均删除，正式stop/start恢复 media disabled/max0；最终 PostgreSQL55434/PID27752、API3016/PID26452、Web5176/PID6828，source_fresh=true，grant/approval残留0。
- 回归：engagement-prompt、precision安全23/23/风险54/54（precision/recall=1.000）、quality、generation-success、real-media、persistence、URL、media-observability、XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）与`git diff --check`全部PASS。precision首次漏传既有fixture环境变量而入口fail-closed，补齐任务卡既有资产路径后PASS；不存在产品回归。
- 放行结论：BY-26“三张真实客户合格图”业务P1关闭，范围内P0/P1=0。仅验收环境完成3/3，不部署生产、不自动开放媒体；BY-19/BY-20继续PAUSED。

## 2026-08-27 safety v2.6 面板网格占用率精度闭环（零 Provider）

- 修复前红灯：将真实安全负例SHA `f00d1cb9…f8d8`接入正式768×1024→384×512采样链，safety-v2.5稳定以`ui_layout/interface_or_watermark_like`拒绝。Champion只保存SHA、版本、bbox与脱敏布局指标：panel=5、horizontal long edges=4、vertical long edges=14、rowGroups=3、columnGroups=4、repeatedBars=2、barRows=1、barColumns=1，不保存图片正文、Prompt、Provider响应、凭据或租户标识。
- 可观测根因：旧panel-grid把“面板数≥5、行组≥3、列组≥2、存在横纵长边”直接作为UI联合证据，却没有验证面板是否高密度占据规则行列交点。瓶罐、圆托盘、窗框与木桌轮廓分散在3×4个位置，仅5个有效面板，网格占用率=`5/(3×4)=0.417`，仍满足旧条件；repeated-bars只有2个且行/列组均为1，并未构成另一条UI证据。
- 单变量 Challenger：版本升级为`beauty-image-safety-v2.6`，只为panel-grid分支增加`panelLikeRegions/(rowGroups×columnGroups) >= 0.60`；f00d的0.417不再被判UI。重复条带UI分支、横纵长边、面板几何以及QR、条码、glyph、corner-watermark、文字/Logo、人物和客户fail-closed均未放宽，也没有SHA、路径或任务白名单。
- 诊断与版本钉死：新增v2.5 Champion和v2.6 Challenger脱敏fixture，分别保存相同几何指标、0.417占用率、0.60门槛及panel/repeated-bars分支结果。正式检测器、real-media smoke和一次性真实runner全部同步钉死safety-v2.6；历史v2.5批次继续按原版本与结果只读，不重判、不开放资产。
- 对照与三次一致性：安全集加入本次真实密集静物后23/23 PASS；风险集保留真实产品UI、真实人物+伪标签、历史二维码样式/伪品牌图，以及合成QR、条码、中英文/乱码、Logo、水印、角标、卡片网格、按钮工具栏、边框弹窗和缩放/旋转/低对比变体，共54/54 REJECT。相同Eval连续3次precision/recall=`1.000/1.000`，Provider calls=0。
- 验证：图片precision/quality/generation-success/real-media零调用/persistence/URL/media-observability/XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）与`git diff --check`全部PASS；Provider/外网/grant=0、费用¥0。
- 环境边界：本原子没有Web、DOM、路由、数据库、账本或历史资产状态变化，因此按范围未重复桌面/390px E2E，也未刷新或启用媒体环境。正式status记录当前source=`753AC360…`、旧runtime=`67AD10C0…`、source_fresh=false（预期的未刷新状态），但media仍为disabled/max0、grant/approval文件0；该旧runtime不得用于声称已运行safety-v2.6。
- 放行结论：QA-20260827-014检测精度子范围P0/P1=0；BY-26三张客户合格图业务P1仍为1。下一恢复点按用户既有单批≤¥1授权重新物化仅`aliyun_bailian/wan2.7-image`、plan-v2/prompt-v1.2/safety-v2.6、最多3张且无重试/修复/换模/补图/追加的一次性真实终验；BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 plan-v2/prompt-v1.2/safety-v2.5 受控真实三图终验（安全失败关闭）

- Provider 前门禁：新增修复前红灯证明真实 runner/grant 原先没有钉住 `beauty-xhs-image-plan-v2`；修复后 grant 同时钉死 `aliyun_bailian/wan2.7-image`、plan-v2、prompt-v1.2、safety-v2.5、最多3张顺序任务、单图保守¥0.20、总额¥0.60≤服务端¥1，重试/修复/换模/补图/追加/其他Provider均为0。调用前 source/runtime=`67AD10C0…D56`、source_fresh=true、ready/database、全合成租户/entitlement、300测试积分、人民币成本门禁和media real/max3全部PASS。
- 真实结果：封面图768×1024、SHA `d53b9885…1de9`，技术成功、原子落盘、safety-v2.5与人工只读可用性全部PASS。内容图768×1024、SHA `f00d1cb9…f8d8`，技术成功并落盘，但自动门禁以 `interface_or_watermark_like/ui_layout` 拒绝；脱敏证据 confidence=0.926、bbox=`(20,422,678,600)`，panelLikeRegions=5、horizontalLongEdges=4、verticalLongEdges=14、rowGroups=3、columnGroups=4、repeatedBarRegions=2。人工只读只见无字无标的米色泵瓶、圆托盘、窗边和木桌静物，但没有覆盖自动门禁。互动图未提交，无第3/4次Provider任务。
- 批次、账本与费用：实际2个图片Provider任务且均技术成功，自动重试/修复/换模/补图/追加0，保守费用¥0.40；batch=`quality_failed`、客户资产/下载0。300测试积分一次预留、结算0、一次全额释放、净0；重复确认没有新增任务、交易或费用。文本仅controlled_mock，不产生真实文本费用，视频/ASR/其他Provider0。
- 验收 runner 修复：Windows Chrome 151 启动器会在浏览器进程接管后以code 0退出，旧runner误认为启动失败；现改为等待`DevToolsActivePort`并以CDP `Browser.close`收口。首次真实资产落盘后还暴露runner未显式传入`UPLOAD_DIR`，人工审核材料定位失败；修复为从仓库绝对路径确定性解析正式上传目录。两项均未创建新批次、未重试Provider、未改变资产或账本。
- 用户路径与postflight：live postflight确认2个唯一Provider任务、2个不可变落盘资产、1个自动PASS+人工批准、1个质量拒绝、1个未提交；真实Chrome桌面1440/390px、刷新/历史、重复确认、owner/跨租户404、console0、postflight外部Provider请求0全部PASS。
- 撤权与环境：一次性grant与media approval均已删除；同一AcceptanceRoot经正式stop/start恢复PostgreSQL55434/PID26384、API3016/PID28324、Web5176/PID22852，source/runtime完整指纹=`67AD10C0052B76F8B9EB459D8D0A141EDCD185296A5C6BA61F4ACACB2F5D4D56`、source_fresh=true、ready/database=true、Web200、text=controlled_mock、media=disabled/max0，受控浏览器残留0。
- 回归：图片engagement-prompt/quality/generation-success/live-postflight/real-media/persistence/URL/media-observability/XHS同页、API/Web/Agent typecheck、图片precision安全22/22与风险54/54、`qa:fast`、`qa:regression`、`qa:full`（含build）全部PASS；最终diff-check见收口记录。precision首次运行只因未提供8个受控资产路径的测试前置而FAIL，补齐既有审计资产路径后PASS，不记为产品失败。
- 放行结论：安全、费用、账本、租户、幂等P0=0；三张客户合格图没有达成，QA-20260827-014/BY-26业务P1=1，不提供客户图片验收入口、不部署。下一原子只做零Provider的`ui_layout`精度红灯：将SHA `f00d1cb9…f8d8`作为只读安全负例，配合真实UI/水印/卡片/按钮/二维码/文字Logo/人物正例，保存Champion中间特征并一次只改变一个主要变量；风险召回不得下降且同批连续3次precision/recall必须为1.000。禁止SHA白名单、人工覆盖或追加真实调用；BY-19/BY-20继续PAUSED。

## 2026-08-27 互动图纯摄影构图与 payload 闭环（零 Provider）

- 修复前红灯与 provenance：新增 `beauty-image-engagement-layout-champion-v1`，只保存真实互动拒绝图 SHA `acc26153…385d`、角色、质量原因、人工只读风险类型和旧版本语义布尔值，不保存图片正文、Prompt、Provider响应或凭据。专项初跑稳定因实际仍为 `beauty-xhs-image-plan-v1` 而 FAIL，证明红灯没有依赖Provider。
- 完整路径根因：固定XHS Skill生成的互动制作说明会进入 `parseBeautyImageDirections → buildBeautyImageProviderInput`。旧 plan-v1/prompt-v1.1 的 engagement purpose/composition/providerInstruction 同时强调“互动问题、后期互动短句安全区、画面下方留白”；虽然负向 prompt 已禁卡片/按钮，但正向排版语义仍诱导海报、中文展示文字与UI构图。本次真实拒绝是 safety-v2.5 的正确真阳性，不归因检测误报。
- 单变量 Challenger：升级为 `beauty-xhs-image-plan-v2` / `beauty-image-provider-prompt-v1.2`，只改变 engagement 摄影构图变量：完整纯摄影静物/环境细节、与正文主题一致、无文案预留；显式非海报、卡片、清单、信息图、社交界面、按钮、对话框、标题栏和文字排版区。组装器只对 engagement 清除来源制作说明中的下方留白、互动问题与文案安全区语义；中文互动句只作为网页文字/后期叠字元数据，不进入生成画面。
- 不回退证据：封面与内容角色的 prompt/negative/watermark 语义 SHA-256 分别钉死为 `f0d36dec…4456`、`0a52b8dc…8db`，与 v1.1 Champion 一致；两角色 plan 的 purpose/composition/textStrategy 也精确保持原值。safety-v2.5 未改，二维码、条码、真实文字/Logo、水印、UI、人物与fail-closed召回未放宽。
- 稳定性与门禁：Challenger 同批连续3次 PASS；图片 precision 安全22/22、风险54/54，precision/recall=`1.000/1.000`。XHS同页、quality、generation-success、real-media零调用、persistence、URL、media-observability、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）与`git diff --check`全部PASS；Provider/网络/grant=0、费用¥0。
- 环境与放行：没有页面DOM、路由、数据库或历史资产状态变化，按范围未重复浏览器E2E，也未刷新/启用媒体环境；历史 plan-v1 批次按记录只读恢复。该子范围P0/P1=0，BY-26完整三图业务P1仍为1。下一恢复点按既有单批≤¥1授权重新物化仅 `aliyun_bailian/wan2.7-image`、plan-v2/prompt-v1.2/safety-v2.5、最多3张且无重试/修复/换模/补图/追加的一次性真实终验；BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 safety v2.5 最终受控真实三图终验（真实风险，安全失败关闭）

- 授权与调用前门禁：新不可重放grant固定`aliyun_bailian/wan2.7-image`、prompt-v1.1/safety-v2.5、最多3张顺序任务、单图保守¥0.20、总上限¥0.60≤用户¥1；重试、修复、换模、补图、追加和其他Provider均为0。调用前source/runtime=`A81875C1…`、ready/database、全合成租户/entitlement、300测试积分、服务端人民币预算、media real/max3与进程内凭据预检全部PASS。
- 真实结果：封面SHA `60d487d0…6948`、内容SHA `1ac75e65…c9d`均为768×1024，技术/原子落盘/v2.5自动门禁/人工只读可用性PASS。互动图SHA `acc26153…385d`技术成功并落盘，但画面确有明显中文展示文字及卡片式界面布局，v2.5以`visible_text_or_brand_like/glyph_sequence`正确拒绝；人工没有覆盖门禁。3个Provider任务后终止，无第4次调用。
- 批次与账本：batch=`quality_failed`、客户资产/下载0；300测试积分只预留一次、结算0、全额释放一次、净0，三项job均为refunded。Provider保守费用¥0.60，文本/视频/ASR/其他Provider=0；重复确认没有新增任务或账本事务。
- 用户路径与postflight：真实Chrome桌面1440/390px、刷新/历史、重复确认、owner/跨租户404、console0全部PASS；postflight再次审计3个Provider task、3个不可变落盘资产、1个真实质量拒绝、客户可用0、追加Provider请求0。
- 撤权与环境：一次性grant与`.env.media-approval`均已删除；同一AcceptanceRoot经正式stop/start恢复PostgreSQL55434/PID19728、API3016/PID20232、Web5176/PID21024，source/runtime=`A81875C1…`、source_fresh=true、ready/database=true、text=controlled_mock、media=disabled/max0。本地环境保留在线但本批无客户可下载资产，不部署。
- 回归：图片quality/generation-success/live-postflight/real-media/persistence/URL/media-observability/XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）与`git diff --check`均PASS。
- 放行结论：安全、账本、租户、幂等和费用范围P0=0；三张客户合格图未达成，BY-26业务P1仍为1。下一原子只做零Provider的互动图生成成功率根因：审计role=`engagement`的制作说明到Provider payload是否诱导标题/互动卡片布局，一次只调整一个构图/纯画面约束变量并保持现有真实文字/UI召回；不得追加真实调用、不得恢复BY-19/BY-20。

## 2026-08-27 v2.5 角落构件垂直占比精度闭环（零 Provider）

- 修复前红灯：把最新真实安全图 SHA `19f36810…d946` 作为只读负例接入正式 768×1024→384×512 采样链，v2.4 稳定以 `corner_watermark/edge_components` 拒绝。Champion 仅保存 SHA、版本、bbox 和脱敏几何摘要：6/6 glyph-like components、bbox sampled height=10、mean component height=4.333、mean-height/union-height=0.433、widthCoverage=0.577、edgePixelDensity=0.125、readableSequenceRecovered=false；不保存图片正文、Prompt、Provider响应或凭据。
- 可观测根因：v2.4 已要求基线、组件占比、宽度覆盖、密度、间距与高度变化，但没有要求各构件在联合框内占据足够垂直高度。右下圆形棉片轮廓与桌布纹理形成6个对齐微小边缘组件，平均只覆盖联合框高度的43.3%，仍被当作角落字形/水印序列；这与QR、条码、通用glyph、UI、Provider、持久化或renderer无关。
- 单变量 Challenger：检测器升级为 `beauty-image-safety-v2.5`，只为 corner bright/edge component sequence 增加 `meanComponentHeight / unionHeight >= 0.55` 的尺度无关联合证据，并把实际值与门槛写入脱敏 evidence。QR finder、条码、通用文字/Logo、真实水印/UI、人物、manual-review和客户fail-closed均未改变；没有SHA、文件名、路径或任务白名单。
- 对照与稳定性：安全集新增本次圆形棉片/织物真实图，共22/22 PASS；风险集保留真实二维码样式/伪品牌图、人物+伪标签、真实产品UI及QR、条码、中英文/乱码、Logo、水印、角标、卡片/按钮/弹窗和缩放/旋转/低对比变体，共54/54 REJECT。相同Eval连续3次precision/recall=`1.000/1.000`。
- 验证：图片precision/quality/generation-success/real-media零调用/持久化/URL/media-observability/XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）及`git diff --check`全部PASS；Provider/网络/grant=0、费用¥0。
- 环境边界：没有Web、DOM、路由、数据库或历史资产状态变化，故按范围未重复桌面/390px E2E，也遵照本原子要求未刷新或启用受控媒体环境；历史v2.4批次继续只读且不重判、不开放下载。
- 精确文件归属：`apps/api/src/products/beauty-industry/media-contract.ts`、`scripts/beauty-industry-image-quality-precision-p1-smoke.ts`、`scripts/fixtures/beauty-image-safety-v24-cotton-pads-champion.json`、`scripts/beauty-industry-xhs-image-live-acceptance.mjs`、`scripts/beauty-industry-real-media-smoke.ts`及本任务/质量文档。
- 放行结论：QA-20260827-013 检测精度子范围P0/P1=0；BY-26三张客户合格图业务P1仍为1。下一恢复点按既有单批人民币≤¥1授权，重新物化仅 `aliyun_bailian/wan2.7-image`、prompt-v1.1/safety-v2.5、最多3张、无重试/补图/换模/追加的一次性不可重放grant，并先刷新独立受控环境核对source_fresh；BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 safety v2.4 最终受控真实三图终验（安全失败关闭）

- Provider 前红灯与最小修复：既有一次性 grant 只钉住 Provider/模型/次数/人民币上限，未钉住正式 `beauty-image-provider-prompt-v1.1` 与 `beauty-image-safety-v2.4`。新增红灯先稳定报 `live grant does not pin provider prompt version`；runner 随后在任何 Provider 请求前同时校验 grant 中的 prompt/safety 版本与当前源码导出版本。`beauty-industry:real-media-smoke` 固化该门禁，不改变图片生成、检测或账本业务逻辑。
- 授权与调用前门禁：新的不可重放 grant 仅允许 `aliyun_bailian/wan2.7-image`、prompt v1.1、safety v2.4、最多3个顺序任务，单图保守¥0.20、整批最坏¥0.60≤用户¥1；其他 Provider、自动重试、修复、换模、补图和追加均为0。调用前 source/runtime=`B4F85F79…`、`source_fresh=true`、ready/database、全合成 beauty-industry 租户/entitlement、300测试积分、人民币预算与 media max3 全部通过。
- 真实结果：仅封面任务被提交。Provider 技术成功并原子落盘，768×1024、SHA `19f36810…d946`；v2.4 以 `interface_or_watermark_like/corner_watermark` 拒绝，脱敏证据为 bbox=`(556,968,104,20)`、confidence=0.978、method=`edge_components`、glyph=6/6、widthCoverage=0.577、edgePixelDensity=0.125、readableSequenceRecovered=false。人工只读仅见无字无标的米色容器、毛巾和圆形棉片静物，未识别出文字、Logo、二维码、水印、UI或人物，但没有覆盖自动门禁。
- 停止、账本与客户结果：首图拒绝后内容图和互动图均未提交，无第2/3/4次任务。整批 `quality_failed`、客户资产/下载0；300测试积分一次预留一次全额释放，settled=0、净0。实际1个图片 Provider 任务，保守费用¥0.20；文本/视频/ASR/其他Provider0。
- 浏览器与 postflight：正式只读审计确认1个技术成功且质量拒绝资产、2个未提交占位、审计资产SHA不变；桌面1440、390px、刷新/历史、重复确认、owner/跨租户404、console0、外部Provider追加请求0全部PASS。
- 撤权与环境：一次性 consumed grant 与 media approval 已删除；受控环境经正式 stop/start 恢复为 PostgreSQL55434/PID22408、API3016/PID19008、Web5176/PID18696，source/runtime=`B4F85F79…`、source_fresh=true、ready/database=true、Web200、text=controlled_mock、media=disabled/max0。脱敏记录位于仓库外 `beauty-industry-by26-v23-final-live-20260827/runtime/by26-v24-final.verification.json`。
- 回归：图片 quality/generation-success/real-media/persistence/URL/media-observability/XHS 同页专项、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）全部PASS；最终 `git diff --check` 见本任务收口记录。
- 放行结论：安全、账本、租户、费用与失败关闭 P0=0；三张客户合格图没有达成，QA-20260827-013/BY-26 业务 P1=1。本批授权已耗尽，不提供用户测试入口、不部署。下一原子只能以 SHA `19f36810…d946` 做零 Provider、非SHA白名单的 `corner_watermark/edge_components` 精度根因红灯，并保留真实角落水印、二维码、文字Logo和UI召回；BY-19/BY-20继续PAUSED。

## 2026-08-27 v2.4 字形构件占比精度闭环（零 Provider）

- 修复前红灯：真实受控内容图 SHA `bc044be3…af61` 在 v2.3 稳定被 `visible_text_or_brand_like/glyph_sequence` 拒绝。Champion 只保存 SHA、版本和脱敏局部特征：bbox=`(194,796,306,26)`、glyphCount=5、glyphLikeComponents=3、glyphLikeRatio=0.6、componentAspect=`1.125–6.8`、meanAspect=3.493、baselineDeviation=1.939、gapVariation=0.472、heightVariation=0.181、widthCoverage=0.706、edgePixelDensity=0.161、readableSequenceRecovered=false；不保存 Prompt、Provider 正文、凭据或客户内容。
- 根因：v2.3 的通用 `glyph_sequence` 要求基线、间距、覆盖宽度和边缘密度，却没有要求序列中多数构件本身符合字形几何。瓶底、瓶身高光与木架长边形成 5 个对齐组件，其中仅 3 个像字形，最长宽高比达 6.8，仍被整体判成文字/品牌；这与 QR、条码、corner watermark、UI、人物、路由或持久化无关。
- 单变量 Challenger：检测器升级为 `beauty-image-safety-v2.4`，只为通用 glyph sequence 增加 `glyphLikeComponents / glyphCount >= 0.75` 的多数构件证据，并输出可审计的构件数量、占比、门槛及宽高比摘要。QR finder、条码平行线、真实文字/Logo、水印、UI、人物、manual-review 与客户 fail-closed 均未改变；没有 SHA、路径或任务白名单。
- 对照与三次一致性：安全集在既有 20 个负例上新增本次原始 768×1024 无字瓶罐/木架图，共 21/21 PASS；风险集保留真实历史坏图、人物+伪标签、真实产品 UI，以及 QR、条码、中英文/乱码、Logo、水印、角标、卡片/按钮/弹窗及其缩放/旋转/低对比变体，共 54/54 REJECT。相同 Eval 连续 3 次 precision/recall=`1.000/1.000`；历史二维码样式/伪品牌图仍拒绝。
- 边界：本原子只验证正式原始资产输入链；对该安全图额外制造的极端缩小或低对比变体会进入其他既有角落/UI分支，不属于这次单变量 glyph 修复，未被冒充为当前 PASS，留待独立证据驱动任务。历史批次不按 v2.4 重判，也不开放下载。
- 验证：图片 precision/quality/generation-success/real-media 零调用/持久化/URL/media-observability/XHS 同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含 build）与 `git diff --check` 全部 PASS；Provider/外部网络 0、费用 ¥0。没有 Web、DOM、路由或客户资产状态变化，因此按范围未重复桌面/390px E2E，也未刷新受控运行环境，媒体继续 disabled/max0。
- 精确文件归属：`apps/api/src/products/beauty-industry/media-contract.ts`、`scripts/beauty-industry-image-quality-precision-p1-smoke.ts`、`scripts/fixtures/beauty-image-safety-v23-blank-bottles-champion.json`及本任务/质量文档。
- 放行结论：QA-20260827-012 检测精度子范围 P0/P1=0；BY-26“三张客户合格图”真实业务验收仍 P1=1。本轮未创建 grant、未调用 Provider、未部署；BY-19/BY-20 继续 PAUSED。

## 2026-08-27 v2.3 修复后最终真实终验（安全失败关闭）

- 授权与环境：新的不可重放 grant 仅允许 `aliyun_bailian/wan2.7-image`、`beauty-image-provider-prompt-v1.1`、`beauty-image-safety-v2.3`、最多3个顺序任务，单图保守¥0.20、整批最坏¥0.60≤用户¥1；其他 Provider、自动重试、修复、换模、补图和追加均为0。调用前 source/runtime=`690A35A6…`、source_fresh、数据库、全合成租户/entitlement、300测试积分、人民币预算和media max3均通过。
- 封面图：技术成功、原子落盘、自动质量与人工只读可用性全部PASS；768×1024，SHA `1de67102…f1c3`。人工只读只见无字无标的米色中性静物，不见人物、文字、品牌、Logo、二维码、条码、水印或UI，因而允许提交第2张。
- 内容图：技术成功并原子落盘；768×1024，SHA `bc044be3…af61`。v2.3以 `visible_text_or_brand_like` 拒绝；人工只读看到四个米色无标签瓶罐、木架和毛巾，未识别出可见文字、品牌、Logo、二维码、水印、UI或人物。人工观察没有覆盖自动硬门禁，登记 QA-20260827-012；本原子任务不重复开启检测器修复。
- 停止与客户结果：第2张拒绝后第3张未提交，无第3/4次Provider调用。整批`quality_failed`、客户资产/下载0；300测试积分一次预留一次全额释放，净0。实际2个图片Provider任务，保守费用¥0.40；文本/视频/ASR/其他Provider0。
- 运行器安全插曲：首图成功后，验收runner因本地进程未收到`UPLOAD_DIR`而在人工审核材料化前停止；没有重跑runner、没有创建新grant或新批次。以同一批次、同一授权计数完成首图人工审核并继续轮询，随后在第2张自动拒绝处正常停止。该插曲没有改变Provider任务数、资产SHA或账本。
- Postflight：live postflight确认2个唯一Provider任务、2个技术成功资产、1个自动PASS+人工批准、1个质量拒绝、1个未提交；重复审计和重复确认没有新增任务、交易或余额变化。owner与跨租户资产/下载均404，桌面1440、390px、刷新/历史、console0、postflight外部Provider请求0均PASS。
- 撤权与环境：一次性approval、consumed grant及临时续跑脚本均已删除；正式stop/start后环境为PostgreSQL55434、API3016、Web5176，source/runtime=`690A35A6…`、source_fresh=true、ready database=true、Web200、text=controlled_mock、media=disabled/max0。脱敏终验记录保存在仓库外`beauty-industry-by26-v23-final-live-20260827/runtime/by26-v23-final.verification.json`。
- 回归：图片质量/生成成功率/持久化/URL/media-observability/XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）和`git diff --check`全部PASS。
- 放行结论：安全、费用、账本和客户隔离范围P0=0；三张客户合格图仍为P1=1。下一恢复点只能对SHA `bc044be3…af61`建立零Provider、非SHA白名单的`visible_text_or_brand_like`精度红灯，保留真实文字/品牌/Logo召回；不得追加付费调用、不部署，BY-19/BY-20继续PAUSED。

## 2026-08-27 v2.3 双向长边 UI 证据精度闭环（零 Provider）

- 修复前红灯：真实受控内容图SHA `63f63a70…3fe9` 在v2.2稳定被 `ui_layout/interface_or_watermark_like` 拒绝。Champion只保存SHA、版本、bbox和脱敏几何摘要：horizontal long edges=0、vertical long edges=7、panel-like regions=6、row groups=3、column groups=3、repeated bars=2、confidence=0.936；不保存Provider正文、Prompt、凭据或客户数据。
- 根因：v2.2的 `panelGrid` 只要求面板数/行列分组及“横向+纵向长边总数≥3”，没有要求二维界面布局同时具有横向与纵向长边。瓶罐竖边、木架与采样后的自然轮廓形成6个组件和7条纵向长边，即使横向证据为0仍被当作界面网格；不是二维码、文字Logo、corner watermark、Provider、持久化或renderer问题。
- 单变量 Challenger：检测器升级为 `beauty-image-safety-v2.3`，只把panel-grid长边条件改为horizontal≥1且vertical≥1。`repeatedBars`、真实水印/角标、QR、条码、glyph、文字/Logo、manual-review与客户fail-closed状态机均未改变；没有SHA/任务/文件名白名单。
- 对照扩展：安全集新增真实无字瓶罐木架图、瓶罐货架和建筑线条；风险集新增仓库内真实产品UI截图及卡片网格、按钮工具栏、边框弹窗，并保留二维码、条码、中英文/乱码、Logo、水印、角标和UI变体。最终安全20/20 PASS、风险54/54 REJECT，precision/recall=`1.000/1.000`，连续3次一致；真实UI截图仍由 `ui_layout` 拒绝。
- 验证：图片precision/quality/generation-success/real-media零调用/持久化/URL/media-observability/XHS专项、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）及`git diff --check`均PASS；Provider/外网0、费用¥0。没有Web、DOM或路由变化，因此按范围不重复桌面/390px E2E。
- 精确文件归属：`apps/api/src/products/beauty-industry/media-contract.ts`、`scripts/beauty-industry-image-quality-precision-p1-smoke.ts`、`scripts/fixtures/beauty-image-safety-v22-ui-layout-champion.json`及本任务文档。当前受控环境未为本次API检测器改动刷新；媒体仍disabled，下一次真实终验必须新建受控环境并重新核对source_fresh。
- 放行结论：QA-20260827-010检测精度子范围P0/P1=0；BY-26“三张客户合格图”真实业务验收仍P1=1。本轮不创建grant、不调用Provider、不部署，BY-19/BY-20继续PAUSED。

## 2026-08-27 v2.2 最终受控真实终验（安全失败关闭）

- 授权与调用前门禁：新的不可重放 grant 仅允许 `aliyun_bailian/wan2.7-image`、`beauty-image-provider-prompt-v1.1`、`beauty-image-safety-v2.2`、最多3个顺序任务，单图保守¥0.20、整批最坏¥0.60≤用户¥1；其他 Provider、重试、修复、换模、补图和追加均为0。调用前 source/runtime=`C13BA148…`、`source_fresh=true`、数据库、全合成租户/entitlement、300测试积分、人民币成本和media max3均通过。
- 封面图：技术成功、原子落盘、v2.2自动质量与人工只读可用性均PASS；768×1024，SHA `872166db…e316`。人工只读仅见无字无标中性容器，不见人物、文字、品牌、Logo、二维码、条码、水印或UI，因而允许提交第2张。
- 内容图：技术成功并原子落盘；768×1024，SHA `63f63a70…3fe9`。v2.2以 `interface_or_watermark_like/ui_layout` 拒绝，置信度0.936，脱敏bbox `(2,364,764,658)`；联合特征为panel-like regions=6、vertical long edges=7、repeated bar regions=2、aligned-panel evidence=true。人工只读仅见米色无字瓶罐/木架静物，未识别出文字、品牌、二维码、水印、UI或人物，但人工判断没有覆盖正式门禁，登记 QA-20260827-010。
- 停止与客户结果：第2张拒绝后立即停止，第3张未提交，无第3/4次 Provider 调用。整批 `quality_failed`、客户资产/下载0；300测试积分一次预留一次全额释放，净0。实际2个图片Provider任务、2个技术成功，保守费用¥0.40；文本/视频/ASR/其他Provider0。
- Web/数据门禁：重复确认未新增任务/账本；owner和跨租户资产均404；桌面1440、390px、刷新/历史、console0均PASS。live postflight确认2任务、1自动PASS、1质量拒绝、1未提交、审计资产SHA不变，postflight额外Provider调用0。
- 撤权与环境：grant已消费并删除，未留下approval；受控环境已按正式身份停止再以media关闭方式重启。PostgreSQL55434/PID4900、API3016/PID19452、Web5176/PID13788，source/runtime=`C13BA148…`、`source_fresh=true`、ready database=true、Web200、text=controlled_mock、media=disabled/max0。
- 回归：图片精度13/13安全与41/41风险、图片质量、生成成功率、real-media零调用、持久化、URL、media-observability、XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）及`git diff --check`全部PASS。
- 放行结论：P0=0、BY-26/QA-20260827-010 P1=1；不部署、不追加真实调用，BY-19/BY-20继续PAUSED。下一恢复点是对 `ui_layout` 在多瓶罐、木架和长边组合上的零Provider precision/recall根因红灯；禁止SHA白名单、降低真实UI/水印召回或用人工目视直接放行。

## 2026-08-27 v2.2 边缘序列像素密度精度闭环（零 Provider）

- 修复前红灯：真实内容图 SHA `fa9e11f9…2846` 在检测器自身 768×1024→384×512 采样链中稳定被 `corner_watermark/edge_components` 与 `glyph_sequence` 拒绝。Champion 脱敏夹具仅保存 SHA、检测版本、bbox 和几何摘要：角落序列 `edgePixelDensity=0.077`，下部字形序列 `edgePixelDensity=0.042`；两者都没有恢复可读字符序列。
- 根因：v2.1 已要求基线、覆盖宽度、间距和高度组合，但没有要求候选组件在其联合 bbox 中形成足够连续的边缘墨迹。瓶罐轮廓、木柜横纹以及缩放采样产生的稀疏断边可跨较宽 bbox，因而同时满足 `corner_watermark` 与 `glyph_sequence` 的几何外形；这不是 route、Provider、持久化或 renderer 问题。
- 单变量 Challenger：检测器升级为 `beauty-image-safety-v2.2`，只为角落亮/边缘组件序列和通用字形序列增加同一个最小边缘像素密度 `0.09`。QR finder、条码条纹、UI布局、真实水印、文字/Logo理由、manual-review和客户 fail-closed 状态机均未改；没有 SHA/文件名/任务白名单。新任务及解码失败审计统一引用导出的 v2.2 版本常量，避免记录与执行版本漂移。
- 对照结果：真实安全瓶罐图与既有安全图共13个负例全部 PASS；历史二维码样式/伪品牌图、真实人物+伪标签图及合成二维码、条码、中英文/乱码、Logo、水印、UI等共41个风险正例全部 REJECT，precision/recall=`1.000/1.000`，连续3次一致。历史坏图字形序列密度 `0.131`，仍高于新门槛并继续拒绝；人物风险图也继续由既有UI/文字结构门禁拒绝。
- 边界：本地检测仍只是确定性风险筛查，不声称语义理解人物或全部画面。对真实图做人为二次极限缩小/低对比处理曾触发另外的 UI/低对比分支，该处理不等同于正式原图输入链，未混入本次单变量生产修复；保留为未来独立 Challenger 候选，不把它当作当前安全放行证据。
- 验证：图片精度专项连续3次、图片质量、生成成功率、real-media零调用、持久化、URL、media-observability、XHS同页、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）、`git diff --check`均PASS；Provider/外网0、费用¥0。没有页面代码变化，因此没有重复浏览器E2E；已有历史批次继续按原检测版本只读，不重算、不开放下载。
- 精确文件归属：`apps/api/src/products/beauty-industry/media-contract.ts`、`apps/api/src/routes/beauty-industry-media.ts`、`scripts/beauty-industry-image-quality-precision-p1-smoke.ts`、`scripts/fixtures/beauty-image-safety-v21-pump-bottles-champion.json`及本任务文档。BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 v1.1 最新真实终验（安全失败关闭）

- 新的一次性不可重放 grant 固定 `aliyun_bailian/wan2.7-image`、`beauty-image-provider-prompt-v1.1`、最多3个顺序任务、单图保守¥0.20、整批最坏¥0.60≤用户¥1；其他 Provider、重试、修复、换模、补图和追加均为0。调用前 API3017/Web5177、PostgreSQL55434、源码指纹 `41E3B4BC…`、`/ready database=true`、租户/entitlement、300积分预留和 media max3 全部通过。
- 封面图技术成功并原子落盘，768×1024，SHA `b51b92f0…f572`；自动质量 PASS，人工只读确认无可见文字、品牌、Logo、二维码、水印、UI或人物，允许继续第2张。
- 内容图技术成功并原子落盘，768×1024，SHA `fa9e11f9…2846`；`beauty-image-safety-v2.1` 以 `corner_watermark/edge_components` 与 `glyph_sequence` 拒绝。人工只读看到的是无标签中性瓶罐、毛巾和木柜静物，没有识别出可见文字、品牌、Logo、二维码、水印、UI或人物；该人工判断不能替代正式门禁，因此仍按自动拒绝停止，登记 QA-20260827-009 做零 Provider 精度根因。
- 第3张互动图未提交，无第3/4次调用。整批 `quality_failed`、客户可用/下载0；300测试积分一次预留、一次全额释放，净0；实际2个 Provider task，保守费用¥0.40，文本/视频/ASR/其他Provider0。
- live postflight、重复确认、owner/双租户资产404、桌面1440/390px、刷新/历史、console0、external0均PASS。浏览器专项首轮仅因同一批三条数据库记录的无序查询互换而失败，修复为按 job id 稳定排序后同一零 Provider 路径PASS；业务数据和账本未变化。
- 隔离 API3017/PID6468、Web5177/PID31264 已按双重身份门禁停止；一次性 approval/consumed grant 已删除。共享验收环境继续 source/runtime=`41E3B4BC…`、source_fresh=true、text=controlled_mock、media=disabled/max0。
- 结论：付费授权已耗尽且本批不追加调用；BY-26业务P1=1、P0=0。下一恢复点只能使用内容图 SHA `fa9e11f9…2846` 做零 Provider、非 SHA 白名单的 `corner_watermark/glyph_sequence` 精度红灯，不得放宽真实文字、Logo、二维码、水印或UI硬门禁。

## 归属

- 产品：美业智能体
- 缺陷：QA-20260826-007
- 风险：高（付费媒体、客户资产、积分、租户隔离）
- 是否允许并行：否；BY-19/BY-20 保持 PAUSED

## 用户结果

技术成功的图片只有通过逐图离线风险筛查后才进入客户图库；含二维码/二维码样式、可见文字/乱码、品牌或界面样式风险的图片只保留为受控审计资产，不能预览或下载。承诺三张而不足三张合格时整批 `quality_failed`，测试积分全额幂等补偿，Provider 成本仍按事实保留。

## 已知证据

- `aliyun_bailian/wan2.7-image` 3/3 技术成功，均为 768×1024；无第4任务，外部费用保守 ¥0.60。
- 指定高风险资产 SHA-256 为 `0e70f36a16453084f753974d4639b7601b6debbf0b1baa5b9c65f12129a0ae86`；三张原始资产必须原样保留。
- 当前实现以 `status=succeeded + assetStatus=persisted` 直接开放客户资产，并按技术成功张数结算；没有逐图视觉质量状态或完整批次交付门禁。

## 验收条件

1. 正常：无文字/Logo/二维码/水印/界面风险的受控 PNG 通过；Provider payload 确实包含纯画面正向约束、完整负向词并关闭水印。
2. 失败：风险图标记 `visual_quality_rejected`、客户资产 404、仅审核摘要可见；三张不足三张合格则批次 `quality_failed`，不自动重试/补图/换模。
3. 账本：完整合格批次才结算；既有已 settled 的失败批次只新增一次 300 积分补偿交易，不改写旧 consume，不伪造 Provider 退款。
4. 安全：三张原始文件不修改/覆盖/删除；跨租户仍 404；日志只含 SHA、风险码、尺寸和状态，不含 Prompt、Provider 原始响应或密钥。
5. 页面：技术状态与客户质量分离，刷新/历史恢复；桌面和390px无溢出，console=0、external Provider=0。

## 不做

- 不调用任何真实 Provider、OCR 云服务或视觉模型；不生成替代图片。
- 不部署生产、不启用媒体、不恢复 BY-19/BY-20。
- 本地检测只作为风险筛查；不能声称完整理解画面，无法确定时必须 fail-closed。

## 文件归属

- API：美业媒体合同、离线图片风险筛查、媒体路由、账本补偿、租户资产授权。
- Web：图片批次质量状态、客户图库与折叠审核。
- 测试：现有三资产离线红绿灯、payload 快照、数据库补偿/幂等/隔离、真实 Chrome。
- 文档：本任务卡、STATUS、TEST_MATRIX、BUG_REGRESSIONS、PRODUCT/WORKFLOW/CONTRACTS、任务索引。

## 恢复点

QA-20260826-007 的“不合格技术成功资产对客户开放”已被门禁阻断，但修复后真实终验首图被确定性筛查拒绝、客户完整交付仍为 0，因此 BY-26 业务验收继续 P1。媒体保持 `disabled/max_real_images=0`；本次授权已经消费，未经新的精确授权不得调用。

## 根因与红绿灯

- 修复前红灯先稳定得到 `TypeError: assessBeautyImageSafety is not a function`，随后命中媒体路由缺少 `visual_quality_rejected`，证明旧链只判技术成功，没有客户质量门禁。
- 三张受控原始资产逐图离线筛查，指定 SHA `0e70f36a…a0ae86` 与另外两张均命中二维码/条形码样式、可见文字/品牌样式或界面/水印样式风险；检测边界固定为 `deterministic_risk_screen_only`，不声称理解全部画面。
- 合成纯画面 PNG 通过；合成二维码夹具失败关闭。每套 Provider payload 快照均包含纯画面要求和“禁止文字、字母、数字、Logo、品牌、二维码、条形码、水印、界面元素”的负向约束，并把 `watermark=false` 真正传给百炼请求。

## 完成实现

- 每张技术成功图片先持久化原始资产，再走确定性本地风险筛查；只有 `qualityStatus=passed` 才产生客户资产 URL。拒绝图保持 `status=succeeded` 作为技术事实，同时写入 `assetStatus=quality_rejected`、`errorMessage=visual_quality_rejected`，客户读取与下载均 404。
- 三图承诺采用完整交付语义：任一张未通过则批次 `quality_failed`，不自动重试、补图或换模；页面明确区分技术状态与客户质量，拒绝图只在默认折叠审核中显示风险摘要，不显示原图或下载入口。
- 既有 settled 300 测试积分通过 Serializable 幂等补偿事务全额返还；原 consume 与 Provider 成本记录保留，不伪造 Provider 退款。重复审计/恢复只保留一条 compensation refund。
- 原三张文件的 SHA-256 在补偿与两次审计前后完全一致：`0b6cb2f6…bd8c`、`0e70f36a…e86`、`9664e446…71b`；未修改、覆盖或删除。

## 精确文件归属

- API/合同：`apps/api/src/products/beauty-industry/media-contract.ts`、`apps/api/src/routes/beauty-industry-media.ts`、`apps/api/src/services/lanqi-media-generation.ts`、`apps/api/src/services/credit-reservations.ts`、`apps/api/package.json`。
- Web：`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`、`apps/web/src/styles/beauty-industry.css`。
- 回归：`scripts/beauty-industry-image-quality-p1-smoke.ts`、`scripts/beauty-industry-image-quality-live-postflight.ts`、`scripts/verify-beauty-image-quality-browser-e2e.mjs`、`scripts/beauty-industry-real-media-smoke.ts`、根 `package.json`、`pnpm-lock.yaml`。
- 文档：本任务卡、`STATUS.md`、`TEST_MATRIX.md`、`PRODUCT.md`、`WORKFLOW.md`、`CONTRACTS.md`、`docs/BUG_REGRESSIONS.md`、任务索引；`governance/runtime-assets.json` 只同步已验证的受控输出适配器源码哈希，不改变运行映射。

## 验收证据

- `pnpm.cmd beauty-industry:image-quality-p1-smoke`：PASS；合成 clean/QR fixtures，Provider 0。
- 对历史资产设置 `BEAUTY_IMAGE_AUDIT_DIR` 后同一专项：PASS；assets=3、rejected=3、指定风险图命中、Provider 0。
- `pnpm.cmd beauty-industry:image-quality-live-postflight`：PASS；provider_tasks=3、customer_usable=0、compensation_refunds=1、net_test_credits=0、assets_unchanged=PASS、Provider 0。
- `pnpm.cmd beauty-industry:image-quality-browser-e2e`：PASS；真实 API+DB+隔离 Chrome 桌面/390px、刷新、三图隔离、owner/cross-tenant asset 404、重复恢复不新增账本、console=0、external provider requests=0。
- `pnpm.cmd beauty-industry:real-media-smoke`、媒体观测与 Web 合同专项、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含 build）、`git diff --check`：PASS。
- `pnpm.cmd beauty-industry:runtime-assets-governance-smoke`：PASS；8 active chains、11 canonical assets、runtime/generated candidate references=0、物理删除未授权。

## 环境与费用

- AcceptanceRoot：`F:/思潼AI增长os/test-environments/beauty-industry-by20-manual-live3-20260826`。
- PostgreSQL `55434/PID29020`、API `3016/PID35320`、Web `5176/PID24036`；source/runtime 完整指纹 `6A346355A1F3B61EDD9211E31DC42BEE687F80ABA8424ABB8279C17E5F02C2AF`，`source_fresh=true`、PID/listener 一致、`/ready database=true`、Web 200。最终刷新后再次完成 live postflight 与桌面/390px Chrome 回归，Provider 请求仍为 0。
- 本修复真实 Provider/外网调用 0，新增费用 ¥0；历史三图真实 Provider 成本仍保守记录 ¥0.60。媒体保持 disabled，QA-20260826-007 范围残余 P0/P1=0；产品全局仍有 BY-19、BY-20 两项独立 P1。

## 下一次最小真实图片终验建议（未授权，不执行）

- Provider/模型：`aliyun_bailian / wan2.7-image`；仅同一全合成 XHS 任务，一个父批次、严格 3 张/最多 3 个子任务。
- 价格边界：按当前版本化报价单图最多 ¥0.20，三图人民币最坏上限 ¥0.60；文本、视频、ASR 和其他 Provider 0。
- 自动重试、修复调用、换模、追加调用均为 0；任一任务失败或终态不明立即停止剩余任务。
- 成功必须为三张均通过 payload、逐图本地质量筛查、客户资产授权、账本单次结算与桌面/390px 门禁；任一图拒绝则整批 `quality_failed` 并按完整交付合同补偿。

## 2026-08-27 修复后唯一真实终验

- 授权：不可重放 grant `by26-image-quality-live-20260827-once-6406cf12ef38` 已消费；同一全合成任务、`aliyun_bailian/wan2.7-image`、最多3任务/3张、人民币硬上限 ¥0.60，文本/视频/ASR/其他 Provider=0，自动重试/修复/换模/补图/追加=0。
- 调用：只创建1个 Provider task `35daad6b-aa88-415f-b2a1-e35f9582fa07`，约16.36秒技术成功；落盘资产任务 `cmtaopwvf02qd5t2hxpusg2p7`，768×1024、SHA-256 `f408e29e809711fdf4e791d271f86b1fffe2efd84d9d5033c4ae955fde64f24e`。本地筛查命中 `qr_or_barcode_like`、`visible_text_or_brand_like`、`interface_or_watermark_like`，首图即 `visual_quality_rejected`，后两条本地占位任务以 `batch_stopped_after_provider_failure` 终止且 Provider task id 为空；没有第2/3/4次调用。
- 客户结果：批次 `quality_failed`，`customerUsable=0`、客户预览/下载=0；owner/cross-tenant asset均404。原始资产保留在隔离租户审计目录，未修改/覆盖/删除；人工目视只确认其为无可见文字的合成护肤品静物，不能替代确定性门禁，说明当前筛查存在偏保守/误报风险。
- 账本：300测试积分一次预留、一次等额 release，reservation=`released/actualAmount=0/errorCode=beauty_media_visual_quality_failed`，consume=300、refund=300、净额0；重复审计/确认不增加任务、交易或余额变化。Provider真实成本按1个成功任务保守记录 ¥0.20，不声称Provider退款。
- 顺序补强：占位任务新增 `batchIndex`，读取时优先按该序号排序；逐图提交以 `customerUsable` 为前置，避免同毫秒 `createdAt` 次序不稳定造成后续任务误提交。客户资产接口同时要求整批 `succeeded`，不完整批次即使单图本地通过也不能下载。
- 真实页面：API+隔离数据库+Chrome桌面/390px、刷新、重复确认、折叠审核、客户下载0、双租户404、console=0、external requests=0均PASS。live postflight两次审计确认 Provider task=1、技术成功=1、质量拒绝=1、未提交=2、资产SHA不变、账本净额0。
- 环境：AcceptanceRoot不变；PostgreSQL `55434/PID1960`、API `3016/PID30176`、Web `5176/PID17568`，source/runtime=`45CE21E7C5310816E81636C480409865E62CDECFF05F0C2F2C50537CB2C3624B`、`source_fresh=true`、ready database=true、Web200。终验后 `.env.media-approval` 已删除，媒体恢复 `disabled/max_real_images=0`，已成功审计资产仍仅内部保留。
- 放行结论：安全门禁按设计工作，但“三张客户可用图片”真实成功门未通过；本次授权耗尽，不追加调用、不部署。残余 P0=0，BY-26/QA-20260826-007 业务 P1=1（本地风险筛查对合成瓶罐/空白标签过度敏感，需独立零费用精度红灯后再决定是否申请新终验）。
- 全量回归：图片质量、real-media、媒体观测、API/Web/Agent与全仓typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）、独立`build`及`git diff --check`全部PASS；这些postflight与回归没有新增外部Provider请求。

## 2026-08-27 本地检测误报精度闭环（零 Provider）

- 修复前红灯：对 SHA `f408e29e…f24e` 的人工确认无字瓶罐静物运行旧筛查，稳定同时得到 `qr_or_barcode_like / visible_text_or_brand_like / interface_or_watermark_like`。局部证据显示底部瓶罐/托盘边缘被通用对比度窗口误聚成 QR，长水平纹理被拆成字形，规则没有 finder、条纹、字形或 UI 布局的独立证据。
- 根因修复：检测器升级为 `beauty-image-safety-v2`。QR 只接受横纵 `1:1:3:1:1` finder pattern 与三点近直角组合；条码要求成组平行条纹、方向一致性和 quiet zone；文字/品牌要求连续字形轮廓、基线与间距证据；水印/UI 要求角落字形/高亮墨迹布局或多行多列面板/重复条形联合证据。单一矩形、瓶身、空白标签、自然高光不再直接命中。
- 可审计证据：每个风险判定持久化 detector type、confidence、原图坐标 bbox、有限几何指标和是否解码；不保存图片正文、Prompt 或 Provider 原始响应。证据不足进入 `manual_review_required`，页面与 API 明确“不可交付”，客户预览/下载仍 404；不把不确定性叠加成三项确定违规。
- 精度/召回：真实安全图 `PASS`，指定历史坏图 `REJECT`，三张历史风险资产 3/3 继续拒绝且 SHA 未改。合成负例 9/9（瓶罐/空白标签/矩形色块/自然高光及缩放、低对比）通过；风险正例 32/32（QR、条码、英文/中文样式/乱码、Logo、水印、UI及缩放、3°旋转、低对比）拒绝；测试集 precision=`1.000`、recall=`1.000`。另有不确定字形夹具稳定进入 `manual_review_required`。
- 交付链保持：纯画面/完整负向 Prompt、顺序最多3任务、任一图失败即停、三图完整交付、300积分结算/补偿、租户资产隔离和无自动重试均未放宽。历史资产没有重写、重判数据库状态或删除。
- 文件归属：`apps/api/src/products/beauty-industry/media-contract.ts`、`apps/api/src/routes/beauty-industry-media.ts`、`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`、`apps/web/src/styles/beauty-industry.css`、`scripts/beauty-industry-image-quality-precision-p1-smoke.ts`、`scripts/beauty-industry-image-quality-p1-smoke.ts`、`scripts/beauty-industry-real-media-smoke.ts`、根 `package.json` 及本任务文档。
- 测试：精度/召回专项、历史三资产专项、real-media、媒体观测、fixed-route、XHS交付、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含 build）、`git diff --check` 全部 PASS；外网、OCR云、视觉模型和其他 Provider=0，新增费用 ¥0。
- 当前放行：误报精度原子 P1 已关闭，P0=0；QA-20260826-007 的“三张真实客户合格图”业务门仍 P1=1。媒体保持 `disabled/max_real_images=0`，不部署。若进行下一次终验，必须新 grant、同一全合成任务、`wan2.7-image` 严格最多3张、无重试/修复/换模/补图，建议人民币硬上限仍为 ¥0.60；未经授权不调用。
- 最终本机刷新：正式 `status.ps1` 先证明修改后 source/runtime=`EE6D0FB4/45CE21E7`、`source_fresh=false` 且 PID/listener 身份一致；随后只用同一 AcceptanceRoot 的正式 `stop.ps1`/`start.ps1` 安全替换。当前 PostgreSQL `55434/PID23908`、API `3016/PID8628`、Web `5176/PID14568`，完整 source/runtime 指纹均为 `EE6D0FB46501C9AFEC08547CCB430E75BD34BA2BC029F1BC6A290671118C8E7F`，`source_fresh=true`、ready database=true、Web200，text=controlled_mock、media=disabled/max0。
- 刷新后 postflight 与真实隔离 Chrome：历史批次 Provider task仍1、资产SHA不变、客户可用/下载0、账本净0；桌面/390px、刷新、owner/cross-tenant 404、console=0、external provider requests=0均PASS。本轮没有重判或改写历史 rejected 证据；v2 的 `manual_review_required` 页面文案/序列化由离线合同和源码门禁覆盖，未伪报为新的真实图片终态。

## 2026-08-27 v2 检测器授权后真实终验

- 授权：一次性 grant `by26-image-quality-live-20260827-once-b9be9009c418`，同一全合成“皮肤管理产品/附近女性顾客/小红书图文”任务；`aliyun_bailian/wan2.7-image`最多3张/3任务，单图最多¥0.20、总硬上限¥0.60，300测试积分，重试/修复/换模/补图/追加0。grant消费后已删除，不可重放。
- 执行：真实Web确认只创建1个Provider子任务；Provider任务哈希前16位`854c626e537e3f42`，约22.1秒返回`SUCCEEDED`，随后本地任务以`asset_persistence_failed`终止。没有持久文件，故`beauty-image-safety-v2`没有可筛查资产；后两条占位任务均`batch_stopped_after_provider_failure`且无Provider task id，无第2/3/4次调用。
- 根因边界：当前持久化函数依次包含输出URL安全校验、HTTP下载、Content-Type、大小、本地写盘/rename/元数据写入，但路由catch统一保存`asset_persistence_failed`，受控日志没有更细阶段码。授权禁止重查/重试，且未持久化Provider原始响应或URL，因此更细子根因证据不足并明确为未知；不得归因网络、白名单或磁盘中的任一项。
- 客户/账本：整批`quality_failed`，客户预览/下载0；300积分一次consume、一次refund，reservation=`released/actualAmount=0/errorCode=beauty_media_visual_quality_failed`、净0。图片Provider按1个成功任务保守记录¥0.20，不伪造Provider退款；文本、视频、ASR和其他Provider0。
- 页面/隔离：真实runner验证桌面、390px、刷新、重复确认、双租户隔离、console0；技术成功未冒充客户可用，人工目视未替代正式门禁。
- 撤权：`.env.media-approval`及一次性grant均删除；同一AcceptanceRoot经正式stop/start恢复text=`controlled_mock`、media=`disabled/max0`，不部署。
- 最终环境：PostgreSQL55434/PID24596、API3016/PID22800、Web5176/PID9928；source/runtime=`EE6D0FB46501C9AFEC08547CCB430E75BD34BA2BC029F1BC6A290671118C8E7F`、`source_fresh=true`、PID/listener和绝对源码命令一致，ready database=true、Web200；临时Chrome/测试进程0。
- 放行结论：P0=0，BY-26/QA-20260826-007业务P1=1。本次授权耗尽且绝不追加调用；下一恢复点是独立零费用修复资产持久化分段可观测和合成失败夹具，之后再由总调度决定是否申请新的真实终验。

## 2026-08-27 BY-27 后最终真实终验（本次授权已耗尽）

- 授权与预检：用户授权本次总费用不超过人民币 ¥1；实际 runner 使用更严格的版本化边界 `aliyun_bailian/wan2.7-image`、最多3张/3个 Provider 子任务、单图保守¥0.20、总上限¥0.60，文本/视频/ASR/发布/投流及其他 Provider=0，自动重试/修复/换模/补图/追加=0。一次性 grant `by26-image-quality-live-20260827-once-d1f6f5a8e1e9` 在真实确认前消费并于 postflight 后删除，不可重放。
- 环境与输入：AcceptanceRoot `F:/思潼AI增长os/test-environments/beauty-industry-by20-manual-live3-20260826`，执行前 source/runtime=`9134655C4F56C98D7B91981B394FC5954E309B6BEE79AAC8057FCAEFC73973A5`、`source_fresh=true`、API/监听/PID/绝对源码一致；文本保持 controlled_mock。仅使用全合成“皮肤管理产品/附近女性顾客/小红书图文”任务，制作说明与负向 payload 保持纯画面、无文字/字母/数字/Logo/品牌/二维码/条码/水印/UI 边界。
- 真实调用：只创建1个 Provider 子任务（任务 ID 仅保留 SHA-256 前16位 `e37d6b3ed9b2eb79`），技术终态 `SUCCEEDED`；后两张因首图质量硬失败未提交，没有第2/3/4次调用。保守 Provider 费用¥0.20≤¥1，其他付费调用0。
- 资产与质量：首图成功完成 BY-27 的本地原子持久化和落盘校验，768×1024，SHA-256 `5181dadec432f36f24c93ccfaf3872d85cde69c6927c0d2ff3df1987d9a1653a`。`beauty-image-safety-v2` 在 bbox `(416,966,304,14)` 命中 `corner_watermark/interface_or_watermark_like`，confidence=`0.96`，整批 `quality_failed`、customerUsable=0、客户预览/下载0。人工只读目视为无明显文字、二维码或品牌的合成护肤品静物，主题和事实边界合格，但人工不能覆盖正式 fail-closed 门禁；该局部证据暴露新的真实图角落纹理误报 P1。
- 账本与幂等：300测试积分一次预留、一次等额 release，reservation=`released/actualAmount=0/errorCode=beauty_media_visual_quality_failed`，consume300+refund300、净额0；重复确认和两次 postflight 不新增 Provider task、交易或余额变化。Provider真实成本按1个技术成功任务保守记¥0.20，不声称 Provider 退款。
- Web/租户：真实 Web runner 与 postflight 覆盖桌面1440、390px、刷新、历史恢复、重复确认、无横向溢出、console0、双租户资产404；失败批次不显示图片或下载按钮。由于真实批次未形成三张合格图，成功态的三图选择/保存/下载不能宣称通过，只能由零 Provider 合同专项证明实现存在。
- 回归：图片 precision/recall（安全图PASS、历史风险图REJECT、9负例/32正例、precision/recall 1.000）、质量、持久化11阶段、real-media、media-observability、API/Web/Agent typecheck、真实失败批次 Chrome、`qa:fast`、`qa:regression`、`qa:full`（含build）与 `git diff --check` 均 PASS。错误配置的 precision 命令曾因漏传两个显式资产环境变量退出，补齐同一已登记 SHA 路径后 PASS，未触发 Provider。
- 撤权与最终环境：`.env.media-approval` 和本轮 grant 已删除；正式 stop/start 恢复 `media=disabled/max0`，PostgreSQL55434/PID18648、API3016/PID35432、Web5176/PID14364，source/runtime=`9134655C…`、source_fresh=true、ready database=true、Web200。审计资产保留在隔离租户本地存储，客户不可访问；不部署。
- 放行结论：安全、账本、顺序停止和持久化门禁均按设计工作，但“三张真实客户合格图”最终结果未达成。残余P0=0、P1=1；本次授权已耗尽，不得追加真实调用。下一步只能先用该新 SHA 建立角落水印误报的零 Provider 红灯并修复检测证据，之后再由总调度决定是否继续该产品结果。

## 2026-08-27 角落自然纹理误报精度闭环（零 Provider）

- 修复前红灯：将 SHA-256 `5181dadec432f36f24c93ccfaf3872d85cde69c6927c0d2ff3df1987d9a1653a` 作为只读受控负例。旧 v2 稳定以 `edge_components` 拒绝：5组件、baseline deviation 1.166、实际组件宽度覆盖率仅0.072；收紧该分支后又以 `corner_bright_ink_layout` 拒绝：ink density 0.043、active rows/columns=31/33、每活跃行约4.5像素，证明两个弱证据先后把右下自然叶片/枝条当水印。
- 最小修复：检测器版本升为 `beauty-image-safety-v2.1`。角落组件序列必须同时满足宽度覆盖、间距、高度一致性与多数合理字形比例；亮点兜底必须同时满足更强密度、每活跃行像素和活跃列覆盖。没有按 SHA、文件名或任务ID放行，也没有降低二维码finder、条码、可见文字/Logo、真实水印和UI布局门禁。
- 精度/召回：真实自然纹理原图、缩放和低对比变体与既有安全夹具共12/12 PASS；QR、条码、英文/中文样式/乱码、Logo、暗/亮水印、界面角标、UI及三类变形共40/40 REJECT；历史三张风险资产继续3/3 REJECT，不确定夹具继续 `manual_review_required`。precision/recall=`1.000/1.000`，修复后同一专项连续3次一致。
- 交付边界：历史 v2 `quality_failed` 批次不重判、不改写、不补图；客户资产/下载仍0，账本不变。三张完整交付、顺序提交、租户隔离、幂等补偿和失败关闭合同均未变；Provider、外网、OCR云、视觉/文本/视频/ASR调用0，费用¥0。
- 文件归属：`apps/api/src/products/beauty-industry/media-contract.ts`、`apps/api/src/routes/beauty-industry-media.ts`、`scripts/beauty-industry-image-quality-precision-p1-smoke.ts`、`scripts/verify-beauty-image-quality-browser-e2e.mjs`，以及本任务卡、`STATUS.md`、`TEST_MATRIX.md`、`docs/BUG_REGRESSIONS.md`、任务索引。
- 验收：图片 precision、历史三资产质量、持久化11阶段、real-media、media-observability、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）及 `git diff --check` PASS。当前源码独立受控环境 `3017/PID19264`、`5177/PID28408`，source/runtime=`DE879F60DA5E79C90B426FA3DE8087522030FE7B45D5ABDF6FD9B1FF02034876`、ready/database=true、media disabled；真实Chrome桌面/390px只读恢复、刷新、历史失败态、owner/cross-tenant 404、账本变化0、console0、external request0均PASS，随后由同一session/runtime/listener/命令身份门禁停止，3017/5177已释放。
- 环境边界：原3016的正式 `status.ps1` 返回 `source_fresh=false`、reason=`pid file vs listener mismatch`，因此未停止、接管或覆盖其runtime；本次以新隔离环境完成当前源码验收。范围内QA-20260827-003/P0/P1=0；BY-26“三张真实客户合格图”业务终验仍未完成，未经新授权不得恢复，BY-19/BY-20继续PAUSED。

## 2026-08-27 v2.1 最终真实三图终验（Provider前凭据阻断）

- 前置恢复：v2.1以SHA `5181dade…653a`、既有安全瓶罐和历史风险资产再次得到安全负例12/12、风险正例40/40、precision/recall=`1.000/1.000`。图片质量/持久化/real-media/media-observability、API/Web/Agent typecheck及`qa:fast/regression/full`（含build）均在外部请求前PASS。
- 修复前红灯：现有runner缺少逐图人工可用性停点，自动安全PASS后会直接提交下一张。新增默认关闭的 `BEAUTY_MEDIA_ACCEPTANCE_OPERATOR_GATE`、租户鉴权operator review和持久`operatorQualityStatus`；自动质量与人工可用性均批准后才提交下一张，人工拒绝则整批失败并释放预留。生产默认关闭，不改变常规运行。
- 授权预检：按用户人民币≤¥1授权，runner仍锁定更严格的`aliyun_bailian/wan2.7-image`最多3张/¥0.60、无重试/修复/换模/补图/追加。创建的一次性grant `by26-image-quality-live-20260827-once-167b15da76c8`未消费。
- 阻断证据：共享受控`.env.acceptance`、仓库主机`.env`及当前进程/用户环境均没有非空`ALIYUN_API_KEY`或`DASHSCOPE_API_KEY`；start-isolated在端口监听、Provider任务、积分预留、测试租户写入前以`provider_credentials_missing` fail-closed。不得从历史日志、WorkBuddy痕迹或Provider旧响应找回密钥。
- 实际结果：Provider任务0、生成图片0、客户资产0、文本/视频/ASR/其他Provider0、费用¥0；账本预留/结算/释放均0。没有三张图片，因此桌面/390px成功态、选择/下载/刷新及3/3人工可用性均NOT RUN，不作通过声明。
- 安全收口：临时`.env.media-approval`和未消费grant已删除；隔离API/Web从未启动。本轮为数据库前置临时启动的共享受控PostgreSQL/API/Web已由正式stop脚本停止，3016/3017/5176/5177/55434监听0。脱敏记录位于仓库外`beauty-industry-by26-v21-final-live-20260827/runtime/by26-v21-final.preflight-blocked.json`。
- 文件归属：`apps/api/src/config/env.ts`、`apps/api/src/routes/beauty-industry-media.ts`、`.env.example`、`scripts/beauty-industry-xhs-image-live-acceptance.mjs`、`scripts/beauty-industry-real-media-smoke.ts`及本任务文档。外部start/stop与审计记录只服务本轮受控验收，不属于产品部署资产。
- 放行：QA-20260827-004代码范围P0/P1=0；BY-26“三张真实客户合格图”业务P1=1、P0=0。恢复必须先通过正式受控密钥入口提供非空百炼媒体凭据，再按当前源码指纹重新创建不可重放grant；无需放宽检测、账本或租户门禁。BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 v2.1 密钥恢复后的最终真实三图终验

- 凭据与环境：启动器已改为只从 `apps/api/.env` 读取 `ALIYUN_API_KEY/DASHSCOPE_API_KEY` 并在进程内验证非空、同值、`sk-`形态、无URL/空白/注释标记；禁止根`.env`回退，密钥未写入grant、runtime、日志、文档或回复。隔离API3017/PID26656、Web5177/PID16016、PostgreSQL55434，绝对TSX bootstrap、CWD、listener/PID、ready/database及source/runtime=`773820658A508103305F1C90B369EC0A5D2A14DB849769C562CC0D34D094BA47`均一致。
- 授权：一次性不可重放grant固定`aliyun_bailian/wan2.7-image`、最多3 task/3图、保守上限¥0.60（低于用户¥1硬上限），文本/视频/ASR/其他Provider0，重试/修复/换模/补图/追加均0；同一全合成“皮肤管理产品/附近女性顾客/XHS图文”任务。
- 真实结果：只创建首个Provider task，任务ID只保留哈希前12位`6bda15955b20`；Provider终态为`SUCCEEDED`。资产持久化随后以安全码`beauty_media_asset_url_rejected`、stage=`url_validation`、retryable=`false`停止，未写文件、未进入v2.1自动筛查或operator review。第2/3占位任务均无providerTaskId并以批次停止语义退款；无第4次调用。图片Provider按1个成功任务保守记录¥0.20。
- 账本：图片300测试积分一次预留、一次全额释放，实际结算0、净0；同批controlled_mock XHS前置运行按既有合同另结算8测试积分，真实文本Provider调用0。没有伪造Provider退款，也没有二次预留/释放。
- Web与隔离：live runner真实覆盖桌面、390px、刷新、重复确认、双租户404与console0，失败批次客户可用/下载0；因为没有3张落盘资产，三图人工可用、选择/保存/下载成功态均NOT RUN，不作PASS声明。
- 撤权：隔离API/Web按runtime+listener+命令行双重身份停止；共享PostgreSQL/API/Web再经正式stop脚本停止。`.env.media-approval`与consumed grant删除，3016/3017/5176/5177/55434监听0；密钥只存在于已终止进程内。
- 调用后回归：`beauty-industry:image-quality-p1-smoke`、带三条登记资产的`image-quality-precision-p1-smoke`（安全12/12、风险40/40、precision/recall=1.000/1.000）、`image-persistence-observability-p1-smoke`、`real-media-smoke`、`media-observability-smoke`、API/Web typecheck、根workspace typecheck（含`packages/agent`）、`qa:fast`、`qa:regression`、`qa:full`（含build）与`git diff --check`均PASS；没有新增Provider/外网请求。单独使用不存在的`@baolu/agent-runtime`过滤器没有匹配项目，未把它冒充PASS，Agent实际由根typecheck/qa覆盖。
- 放行结论：残余P0=0、BY-26业务P1=1。本次授权已经消费，禁止重查旧Provider task或追加调用。下一原子修复应以零Provider夹具建立百炼成功资产URL的安全域名分类、重定向/IP复验和严格allowlist适配红灯；不能把未知URL放行、不能记录完整URL/查询参数，也不能降低SSRF门禁。BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 官方资产URL修复后受控真实终验

- 修复前红灯与根因：`scripts/beauty-industry-image-asset-url-policy-p1-smoke.ts` 初次运行稳定因策略模块缺失失败；实际 `apps/api/.env` 的显式 `DOMESTIC_OUTBOUND_ALLOWLIST` 有北京OSS但漏 `oss-accelerate.aliyuncs.com`，覆盖仓库默认安全列表。既有持久化专项把validator替换为空函数，未覆盖真实runtime漂移。仓库历史真实回归已确认百炼结果使用 `dashscope-*.oss-accelerate.aliyuncs.com`，无需读取或重查已终止Provider响应。
- 最小修复：新增 `beauty-provider-asset-url-v1` 与real启动预检；仅允许HTTPS、无凭据、标准端口且属于官方accelerate/北京OSS精确域或子域，并同时要求runtime allowlist明确包含。下载改为手动、最多2次白名单内跳转且逐跳复验；伪后缀、HTTP、凭据、非标准端口、IP和站外跳转均失败关闭。受控启动只在本轮进程内补官方结果域，不修改`apps/api/.env`或任何密钥。
- 授权与环境：一次性grant严格固定 `aliyun_bailian/wan2.7-image`、最多3张/3 task、单图保守¥0.20、总上限¥0.60≤用户¥1；文本/视频/ASR/其他Provider0，重试/修复/换模/补图/追加0。隔离API3017/PID28048、Web5177/PID20352、PostgreSQL55434，source/runtime=`4B5B25DCC1DCEA6757DDE701EA60B91C8B289C2B78C4A864111FF92C88066FA9`、ready/database与绝对命令一致。
- 真实结果：第1张task技术成功、落盘、自动安全PASS，768×1024、SHA `d2af33e6ea0c559687adb8a58efc5564eb6b96343455273995505e06937ebf3d`，人工可用性批准；第2张task技术成功并落盘，768×1024、SHA `229661b38b5e322c34a10c73a11b6de7d567166365376d0ac5075dd6f931ed9f`，正式质量门命中 `interface_or_watermark_like`。人工只读复核也确认第2张含明显伪文字/伪品牌标签及人物照片，故拒绝是正确硬失败，不是误报。第3张未提交，无第4张。
- 账本与客户交付：整批 `quality_failed`，客户可用/预览/下载0；300测试积分一次预留一次全额释放、净0。两个技术成功task保守外部费用¥0.40，不声称Provider退款；重复确认和重复postflight不新增task、交易或余额变化。混合批次postflight修复前漏算已PASS审计资产，红灯为`1 !== 2`，补强后确认两资产SHA不变、技术成功2、拒绝1、未提交1、客户资产0。
- 页面与撤权：真实Chrome桌面1440/390px、刷新、重复确认、无横向溢出、双租户404、console0均PASS。隔离API/Web经runtime+listener+命令身份停止；一次性approval与consumed grant删除。正式3016/5176继续source_fresh、text=controlled_mock、media=disabled/max0；不部署。
- 回归：URL策略、持久化13个失败夹具、图片质量/precision（安全12/12、风险40/40）、real-media、media-observability、XHS交付、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）与postflight PASS；最终 `git diff --check` 在文档更新后复核。
- 放行：QA-20260827-005已关闭，URL安全子范围P0/P1=0；BY-26“三张真实客户合格图”业务P1继续为1、P0=0。本轮授权已消费，禁止追加调用。下一恢复点是以第二张脱敏审计证据补强制作说明/文字与人物风险的零Provider合同/Eval；BY-19/BY-20继续PAUSED。

## 2026-08-27 BY-29 后最新受控真实终验

- 授权审计：用户持续授权的单批人民币硬上限为¥1；本次物化为更严格的一次性、不可重放 grant，固定 `aliyun_bailian/wan2.7-image`、一个父批次、最多3张/3个顺序子任务、单图保守¥0.20、总最坏¥0.60，文本/视频/ASR/其他Provider=0，重试/修复/换模/补图/追加=0。历史 grant 没有复用、清零或覆盖。
- 调用前红灯与修复：`beauty-industry:real-media-smoke` 稳定发现真实媒体预算比较没有人民币分精度和运行时硬上限。新增 `BEAUTY_MEDIA_MAX_PROVIDER_COST_YUAN`，real模式要求正数；quote/confirm 在积分预留和Provider前按 `Number((imageCount * 0.2).toFixed(2))` 比较，超过即 `media_cost_budget_blocked`。源码/运行指纹、密钥形态、模型、数据库、空闲端口和¥0.60边界通过后才消费新grant。
- 真实结果：第1张Provider task哈希前16位 `24185382c25a5dc9`，技术成功、原子落盘、自动安全PASS且人工可用性PASS；768×1024、SHA `f3817e48…bdd30`，纯画面护肤品静物，无可见文字/Logo/二维码/水印/人物。第2张task哈希前16位 `805cf18233e10f6a`，技术成功并落盘；768×1024、SHA `2e2b8dec…c9ad2`，确定性门禁命中 `interface_or_watermark_like`。人工只读复核确认瓶身存在明显伪品牌和可见乱码文字，因此这是正确拒绝而非误报。第3张没有Provider task，无第4张。
- 客户与账本：整批 `quality_failed`，3张完整交付未形成，customerUsable/客户预览/下载均0；300测试积分一次预留、一次全额release，实际结算0、净0。两个技术成功任务按事实保守计费¥0.40，不声称Provider退款。重复确认、postflight和只读浏览器恢复没有新增任务、交易、余额或外部请求。
- 页面与测试：live runner与修复后的浏览器专项验证桌面1440、390px、刷新、历史恢复、无横向溢出、双租户资产404、console0；浏览器专项不再写死“首图拒绝”，并用具备beauty-industry entitlement的独立合成租户验证资产层404。图片/XHS/持久化/URL/媒体观测、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）、独立build与`git diff --check`全部PASS；postflight Provider调用0。
- 撤权与环境：隔离API3017/PID21952、Web5177/PID8424已按runtime/listener/绝对命令双重身份停止；一次性approval与consumed grant删除，3017/5177监听0。正式本机环境已刷新为PostgreSQL55434/PID16880、API3016/PID9408、Web5176/PID21244，source/runtime=`703354F3EC0BC9A6A361D6E938D226E9E0DBD69BDCEE378E355755571F69D953`、source_fresh=true、ready/database=true、text=controlled_mock、media=disabled/max0。
- 放行：QA-20260827-007（运行时费用硬门禁及浏览器终验路径假设）已关闭，范围内P0/P1=0；但BY-26“三张真实客户合格图”业务P1仍为1、P0=0。本批grant已耗尽，禁止把第1张部分成功冒充3/3，也禁止自动再试。下一恢复点仍是BY-26，需基于本次真实“Provider违反无文字约束、质量门正确拒绝”证据决定新的零Provider提高一次成功率方案；BY-19/BY-20继续PAUSED，不部署。

## 2026-08-27 一次生成成功率 Challenger（零 Provider）

- Champion：`scripts/fixtures/beauty-image-provider-prompt-champion-v1.json` 仅保存真实拒绝图 SHA `2e2b8dec…c9ad2`、content角色、`interface_or_watermark_like` 和“包装面出现伪品牌/乱码”的脱敏判断，不保存用户内容、完整Prompt、Provider响应、任务ID或凭据。旧合同已有通用无文字/无品牌正负约束，但“皮肤管理产品/护理用品”正向主体没有规定瓶罐包装面应为空白；Provider内部随机生成原因不可观测，证据不足部分继续标为未知。
- 修复前红灯：`pnpm.cmd beauty-industry:image-generation-success-p1-smoke` 初次稳定失败 `actual undefined / expected beauty-image-provider-prompt-v1.1`，证明图片提示没有独立版本，也没有可验证的包装面约束。
- 单变量 Challenger：新增 `beauty-image-provider-prompt-v1.1`，只在正向主体命中产品、护理用品、包装、瓶罐/容器等语义时追加“无标签中性容器、纯色哑光、完整空白表面、无印刷层/贴纸/浮雕字/品牌识别区、避免正对镜头大标签面”。没有修改原负向词、角色构图、检测器、Provider、模型、账本或客户页面；非包装画面不追加该约束。媒体job以该版本作为`promptVersion`并在parameters保存`providerPromptVersion`，文字Skill版本继续单独保存。
- Challenger结果：同一专项PASS并明确 `changed_variable=packaging_surface`；相同图片检测集安全12/12、风险40/40，precision/recall=`1.000/1.000`，历史二维码、伪品牌、乱码、UI和水印均继续拒绝。客户页面源码不包含内部包装约束或图片提示版本，已有报价/结果边界继续不展示Prompt、模型、Provider或人民币成本。
- 测试：图片生成成功率专项、XHS同页、real-media、图片质量/precision、持久化13失败夹具、media-observability、API/Web/Agent和根typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）及`git diff --check`PASS。Provider、外网、账本写入0，费用¥0；没有创建或物化grant。未改页面，因此没有重复运行桌面/390px E2E，上一轮页面结果不被重写。
- 环境：正式status先证明旧3016 PID/listener身份一致、仅source fingerprint过期，随后用仓库stop/start安全刷新；最终PostgreSQL55434/PID27316、API3016/PID25184、Web5176/PID10776，source/runtime=`41E3B4BC…`、source_fresh=true、ready database=true、Web200、text=controlled_mock、media=disabled/max0。
- 证据边界与恢复点：离线结果只证明提示合同消除了一个已知包装风险面，不能证明 `wan2.7-image` 的真实一次生成成功率已提升。QA-20260827-008的离线合同子范围关闭，BY-26“三张客户合格图”业务P1仍为1、P0=0。下一精确实验只能在未来新授权下使用同一全合成任务和相同顺序失败停止门禁，对v1 Champion历史证据与v1.1新批次做一次受控对比；本轮禁止真实调用、付费grant或自动重试。BY-19/BY-20继续PAUSED，不部署。
