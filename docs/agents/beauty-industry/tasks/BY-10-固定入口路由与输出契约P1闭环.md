# BY-10 固定入口路由与输出契约 P1 闭环

状态：P1 重新打开（2026-08-25 用户页面验收证明缺少“获客目标 + 四来源采集 + 三关筛选 + TOP10”的完整产品流程；当前停在映射确认点，尚未修改生产页面/API）

## 归属

- 产品：美业智能体（内部 `productCode=beauty-industry`）
- 层级：产品任务含必要的共享路由核对，优先限定在美业产品适配层
- 风险：高（P1：显式入口与最终业务输出不一致）
- 预计修改热点：`apps/api/src/products/beauty-industry/*`、美业专项回归、产品文档；共享 Agent/MCP 热点先只读
- 是否允许并行：否，与竞品视觉任务已串行切换

## 用户结果

用户在网页或 WorkBuddy 选定一个美业功能后，服务端始终执行该入口白名单绑定的唯一 Skill 链和输出契约，不再被自由文本、历史上下文、fallback 或 parser 切换到其他任务。

## 权威映射

- 小红书图文：`wechat-xhs-content-line` + `beauty-industry-xhs` + `beauty-industry-compliance`
- 选题系统：`baolu_topics` + `beauty-industry-content-diff` + `beauty-industry-compliance`
- 内容十件套：`baolu_content_creator` V5 + `beauty-industry-content-diff` + `beauty-industry-compliance`
- 视频数据复盘：`baolu_review_engine` + `beauty-industry-content-diff` + `beauty-industry-compliance`
- 直播话术：`live_script_planner` + `beauty-industry-compliance`
- 直播复盘：`baolu_live_review_engine` + `beauty-industry-compliance`
- 美业销售：`sales_growth_advisor` + `beauty-industry-compliance`
- 图文/视频/直播三个一级入口仅导航，不调用模型、不扣分。
- 日报、知识问答、专属交付、经营诊断、文生视频、图生视频均失败关闭；不引入投流或视频内容复盘执行能力。

## 本次范围

- 逐层复现前端参数、API 白名单、Agent/Skill 选择、Prompt 组合、Provider/fallback、历史上下文、parser/renderer 和 Web/MCP 两条路径。
- 先建立修复前失败回归，再修复最少根因。
- 输出不含敏感 Prompt 的 route receipt；账号、租户、产品和 scope 只从会话/凭据推导。
- 7 个已开放功能的 Web/MCP、对抗表达、模糊/混合表达、高风险重复 3 次和失败终态回归。

## 本次不做

- 不扩展视觉原型，不重新设计美业版面。
- 不调用图片、视频、ASR Provider，不充值、支付、发布或投流。
- 不用关键词补丁、吞异常或模板兜底冒充正常 AI 结果。
- 不覆盖共享热点或其他任务已有未提交改动。

## 验收条件

1. 正常路径：7 功能在 Web/MCP 均返回固定 capability/scope/Skill 链和该能力必填结构，高风险案例 3 次路由 100% 稳定。
2. 失败路径：错 scope、未开放能力、Provider 失败、parser 失败、取消、重复请求都 fail closed，不显示成功内容、不重复扣分。
3. 不应发生：不串 Skill/租户/行业，导航零调用，fallback 不冒充成功，MCP 入参不能伪造身份。
4. 可观测结果事件：route receipt 包含 product、channel、capabilityId、scope、skillId/version、constraintVersion、model/provider、parser、fallbackUsed、requestId 和租户脱敏指纹，不含 Prompt/密钥/客户原文。

## 基线与失败证据

- 基线命令：`pnpm.cmd beauty-industry:web-contract-smoke` 当前 PASS，说明旧门禁未覆盖“路由元数据正确但最终业务输出串线”。
- 修复前失败测试/Eval：`beauty-industry:fixed-route-output-p1-smoke` 已稳定复现两条红灯：一是显式小红书入口含“数字分身”时，共享路由把 `wechat-xhs-content-line` 覆盖为其他 Skill；二是路由元数据仍写小红书时，视频复盘/餐饮文本可到达产品持久化前，旧产品层没有最终输出合同。
- 真实证据：2026-08-24 受控 `deepseek-v4-pro` 首次选题结果被新门禁以 `unverified_runtime_fallback` 正确拒绝、未保存为成功，由此证明共享创始人 IP 扩展交付错误覆盖正式 `baolu_topics` 合同；修复后六个文本能力均取得 `finish_reason=stop` 且 `reasoning=0` 的明确终态。
- WorkBuddy 报告仅作为线索；最终根因由当前代码、受控错误 Provider、真实 Provider 安全终态和数据库 JSON-RPC 共同证明。

## 实现记录

- 修改文件：`packages/agent/src/index.ts`；`apps/api/src/products/beauty-industry/{workflows,mcp-adapter,execution,output-contract,profile,route-receipt}.ts`；`apps/api/src/routes/{beauty-industry,workbuddy-mcp}.ts`；`apps/api/src/services/agent-definitions.ts`；`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`；美业专项脚本与本任务文档。
- 数据/接口/配置变化：无 Schema、迁移或生产配置变化。网页/MCP 成功结果新增用户友好的 `abilityUsed` 和脱敏 `routeReceipt`；失败统一 `beauty_output_contract_failed`，不暴露内部 Prompt 或原始错误。
- 兼容性和回滚点：七个工作流集中白名单可独立回滚；美业产品以组合版本识别自己的正式 Skill 合同，创始人 IP 扩展选题交付保持原样并有相邻回归。
- 关键修复：显式 `capabilityLocked + requestedSkillId` 优先于共享关键词路由；美业执行不加载跨能力整段会话；原始 Provider 输出与最终输出均需满足 Skill 合同；fallback/错模块/错行业/Provider/parser 失败均在保存和结算前关闭；用户原始需求与系统档案/模式说明显式分隔。直播话术仅在用户明确要求整场/时长时启用整场 V3 结构，普通话术使用正式标准合同；销售补齐显式异议结构；失败后页面阶段提示进入终态而非继续显示“正在处理”。

## 验证

- 领域命令：`beauty-industry:fixed-route-output-p1-smoke` PASS；`beauty-industry:web-contract-smoke` PASS；隔离 PostgreSQL `beauty-industry:mcp-database-smoke` PASS；`beauty-industry:mcp-platform-smoke` PASS；FIP 选题最终交付相邻回归 PASS。
- `pnpm.cmd qa:fast`：PASS。
- `pnpm.cmd qa:regression`：首次因新增 Provider 观测包装造成旧源码形态断言 FAIL；更新为验证“预算→观测→禁止重试”完整顺序后 PASS。
- `pnpm.cmd qa:full`：PASS。
- 页面/E2E：持久验收环境已核对源码/运行指纹；Chrome 1440×1000 与 390×844 覆盖三分支、规划入口、对抗输入、合同失败终态、重试、刷新恢复、断网、取消晚到、WorkBuddy 专属入口和控制台，均 PASS，非预期控制台错误/请求失败为 0。
- 真实 Provider：选题、内容十件套、小红书文本包、直播话术、直播复盘和美业销售均以 `deepseek-v4-pro` 取得明确成功终态；错合同的中间尝试均失败关闭、释放预留，不自动重试。按安全 usage 聚合保守估算本任务文本费用不超过约 ¥0.75，低于用户批准的单批 ¥1 硬上限；图片/视频/ASR 调用 0。

## 交接

- 残余风险：当前验收链接为本机持久测试环境，不代表生产发布；Provider 实际账单以供应商最终账单为准。生产部署、付费媒体与正式支付均不在本任务范围。
- 后续任务：本任务已关闭，不自动启动视觉、媒体或新产品模块。
- 最后更新日期：2026-08-24

## 2026-08-24 用户验收新增 P1 与关机停点

- 复现输入：网页显式选择“视频获客 → 选题系统”，使用普通的皮肤管理门店选题需求。用户原文不写入任务文档。
- 脱敏证据：请求指纹 `66b13e93…f7cf`；显式路由仍为 `topic_inspiration → baolu_topics@2.1.2 → beauty-industry-content-diff → beauty-industry-compliance`，没有切换到其他模块。
- 失败规则：当前验收环境运行 `controlled_mock`；其选题受控输出只有 3 个简短条目，未满足正式 `baolu_topics` 的最小长度与四来源/三关/TOP10合同，首先命中 `beauty_workflow_output_contract_failed:min_length`。产品层正确 fail closed，AgentRun 未保存，10 测试积分预留已 `released`、`actualAmount=0`，图片/视频/ASR Provider 调用 0。
- 根因阶段结论：不是前端显式路由被覆盖，也不是四来源缺失必然阻断；正式 Skill 已规定来源缺失时标“待补/待核验”并仍可生成第一版。直接原因是把仅供路由测试的简短受控 mock 环境交给用户做业务结果验收，随后被正式输出合同正确拦截。仍需补红灯回归、改善可操作错误、启用受控真实 `deepseek-v4-pro` 验收并完成同输入 3 次质量复测后才能关闭。
- 当前未完成：修复前红灯、最小实现、真实文本 3 次、Web/MCP/账本/租户回归、桌面/390px、`qa:fast`、`qa:regression`、`qa:full`、`git diff --check` 和最新源码验收环境恢复均未运行；不得沿用此前“可验收”结论。
- 本次代码修改：无。只更新本任务卡与 `STATUS.md`；未启动或消耗任何真实 Provider。
- 进程状态：本任务持久验收 PostgreSQL `55434`、API `3016`、Web `5176` 已经现有受控脚本核验身份后停止；停止后数据库无响应、API 未就绪、Web 离线。未知进程未处理。
- 明日恢复点：先在现有 `beauty-industry-controlled-mock-routing-smoke` 增加“普通选题必须满足正式合同/受控 mock 不得冒充业务质量”的修复前红灯；再做最小产品修复和安全观测；最后把隔离验收环境从 `controlled_mock` 切到本机受控的正式文本配置，在单批不超过 ¥1、无自动重试/换模型边界内完成同一正常输入 3 次正式模型复测。

## 2026-08-25 正式闭环

- 修复前红灯：简短 `controlled_mock` 未达到正式 `baolu_topics` 合同；未配置正式 Provider 时旧运行时仍可能静默回到 mock；系统注入的合成租户名/城市被通用质量 rubric 误当作用户要求保留的事实；选题 Prompt 未把目标用户、账号阶段和 TOP10 表头设为精确骨架；390px 的九列表格撑宽页面。上述问题均先由专项或真实页面失败证据复现。
- 根因：验收环境把路由夹具当成业务质量；Provider 未配置边界未失败关闭；通用事实保留检查与美业合成租户剥离规则互相冲突；正式 Skill 说明没有转换成足够稳定的模型输出骨架；宽表缺少结果卡内滚动边界。显式路由本身始终正确，未发生跨 Skill 或跨租户读取。
- 最小修复：受控选题夹具遵循正式四来源/三关/TOP10合同；非 mock 且 Provider 未配置时明确失败；仅对固定美业选题把 rubric 事实源限定为真实用户输入；增加选题精确章节和表头骨架；宽表仅在结果区内部横向滚动。保留 `capabilityLocked`、双重结构/事实校验、预留/释放、幂等、租户隔离和失败关闭。
- 正式模型：同一正常皮肤管理选题在 MCP 连续 3 次 PASS（16.651–20.418 秒），随后真实 Web 页面连续稳定通过；固定为 `topic_inspiration → baolu_topics@2.1.2 → beauty-industry-content-diff@1.1.0 → beauty-industry-compliance@1.0.0`，模型 `deepseek-v4-pro`，fallback 0，缺失来源明确“待补/待核验”。本轮安全聚合为 8 次文本调用、估算约 ¥0.310302，低于单批 ¥1；图片/视频/ASR 0。
- 页面：Chrome 1440×1000 与 390×844 的真实生成、保存、刷新恢复、重复点击防护、断网、取消晚到、WorkBuddy 专属入口均 PASS；控制台错误 0、非预期请求失败 0。
- 门禁：`beauty-industry:web-contract-smoke`、`qa:fast`、`qa:regression`、`qa:full`、build、`git diff --check` 均 PASS。持久验收环境 `source_fresh=true`，数据库/API/Web 保持在线。
- 放行：本次新增 P1 已关闭；无未解决 P0/P1。当前链接仅为本机隔离验收，不代表生产发布。

## 2026-08-25 用户二次验收：完整四来源流程 P1 重新打开

### 新的根因结论

- 前一轮只修复了路由、Provider、正式输出结构和失败关闭，未把创始人 IP 已验收的结构化选题工作流迁移到美业页面。
- 美业页面仍把 `beauty.topic_ideas` 放在通用 `question + professionalOptions` 作曲器内，并把四来源压缩为一个 `contentStructure` 自由文本字段；Web API 与 MCP 同样没有四来源结构化字段，`BEAUTY_WORKFLOWS.topics` 仍是 `evidenceMode: "none"`。
- 因此正常需求虽然固定命中 `topic_inspiration / baolu_topics@2.1.2`，页面却无法让用户填写、上传、选择或连接正式 Skill 所需来源，也无法在调用前区分“已验证/待补/本轮不用”。这属于产品流程缺失，不是继续放宽输出校验可以解决的问题。

### 修复前红灯

- 新增：`node scripts/beauty-industry-topic-workbench-p1-smoke.mjs`。
- 修复前结果：稳定 FAIL，共 25 项；覆盖专属选题工作区缺失、四来源被压成自由文本、Web/MCP 无结构化选题字段、topic workflow 未启用证据门禁、Provider 前未评估来源。
- 本红灯只固定用户旅程与契约缺口；未删除双重校验，未调用 Provider，费用为 ¥0。

### `baolu_topics` 字段 → FIP 组件 → 美业专属组件映射（待总调度确认后编码）

正式主 Skill：`baolu_topics@2.1.2`。美业约束顺序保持 `beauty-industry-content-diff@1.1.0 → beauty-industry-compliance@1.0.0`，仅约束行业事实与合规，不改写主流程。

| Skill 正式字段/阶段 | FIP 当前已验收组件/后端 | 美业拟采用组件与字段 | 失败/待补规则 |
|---|---|---|---|
| 本轮获客目标 | `TopicSystemWorkbench.topicBriefCard`；`identity/targetCustomer/acquisitionGoal/offer/accountStage/industry/subjectId` | `BeautyTopicSystemWorkbench.goalBrief`；门店/项目从已确认经营档案注入，用户本轮填写 `targetCustomer/acquisitionGoal`，可选 `offer/accountStage/platform`，`industry` 从已确认细分赛道带入并可核对 | 缺目标或目标人群不调用；门店/行业未确认时给具体补充入口，不猜测 |
| 私有知识与客户问题 | FIP `AI录音卡`连接、同步、当前 `subjectId` 下已确认转写过滤；请求字段 `transcriptDocumentIds` | 第一来源卡：已授权知识/录音/顾客常见问题；字段 `sourceSelection.transcript + transcriptDocumentIds`，只显示脱敏标题、确认状态与所属门店 | 未连接/无合格资料标“待补”或“本轮不用”；错主体、未确认、敏感资料拒绝 |
| 行业与用户热点 | FIP 行业卡 + `buildIpCapabilityIntelContext`；请求字段 `sourceSelection.industry + industry` | 第二来源卡：当前美业细分赛道、城市/人群及公开线索核验状态；字段 `sourceSelection.industry + industry` | 无可回溯来源标“待核验”，不得把模型常识写成实时热点 |
| 自身账号数据复盘 | FIP `latest-video-review` 卡；请求字段 `sourceSelection.videoReview + videoReviewId` | 第三来源卡：选择当前租户已成功解析/保存的视频数据复盘；字段 `sourceSelection.videoReview + videoReviewId` | 没有真实复盘显示“去上传账号数据复盘”；不得虚构播放/完播/互动/转化 |
| 同行与对标内容 | FIP 对标账号卡；请求字段 `sourceSelection.benchmark + benchmarkAccounts`，公开研究结果再核验 | 第四来源卡：平台、账号名/主页链接、核验状态；字段 `sourceSelection.benchmark + benchmarkAccounts` | 同名/不可访问/无互动证据标“待核验”，不能称爆款 |
| 来源候选池 | FIP `assessFounderIpTopicEvidence` + `buildFounderIpTopicEvidenceDirective`；正式 Skill 内部形成 16–20 条候选 | 第三阶段“来源候选与证据核验”：按四来源显示已采用数量、证据状态、待补动作；候选不暴露系统 Prompt | 至少一项有可核验证据可继续；1/2/3 来源按已有来源生成并标缺口；四项都不可用才阻止 |
| 三关筛选 | 正式 Skill：目标用户兴趣证据 → 共识层级与客资准度 → 账号阶段配比 | 第四阶段“三关筛选与排序”：显示每条通过/待验证、共识层级、客资准度、适用阶段；不显示伪精确综合分 | 无互动证据可作为“待验证”继续，但必须给最小验证动作；不得冒充已通过 |
| 最终交付 | FIP `parseFounderTopicSelections` + TOP10 Markdown 表 + `用此选题进入内容系统` | 第五阶段 TOP10：固定九列 `最终选题/类型/来源/第一关证据/共识层级/客资准度/适用阶段/创作建议`，并保留配比建议、待验证动作、单条选择 | 非 10 条、字段缺失、错行业/错 Skill 或错误来源均失败关闭，不保存、不结算 |
| 下一步 | FIP 创建带 `subjectId/target/audience/sourceEvidence/factBoundary/goalRelation` 的内容草稿 | 美业选择某条后创建同租户内容十件套草稿，携带当前目标、证据状态和事实边界；按钮明确“进入内容十件套”，不自动调用/扣费 | 跨租户、来源任务失效或字段缺失时阻止；不把模型推断晋升为门店事实 |

> 说明：正式 Skill 的用户可见四来源顺序采用 `私有知识与客户问题 → 行业与用户热点 → 自身账号数据复盘 → 同行与对标内容`。FIP 现有页面的卡片顺序和“AI录音卡”旧展示名只作为已验收交互实现来源，不原样复制品牌/旧文案；底层字段映射保持兼容。

### 拟采用的五阶段页面结构（待确认）

1. **本轮获客目标**：目标人群、获客动作、已确认项目、账号阶段；显示经营档案来源与本轮覆盖值。
2. **四来源资料采集**：四张独立卡，每张都有启用状态、资料入口、核验状态、待补动作；连接/上传/选择都发生在本页。
3. **来源候选与证据核验**：先展示真实采集结果与缺口，再允许用户确认进入模型；不向用户输出内部审计大表。
4. **三关筛选与排序**：真实展示证据状态、标签和账号阶段配比，不使用综合分。
5. **TOP10 与内容承接**：最终表格、配比建议、待验证动作；选中一条进入内容十件套并继承目标、来源和事实边界。

## 2026-08-25 内容十件套跨产品提示层 P1 串行闭环

- 修复前证据：`beauty-industry-workflow-composition-smoke.ts` 直接检查最终 `messages`，稳定命中跨产品 sample layer、通用身份和 product experience；污染源可追溯到 `baolu_content_creator` 排序前三个 example 以及 V5 历史示例段。
- 根因：美业 `skillPromptOverride + capabilityLocked` 没有声明自己的组装策略，generic Agent 仍无条件叠加 examples/identity/experience；不是模型能力不足，也不是前端路由改变。
- 修复：增加显式 `locked_product_workflow` policy，并沿 API、self-MCP、runtime 指纹传递；仅美业固定工作流启用干净组装。保留租户、事实、安全、结构和 capability lock；FIP clean branch 和 generic Agent 行为未变。内容十件套采用 V5 十项契约的 capability-safe 视图，再叠加美业差异与合规约束。
- 红灯转绿：修复前 `content-ten:cross_product_example_contamination` FAIL；修复后 7 个美业工作流 × 高风险 3 次 PASS，generic examples 与 FIP 分支不变断言 PASS。
- 真实模型：最终批 Web 1 + MCP 2 连续 3 次 `deepseek-v4-pro` PASS，十件套完整，餐饮/招商/客户名/演示数字 0；同键 replay 未重复调用。三次费用估算约 ¥0.125，本轮累计约 ¥0.206≤¥1；媒体调用 0。
- 账本与凭据：近期 4 条内容十件套成功记录均 settled；3 枚临时 MCP 凭据均 revoked，无悬挂预留。
- 页面：持久隔离环境桌面 1440×1000 与 390×844 均完成真实结果、历史、刷新恢复，控制台错误 0；现有正式页长结果排版偏密集，已在独立视觉原型中形成卡片化方案，未在本原子修复中替换生产页面。
- 门禁：美业专项、MCP 平台、FIP 相邻专项、`qa:fast`、`qa:regression`、`qa:full`、build、`git diff --check` 均 PASS。
- 状态：本 P1 已关闭；BY-10 整体仍因“四来源选题专属工作区尚未接入生产页面/API”保持 P1 打开，不得对外宣称全部可验收。

### 同一 Skill 驱动规范下的旁路盘点（本轮不编码）

| 子页面 | 正式 Skill/版本 | 成熟 FIP 逻辑可复用 | 美业当前缺口与后续专属页面 |
|---|---|---|---|
| 内容十件套 | `baolu_content_creator@5.0.0` + 美业差异/合规 | `ContentSystemWorkbench` 的选题带入、草稿恢复/编辑/保存、缺失后重试、停止生成、Word 下载；正式十件套固定顺序 | 当前仍是通用单框。后续改为“选题与目标 → 必要补充资料/追问卡 → 十件套生成 → 编辑保存/Word下载 → 下一步”，本轮不改 |
| 视频数据复盘 | `baolu_review_engine@2.0.0` + 美业差异/合规 | `VideoReviewWorkbench` 的 CSV/Excel 上传、解析状态、停止、问题咨询/专业报告分离；正式合同要求数据质量审计、字段覆盖、分层和证据边界 | 美业虽有文件输入但仍嵌在通用框架。后续改为“上传 → 解析 → 字段映射/预览 → 复盘 → 选题回流”，无解析不得生成，本轮不改 |
| 视频内容复盘 | `baolu_content_creator@5.0.0` 的 `shooting_editing` 合同 + 美业差异/合规 | `ContentSystemWorkbench` 的真实视频上传入口；正式合同要求元数据、关键帧/画面、音频/转写证据后才出拍摄/口播/节奏/剪辑建议 | 当前产品保持未开放。后续专属页必须显示视觉/ASR解析阶段、部分失败/全失败和待补；解析失败禁止声称看过，本轮不开放 |
| 直播复盘 | `baolu_live_review_engine@3.0.0` + 美业合规 | `LiveReviewWorkbench` 的文字咨询与数据文件上传分离、缺资料降级、八模块结果；正式输入为直播后台数据、转写、原定话术/目标 | 美业当前把资料压在一个 `contentStructure` 输入。后续改为“数据/转写/计划三类资料 → 核验 → 八模块报告 → 下场动作”，完全无证据只给补充清单并停止，本轮不改 |

### 编码前安全停点

- 当前只新增红灯测试与本任务文档；生产 Web/API/MCP/Skill/Prompt/Schema 均未修改。
- 需总调度确认上表的字段映射和五阶段页面结构后，才进入选题专属页面与 Web/MCP 结构化合同编码。

## 2026-08-25 四来源选题完整页面最终闭环

- 已实现：美业选题页语义复用 FIP `TopicSystemWorkbench`，以美业展示层替换品牌文案，不复制第二套四来源逻辑。五阶段为获客目标、四来源采集、来源/证据状态、三关筛选与 TOP10、选择选题进入内容系统。
- 结构化合同：Web 与 MCP 统一传递 `topicWorkflow`；服务端不信任客户端证据，按当前 tenant/user 重取已确认转写与最近视频复盘。1/2/3/4 来源均可运行并标缺口，全部来源不可用才在 Provider 前阻止；错租户、未确认、错行业证据失败关闭。
- 后置门禁：把服务端已验证的目标人群、账号阶段、获客目标写回正式选题章节；只允许“以后进入内容系统生成拍摄脚本”等明确边界说明，真实内容十件套/拍摄脚本章节继续拒绝。该修复处理的是产品后置合同，不改变固定路由或主 Skill。
- 修复前红灯：`beauty-industry-topic-workbench-p1-smoke.mjs` 25 项 FAIL；`beauty-industry-topic-fact-retention-p1-smoke.ts` 先后固定事实保留误拒绝、合法承接误判和真实跨模块章节拒绝。
- 正式模型：最终当前源码以 `deepseek-v4-pro` 完成 MCP 连续 3 次和 Web 1 次，均为 `topic_inspiration / baolu_topics@2.1.2 / beauty-industry-content-diff@1.1.0 / beauty-industry-compliance@1.0.0`，fallback 0、TOP10/四来源/三关完整。最终稳定 4 次估算 ¥0.131944；已跟踪文本调用总额低于本批 ¥1，图片/视频/ASR 0。
- 页面与协议：1440×1000、390×844 的真实生成、保存、刷新恢复、重复点击、断网、TOP10 选择与内容承接 PASS；内容承接不触发新 Provider。隔离 PostgreSQL MCP initialize/list/call、scope、跨租户、账本预留/结算/释放、幂等与撤销 PASS，临时 credential 无 active 残留。
- 门禁：选题专项、Web contract、MCP database、`qa:fast`、`qa:regression`、`qa:full`、build、`git diff --check` 全部 PASS。旧 `topic-knowledge-selection-smoke` 更新为验证解析后的资料来源范围，FIP 默认仍锁定当前资料夹。
- 放行：BY-10 当前范围无未解决 P0/P1，可在本机隔离环境验收；未部署生产。旁路盘点中的内容/复盘页面后续改造仍是独立任务，不纳入本次放行。
