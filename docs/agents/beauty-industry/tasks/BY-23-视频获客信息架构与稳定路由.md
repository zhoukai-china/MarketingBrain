# BY-23 视频获客信息架构与稳定路由

状态：完成

## 归属

- 产品：美业智能体（`productCode=beauty-industry`）
- 层级：产品专用
- 风险：中（用户可见命名、导航、深链与权限）
- 预计修改热点：`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`、`BeautyIndustryWorkBuddyPage.tsx`、导航/路由专项与产品文档
- 是否允许并行：否；BY-21 自动矩阵后串行实施，BY-20/BY-19继续暂停

## 用户结果

视频获客首页以六张清楚卡片组织选题、内容、复盘与规划能力；用户可从复盘系统独立页选择数据或内容复盘，旧深链继续可用。

## 本次范围

- 用户可见“内容十件套”统一改名为“内容系统”，内部 tool/capability/Skill 和历史十件结构不变。
- 新增 canonical `/acquisition/video/content`，旧 `/content-ten` 保持同工作区兼容且 `apiBase` 不丢失。
- 新增 `/acquisition/video/review`，只选择既有视频数据复盘与视频内容复盘。
- 新增 `/acquisition/video/viral-replication` 规划页，无执行、账本或 Provider 副作用。
- 视频获客一级页固定六卡：选题系统、内容系统、复盘系统、爆款复刻（规划）、文生视频（规划）、图生视频（规划）。

## 本次不做

- 不实现爆款复刻，不改变任何正式 capability/Skill/权限/账本/媒体门禁。
- 不调用 Provider、不部署、不恢复 BY-20/BY-19。

## 验收条件

1. 正常路径：新旧内容深链、复盘二级页和两个复盘旧深链都可稳定定位并保留 `apiBase`。
2. 失败路径：无权限只显示真实状态，不创建任务；规划页只有边界说明。
3. 不应发生：规划卡执行、扣费、Provider 调用、旧路由丢失或 WorkBuddy capability 改名。
4. 可观测结果事件：路由/标题/卡片数量、Provider=0、费用¥0。

## 基线与失败证据

- 红灯：旧视频一级页并列暴露两个复盘入口、用户可见名仍为内容十件套、无复盘系统页和爆款复刻规划页。
- 绿灯：`pnpm.cmd beauty-industry:video-ia-p1-smoke`、导航壳层、旅程与 Web 类型 PASS。

## 实现记录

- 修改文件：`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`、`apps/web/src/pages/BeautyIndustryWorkBuddyPage.tsx`、`apps/api/src/products/beauty-industry/{workflows,mcp-adapter}.ts`、相关 smoke/package scripts。
- 数据/接口/配置变化：无；仅用户可见命名和路由层次变化。
- 兼容性和回滚点：旧 `content-ten`、data-review、content-review 路由仍解析到原工作区；可独立回滚新增页面路由。

## 验证

- `beauty-industry:video-ia-p1-smoke`、`navigation-shell-p1-smoke`、`journey-p1-smoke`、Web/API typecheck：PASS。
- 页面/E2E：真实 Chrome 桌面/390px PASS；六卡、复盘二级选择、canonical/旧别名、刷新/返回、`apiBase` 保留、规划页零副作用、console errors=0、Provider requests=0。
- `pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`（含 build）、`git diff --check`：PASS。
- 未运行项：真实 Provider、生产部署，按范围禁止。

## 交接

- 残余风险：范围内 P0/P1=0。
- 后续任务：BY-24 已完成；本机当前源码入口统一交付。
- 最后更新日期：2026-08-26
