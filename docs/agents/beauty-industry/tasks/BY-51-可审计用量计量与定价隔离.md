# BY-51 可审计用量计量与定价隔离

状态：本地范围完成/idle；2026-09-05。唯一美业编码任务，BY50本地完成后串行接力。

## 归属与用户结果

美业通用核心含最小服务端审计子改动，高风险。让运营能够区分真实调用用量、未知用量与客户积分结算，失败和重放不会抹掉成本证据。本轮先接BY50默认关闭的视频执行链，不恢复暂停入口，不声称全平台计量已迁移。

## 范围 / 不做

- 复用AuditLog主键幂等、既有数据库/事务、CreditReservation/CreditTransaction和视频permit；不造第二账本或公开consume接口。
- 独立action/call/attempt及provider关联；token/image/video_second的严格离线合同；明细与按模型/单位/价格版本分组聚合分离。没有usage/价格/账单证据保持unknown，不写成0。
- 当前正式视频HTTP链追加不可覆写事件；审计失败在外部调用前阻断，提交后未知保留pending，不重发；迟到usage只补审计，不追扣积分。
- 不改报价、月费/额度策略、不实施A3页面改版或AI剪辑、不修改FastAPI3007/WorkBuddy原件。不读密钥、登录云、联网、Provider、费用、grant、邀请、生产发布或现有验收环境。

## 验收 / 红灯计划

1. 正式HTTP视频成功/失败路径没有独立用量事件；missing usage直接return、观察费用覆盖，没有可恢复的未决调用计量明细。先自动红灯再修。
2. 启动记录先落库，重复/并发同事件幂等；冲突字段拒绝。进程中断保留未知、迟到证据可恢复且不倒改历史。
3. 多模型多单位分组不盲加；缓存和计费来源独立；货币用整数微单位，已知部分不冒充完整成本。
4. 三轮合成专项与独立PG并发/恢复、跨租户、原视频账本只结算成功逻辑动作一次；审计服务不写积分表。
5. 相邻专项、API/Web/Agent/Shared typecheck、qa:full含fast/regression/build、diff-check。无DOM/路由新增则不重复浏览器，不冒称UI验收。

## 候选与基线

冻结根`C:/Users/book/WorkBuddy/sitong-outsourcing/_FREEZE-local-candidate-20260905`仅参考；已完整读START-HERE、能力映射v2026-09-05-l、计费规格、增量§10–16。指定下午补充文件未在该根找到，已反馈调度；不借此读未冻结副本。MANIFEST的520项/779链接为候选自述，不是正式验收。

正式证据：AgentRun仅tokenEstimate；CreditReservation负责用户逻辑动作；AuditLog已有主键及tenant索引；BY50 permit有一次性claim/计数，但observe缺usage直接return、有usage只update观察成本。billing route已有credits_only，不能替换成候选订阅/消费接口。

修改前工作树477条既有未提交状态；不提交/回退/覆盖其他语义。精确归属、测试、日志与未完成项在收口更新。

## 本轮精确文件归属

- 新增通用美业内部服务`apps/api/src/services/beauty-usage-metering.ts`：严格数值/安全字段、不可覆写AuditLog、幂等绑定/未知/冲突、多单位分组、上海月视图。无新schema、依赖或public route。
- 既有BY50文件`beauty-video-execution-permit.ts`仅追加计量begin/accepted/observe；`beauty-video-controlled-execution.ts`仅包装提交ack与poll/异常的观测。保留签名、次数、预算、素材、账本安全门禁。
- 新增`scripts/beauty-usage-metering-smoke.ts`和`scripts/fixtures/beauty-usage-worker.ts`；修改BY50 `scripts/beauty-video-execution-smoke.ts`追加正式HTTP证据、故障/迟到/DTO与账本断言，保留BY50测试。`replication-test-db.ts`只补AuditLog主键唯一语义。
- `scripts/acceptance/beauty-industry/video-foundation-db.ps1`与`video-foundation-stop.ps1`仅增加BY51独立root/套件白名单，复用原PG身份门禁；package.json仅新增usage专项及已有video-execution专项先串行运行它，不重写其他既有脚本。
- 文档本卡/STATUS/TEST_MATRIX/PRODUCT/WORKFLOW/CONTRACTS/tasks README，PRE_FINAL_FOUNDATIONS采纳拒绝矩阵，新增USAGE_METERING与BUG_REGRESSIONS条目。以上全属通用美业核心，不增品牌知识；工作树大部分为BY11–50及其他任务既有未提交，不整体提交。

## 过程证据与纠偏

- `red.log`真实注册HTTP/协议fixture在成功任务后无beauty_usage_v1事件，精确断言FAIL；补服务与接线后三轮PASS。
- PG首轮测试夹具误用User.name（正式为nickname）失败；只修fixture字段，不改Schema。随后PG三轮通过。
- 新增审计故障PG测试首次FAIL：只替换主Prisma client方法未影响interactive transaction，故障未注入。改在真实transaction callback代理该次audit.create，三轮验证预留释放一次、submitCount回滚0、Provider0；没有把该测试缺陷伪报成用户生产事故。失败root都保留并安全停止。
- 最终fixture覆盖未知价/用量、缓存、token/image/video_second分组、失败attempt、明确账单与价格估计分离、重复值/冲突、数值小数规范化、晚到跨月、跨租户/店/人、审计失败、3进程append、停止子进程后重建read、无计量积分写入。它不是已接真实文本流的证明；stream delta只允许具体adapter先归一化，矛盾快照继续unknown。

## 候选来源结论

经调度澄清，允许只读唯一工作副本附件`CODEX-增量补充-20260905-下午.md`，已经完整读取；不读取其余未冻结页面。采纳A3语义审计/AI剪辑后续需求/G单位分层，拒绝整包覆盖、无授权chat-test部署、改价与cache商业承诺。文件hash、A3六行/九行与“11处”的口径差异、AI剪辑授权边界均已记PRE_FINAL_FOUNDATIONS。

## 最终验收与命令

- `pnpm.cmd beauty-industry:usage-metering-smoke`（也纳入video-execution/qa:regression）：3轮PASS，纯fixture与独立PG均覆盖多单位/缓存/币种价格、未知/迟到/冲突、月份/跨租户、三进程主键竞争、幂等重建。计量服务creditWrites=0。
- `pnpm.cmd beauty-industry:video-execution-smoke`：正式Fastify handler+受控真实协议适配注入，3轮PASS；每轮合成submit8/poll4/download3/storage177，Provider/外网0、费用0；两个既有成功逻辑任务净测试积分200，失败/审计故障/迟到不再结算。数字不是商业新报价或真实云开销。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/acceptance/beauty-industry/video-foundation-db.ps1 -Suite BY51 -Port 55451`：最终exit0，独立PG三轮计量+三轮完整视频HTTP PASS，三进程同事件只有一条；旧表/schema兼容无新迁移、原BY50保留记录回退探针PASS。
- `pnpm.cmd --filter @baolu/api typecheck` PASS；最终`pnpm.cmd qa:full` exit0（`qa-full-release.log`），实际包含qa:fast、qa:regression、API/Web/Shared/Agent全仓typecheck和build，BY45–50、媒体/WorkBuddy/品牌/XHS相邻回归PASS。没有把过程失败标绿。
- `git diff --check` PASS。主证据根`F:/思潼AI增长os/test-environments/by51-metering-offline-20260905`：red.log、green-final.log、PG过程与pg-frozen-source.log、qa-full-release.log、前后status/stat和diff-check。

## 环境 / 文件指纹 / 停止与回退

- 最终owned PG根`F:/思潼AI增长os/test-environments/by51-offline-275f0c542fc9499fbb7f3d6033394dc7`，session同尾号，PG55451/PID25160，14:50:06启动、14:50:56按runtime/data/绝对命令/listener双身份停止，合成审计DB保留。test.log/video-http-test.log各3轮PASS，stop.log及runtime.state=stopped俱在。所有本轮其他PG root也已停止，不处理其他进程。
- 正式维护宿主Windows PowerShell5.1组合源码/runtime一致：`E12B1C24410AFAD905DEE3A37DD5226723AD2B8F0A67ED6F865B9F456153CCA7`。只读复核PowerShell7.6产生`3B0FF2B97B89211CDEAB01F0736DBACD8F347D1D84468EE6DB322524DAAD7685`，核心文件SHA未变；宿主口径差异P2未在本卡改共享维护脚本，不猜具体排序/编码原因，不混用这两个hash判断fresh。没有实际API/Web runtime可宣称fresh。
- 核心SHA256：meter=`CE416E6E6CB4736BB1B0B564C536CAE866A00FE93968649FB19E663CBAD93AE2`；permit=`30719C1F0809409B8553D5156C15281F1BEDB959F1B7063632D5740C2A721F54`；assembly=`9F8EE3DF3EA87922FFDF2F5A8258557E65638D350ADA24100B59473E1C94587A`。
- 不删除审计/账本/历史。回退先保持execution disabled，再回退本卡语义接线；原表和事件保留，未知调用不重发。需要停止仍运行的BY51环境时仅正式`video-foundation-stop.ps1 -AcceptanceRoot <精确owned root>`，当前已停止不重复认领。
- 最终工作树479项（粗粒度git status；目录内多文件合并显示），前后状态保存；没有commit/整体回退，其他既有477项语义保护，最终新增文件与共享追加范围见上表。

## 未运行、残余与下一恢复点

本地计量子范围P0/P1=0，QA-20260905-007关闭；环境跨宿主指纹P2保留。无DOM/客户API/页面合同变化，本轮不重复1440/390Chrome、不启动API/Web/用户验收入口；真实Provider/流式SDK/云账单/生产对账未验收，不能宣称全平台计量、真实云成本或客户报表已上线。费用0、外部请求0、grant0、邀请0、生产部署0。

下一唯一建议A3对应正式共享导航/积分入口零费用只读差距审计；发现正式P1再独立建卡，不因候选自报缺陷直接改产品。不自动启动AI剪辑、不继续外部账号验证或恢复暂停产品。停止并向总调度反馈。
