# BY-12 美业智能体工作台正式首页

状态：已完成（2026-08-25，本机隔离环境可验收，未部署生产）

## 归属

- 产品：美业智能体（`productCode=beauty-industry`）
- 层级：产品专用，复用现有租户、积分、经营档案、WorkBuddy 与 AgentRun 合同
- 风险：高（正式首页、权限、真实租户数据、深链和核心导航）
- 预计修改热点：美业经营工作台页面与样式、Web 路由、首页确定性回归和产品文档
- 是否允许并行：否；同一美业智能体当前只允许本任务编码

## 用户结果

用户进入美业智能体后，先在正式经营首页看见真实经营状态和下一步建议，再按权限进入获客、销售、经营档案、WorkBuddy 或任务中心，并能通过深链、刷新和返回恢复正确页面。

## 本次范围

- 深绿色侧栏、暖白内容区、今日建议、经营档案完整度、图文/视频/直播三张获客卡、销售、数据连接和最近任务。
- 积分、档案、连接、今日建议与最近任务只读取当前租户真实接口数据；无数据展示真实空状态。
- 开放状态由服务端工具权限和真实连接状态决定；未开放能力显示“规划中”。
- 经营档案、WorkBuddy、任务中心和现有子工作区使用稳定路由，支持刷新和浏览器返回。
- 加载、接口错误、重试、桌面、390px、控制台与租户隔离回归。

## 本次不做

- 不修改 Skill 映射，不增加自由文本路由。
- 不开发视频数据复盘、视频内容复盘、直播复盘或其他新模块；只保留已有入口和开放状态。
- 不调用文本、图片、视频或 ASR Provider；新增费用必须为 0。
- 不部署生产。

## 验收条件

1. 正常路径：真实租户首页数据正确，图文/视频/直播/销售、经营档案、WorkBuddy 和任务中心均进入唯一正确页面；子工作区深链刷新、返回恢复。
2. 失败路径：初始加载和连接状态请求失败时显示安全错误与重试；无档案、无连接、无任务时显示真实空状态。
3. 不应发生：不得显示 `2680`、`12条`、`2个`等原型示例；不得把无权限能力表现为可用；不得改变 Skill/工具映射、调用 Provider、跨租户读取或制造伪连接状态。
4. 可观测结果事件：首页数据来自现有鉴权 overview/profile/history/connections；导航只改变产品路由，不创建 AgentRun、媒体任务、积分流水或外部动作。

## 基线与失败证据

- 基线命令：`pnpm.cmd beauty-industry:home-p1-smoke`。
- 修复前失败测试/Eval：FAIL，报错 `missing formal-home marker: beautyIndustryShell`；同一时点 `pnpm.cmd beauty-industry:web-contract-smoke` PASS，证明既有业务合同可用，缺口集中在正式首页壳层与导航。
- 现象：根路由直接纵向展开 Hero、档案和工作区，缺少经营总览、真实连接状态、任务中心和稳定子路由。
- 根因：BY-10/BY-11 先完成业务工作区与真实合同，尚未建立面向经营者的首页信息架构和可回归的 Web 路由契约。
- 连带影响：用户无法先判断真实经营状态再进入任务；刷新和返回也没有独立首页/档案/任务中心状态可恢复。

## 实现记录

- 修改文件：
  - `apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`：新增正式首页、经营档案和任务中心视图；接入真实 overview/profile/history/WorkBuddy connection 数据；实现权限感知卡片、空态、错误重试与稳定子路由。
  - `apps/web/src/styles/beauty-industry.css`：新增深绿色侧栏、暖白内容区、桌面与 390px 响应式样式。
  - `apps/web/src/main.tsx`：让 `/agents/beauty-industry/*` 由美业页面恢复深链。
  - `scripts/beauty-industry-home-p1-smoke.mjs`、`package.json`：新增 P1 首页确定性回归并并入美业 Web 合同门禁。
  - 本任务卡、`STATUS.md`、`TEST_MATRIX.md`、`tasks/README.md`、`docs/BUG_REGRESSIONS.md`：登记范围、红灯、根因、回归和交接。
- 文件归属：上述页面和样式文件同时承载 BY-10/BY-11 的既有未提交实现；BY-12 只增加正式首页、路由、样式和回归相关差异，未覆盖、回退或整体提交其他任务改动。
- 数据/接口/配置变化：无 Schema、API、Skill 映射、自由文本路由或生产配置变化；只复用现有鉴权接口。
- 兼容性和回滚点：正式首页视图、嵌套路由、BY-12 样式和回归脚本为独立回滚点；现有图文、视频、直播工作区和执行合同保持兼容。

## 验证

- 修复后领域命令：
  - `pnpm.cmd beauty-industry:home-p1-smoke`：PASS。
  - `pnpm.cmd beauty-industry:web-contract-smoke`：PASS。
  - `pnpm.cmd --filter @baolu/web typecheck`：PASS。
  - `pnpm.cmd --filter @baolu/web build`：PASS。
  - `node --import tsx scripts/beauty-industry-profile-database-smoke.ts`：PASS，验证租户 A/B 隔离并清理夹具。
  - `pnpm.cmd beauty-industry:mcp-database-smoke`：PASS，验证产品凭据、scope、租户绑定、幂等、计费与撤销/轮换；仅使用合成合同 Provider，无外部调用。
- 质量门禁：`pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`、`git diff --check` 全部 PASS；`qa:full` 包含全仓 typecheck、回归和 workspace build。
- 页面/E2E：在 `http://127.0.0.1:5176/agents/beauty-industry?apiBase=http%3A%2F%2F127.0.0.1%3A3016` 实际验证：
  - 桌面端读取当前租户真实积分 `488`、档案 `0%`、最近任务 `1`、WorkBuddy 未连接和真实今日建议；未出现原型示例数字。
  - 视频选题、任务中心已有任务、经营档案、WorkBuddy 均进入唯一正确页面；深链刷新和浏览器返回恢复正确。
  - API 指向不可达端口时显示安全错误和重试；重试仍失败时状态稳定，页面未泄露堆栈。
  - 390px 验证无文档级横向溢出；桌面、移动和失败页控制台均无新增 error/warning。
- 费用：本项未调用任何模型或媒体 Provider，新增费用为 `0`。
- 未运行项：生产部署、生产配置变更、真实外部动作和后续视频/直播复盘模块均按范围未运行。

## 交接

- 受控环境：PostgreSQL `127.0.0.1:55434`（PID `12564`，数据目录 `F:/思潼AI增长os/test-environments/beauty-industry-acceptance-20260821/pgdata`）、API `127.0.0.1:3016`（PID `28764`）、Web `127.0.0.1:5176`（PID `25232`）；均核对为本产品验收环境，未停止未知进程。
- 验收链接：`http://127.0.0.1:5176/agents/beauty-industry?apiBase=http%3A%2F%2F127.0.0.1%3A3016`。
- 残余风险：无未解决 P0/P1；未发现新增 P2/P3。工作树包含其他既有未提交内容，后续提交必须继续按文件/差异归属隔离。
- 用户验收清单（不超过 5 步）：
  1. 打开验收链接，核对租户名、积分、今日建议、档案完整度、连接状态和最近任务均为当前真实数据。
  2. 从视频卡进入视频选题，刷新页面，再使用浏览器返回确认回到首页。
  3. 打开任务中心并进入已有任务，确认返回任务中心和首页均正确。
  4. 分别打开经营档案与 WorkBuddy，确认无重复入口、伪连接或无效按钮。
  5. 将视口缩至 390px，确认页面无横向溢出，且未开放能力显示“规划中”。
- 后续任务：首页完成后停止并反馈总调度；不得自行启动任何复盘模块。
- 最后更新日期：2026-08-25
