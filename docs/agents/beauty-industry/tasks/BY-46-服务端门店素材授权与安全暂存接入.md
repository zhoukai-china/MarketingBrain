# BY-46 服务端门店素材授权与安全暂存接入

状态：零Provider本地工程范围完成，idle；开工2026-09-05 08:14 +08:00，当日收口。真实云端/用户页面未放行。

## 归属/唯一结果

- beauty-industry通用核心，产品任务含文件/Schema平台子改动，高风险；唯一编码任务，不并行。
- BY-45基础已完成，不重复开发协议。将其admission接到服务端持久素材授权与可替换私有暂存接口，合成File→授权→暂存→现有job闭环；不等同真实云端/视频验收。
- XHS、经营问答、BY-19/20/43、BY-44外部接入保持暂停。不建邀请/grant、不读取密钥、不部署，Provider与真实客户/肖像上传0、费用0。

## 范围/不做

- 复用UploadedFile、Membership、Store、TenantProductEntitlement、AuditLog、既有视频任务/积分。
- 服务端派生tenant/store；文件hash、用途、owner/kol、声明依据引用、到期/撤销/版本。记录声明不声称法律真实性已独立核验。
- 安全私有文件读取、类型/尺寸实测；暂存对象最小范围、TTL、身份绑定、失败清理；调用前再核验与撤销语义。
- 未配置真实暂存保持fail-closed；本地注入transport只作隔离验证，不伪装公网/云端已开通；不绕过原型file URL浏览器拒绝，不新做页面。

## 基线与红灯

- BY-45专项PASS；正式默认admission仍没有文件授权持久记录和staging绑定，quote不可确认，confirm422。
- 初始红灯：新专项稳定失败`BY46 missing server-persisted file/store authorization admission`（尚无独立持久模块/Schema）；后续补权限、hash、撤销、TOCTOU、暂存与账本HTTP+PG回归，不以客户端勾选代替服务端证据。
- 集成红灯：同租户其他用户job混入列表后，正式GET history因admission的404整体失败。新增自动断言`BY46 unrelated user's job must not break authorized history`稳定得到404≠200。修复只在查询limit前按user过滤，并对逐项已撤销/缺失资源隐藏；DB、审计和产品权限错误不吞。红绿日志见下文。

## 脏改与文件归属

- schema基线394新增/66删除行，最终450新增/66删除行；本项增量56行仅追加`BeautyVideoAssetAuthorization`、`BeautyVideoStagingLease`及Tenant/Store/UploadedFile最小反向关系，不改既有字段/枚举/迁移。已有巨量未提交改动全部保留，未提交/回退/整体覆盖。
- 新增：`apps/api/src/services/beauty-video-asset-authorization.ts`（声明/角色/版本/撤销）；`beauty-video-private-files.ts`（私有bytes/hash/类型实测）；`beauty-video-private-staging.ts`（可替换driver、本地签名与TTL/清理）；`beauty-video-material-integration.ts`（组装/每阶段重验/撤权停止）；`scripts/beauty-video-material-authorization-smoke.ts`；`packages/db/prisma/migrations/202609050001_beauty_video_material_authorizations/migration.sql`。
- 在BY-45文件上仅追加本项接口：`apps/api/src/services/viral-video-replication.ts`的stagingLeaseId；`viral-video-replication-runtime.ts`的授权hook、提前幂等恢复、暂存清理与历史user过滤；`apps/api/src/routes/viral-video-replication.ts`的声明/撤销和默认fail-closed组装、有效历史过滤。
- 测试/脚本：扩展`scripts/fixtures/replication-test-db.ts`，`scripts/acceptance/beauty-industry/video-foundation-db.ps1`和`video-foundation-stop.ps1`只增加BY46独立root/suite；`package.json`新增本专项并把BY45/BY46加入qa:regression。没有修改品牌包、Skill或页面，也没有引入依赖。
- 文档：本卡、STATUS、TEST_MATRIX、PRODUCT、WORKFLOW、CONTRACTS、tasks/README、PRE_FINAL_FOUNDATIONS、BUG_REGRESSIONS。源码新增四个beauty-video模块内候选/intake/quarantine/兰琪字符串运行依赖0。

## 验收/交接

- 三轮内存与三轮独立PostgreSQL均PASS：身份/门店/角色/用途/expiry/revoke/hash、同hash角色重传拒绝、文件路径与类型/字节/尺寸、并发声明/确认、暂存失败/TTL/签名/清理失败恢复、预留后提交前撤权、外部已提交状态的本地合成恢复、历史过滤/下载隔离、幂等重放。每轮只一个注入submit，真实Provider0；本地私有服务实际HTTP总15次。
- 每轮测试积分1000→900（一个成功100积分），另两个失败预留各等额释放，最终released=2/settled=1；这只是合成账本，不是获批视频报价或真实Provider计费。
- `pnpm.cmd beauty-industry:video-material-authorization-smoke` PASS x3；`pnpm.cmd beauty-industry:video-foundation-smoke` PASS（qa:regression内再次运行）；`pnpm.cmd --filter @baolu/api typecheck` PASS；`pnpm.cmd qa:fast` PASS；`pnpm.cmd qa:regression` PASS；最终`pnpm.cmd qa:full` PASS，包含新源码重新运行qa:fast、qa:regression、全仓typecheck（API/Web/Agent等）和build。`git diff --check` PASS；仅既有LF/CRLF提示，不当测试失败。
- DB命令：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/acceptance/beauty-industry/video-foundation-db.ps1 -Suite BY46 -Port 55446`。最终合成空库升级新迁移PASS；有记录后在事务内rename两表/核对记录数/还原表名PASS，未删持久客户数据，未做生产迁移或旧版应用部署回滚演练。
- Prisma validate初次受仓库默认DATABASE_URL格式影响失败；进程内使用明确合成URL后validate/generate通过，没有输出/复制真实密钥。

## 最终证据、边界与恢复

- 最终PG证据根：`F:/思潼AI增长os/test-environments/by46-offline-15ee41e6563e402491854932191c73ba`，runtime/test/schema/stop日志；55446/PID23856，08:46:14启动→08:46:39已按data/runtime/绝对命令/listener身份核验停止。source=`25043259285DAF4F4687B05FCBC45C0EEF8EE3FFEC744BD6F8D631B0C922D518`。
- 红绿与qa完整日志根：`F:/思潼AI增长os/test-environments/by46-offline-e07823b78ead4bc1b7affada3a6eaddb`，`history-red.log`（确切404）、`history-green.log`、`qa-fast.log`、`qa-regression.log`、`qa-full.log`。该根早先PG6636也已安全停止，runtime源指纹仅代表其当次阶段，不能冒充最终源码。
- Schema SHA256=`51B667F82598E64EE737AB7DB59933E38DA0B6946AE755FF8024B34A448412E7`；新增migration=`3896563AD42128AED4348060358458C9FA1369229EB3ABED1D63810A0812E1FF`。既有source指纹函数不覆盖schema，故另记hash。
- 环境限制：既有Get-SourceFingerprint跨PowerShell主版本输出不同（PS7.6.5为`B664F563…B42414`）；改回与正式runner相同的Windows PowerShell并带ExecutionPolicy Bypass复核仍精确为`25043259…22D518`，源码扫描未发现运行后改动。此处只记录shell相关性，不把跨shell值当源码漂移/接管依据，也未顺手修改公共指纹算法。后续维护须用同一正式启动入口；跨shell可重复性属于独立P2候选。
- 最终相邻`node apps/api/node_modules/tsx/dist/cli.mjs scripts/viral-video-replication-smoke.ts` PASS。全仓未提交列表和diff统计已存同日志根`git-status.txt`/`git-diff-stat.txt`，最终`diff-check.log`无差异错误；不包含密钥、文件正文或私有签名URL。
- 测试端口/进程均停止，最终55446/55434/3016/5176无监听；未接管旧环境，未创建浏览器/邀请/grant。后续重跑必须全新BY46 root/空闲端口；如需停止遗留本项PG，仅使用`video-foundation-stop.ps1 -AcceptanceRoot <该唯一root>`双身份核验，不处理未知PID。
- 回滚：停用BY46组装并保留新增表与声明/lease审计；旧BY45缺admission继续422。数据库资产不drop，需恢复时核对版本/hash后重启独立环境；本轮没有执行生产回滚/迁移。
- 未运行：真实存储/外部视频、客户素材/肖像、云端DNS/凭据/删除验收均0；没有真实视频预算/素材上传授权，不得用旧图片≤1元授权。无DOM/路由页面改动、没有UI定稿，本轮未运行桌面/390浏览器E2E，也未绕过此前CUA file URL拒绝；HTTP handler注入及真实loopback服务不能冒充Chrome页面PASS。
- 费用¥0，本地工程P0/P1=0；真实端到端前置阻塞1（私有云driver+明确授权素材/视频预算及页面验收），能力不开放。BY-19/BY-20/XHS等保持PAUSED。
- 下一唯一建议：先做清单C“既有ASR文件/任务恢复协议”的零Provider只读差距审计；确认真实缺口后另卡，不自动开发或读取客户录音。BY46真实云接入要独立授权，不为继续而扩大权限。

## 后续用户验收清单（本轮不发放入口）

1. 待正式授权页面完成后，确认当前门店/文件及声明依据；区分“声明已记录”与“法律真实性核验”。
2. 试跨店/错角色/缺依据：须在任务和积分前阻断。
3. 测撤销/到期/hash变化：不得继续提交或下载，原文件与审计仍保留。
4. 后续真实暂存和视频获单独授权后，才测试一次生成/重复确认/历史与积分。
