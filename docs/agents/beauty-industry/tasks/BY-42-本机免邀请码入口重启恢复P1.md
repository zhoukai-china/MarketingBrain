# BY-42 本机免邀请码入口重启恢复 P1

状态：已完成

## 归属

- 产品：美业智能体
- 层级：产品验收环境维护与登录用户路径回归
- 风险：中
- 预计修改热点：受控启动记录、产品登录入口、首次直达浏览器回归；仅在真实代码缺陷被证明时修改产品源码
- 是否允许并行：否

## 用户结果

用户在电脑重启后直接打开本机 XHS 深链，可以通过仅本机开通进入原工作台并正常加载，而不是停在缓存登录页或连接失败。

## 本次范围

- 独立核对 PostgreSQL 55434、API 3016、Web 5176 的监听、进程、runtime、源码指纹、ready/database 和静态资源。
- 复现首次直达、产品登录回跳、刷新/返回、桌面 1440 与 390px。
- 环境离线时按正式 AcceptanceRoot 身份门禁恢复当前源码；若恢复后仍失败，再建立代码红灯并做最小修复。
- 保持本机免邀请码只在受控 local-only 环境可见，不新增匿名 API 或生产绕过。

## 本次不做

- 不继续 BY-41 图片场景视觉，不恢复 BY-19/BY-20。
- 不调用文本、图片、视频、ASR 或其他 Provider；费用必须为 ¥0。
- 不部署生产，不停止或接管未知进程，不清理数据库。

## 验收条件

1. 正常路径：首次直达 XHS 深链进入美业登录，点击本机开通后保留 `apiBase` 并返回原深链，主内容可交互。
2. 失败路径：API/数据库离线、失效会话、跨租户和权限错误均显示真实可恢复状态；不得循环登录或伪装成功。
3. 不应发生：不得只依赖 status 记录、丢失 query、开放生产免登录、调用 Provider、覆盖其他任务改动。
4. 可观测结果：listener/PID/绝对命令/runtime/source 一致，`/ready ok=true/database=true`、Web 200；1440/390px 首开/刷新/返回 console 与关键网络错误为 0。

## 基线与失败证据

- 2026-09-01 独立端口核对：55434、3016、5176 均无监听；`/ready` 与 Web 直达均 `connection refused`。
- 正式 `status.ps1` 同步报告 database no response、api_ready=false、web offline、pid_matches_listener=false；旧 runtime 指纹 `4A8B91E5`，当前源码指纹短值 `5C879173`，不能事后认领旧进程。
- 真实浏览器首次直达稳定得到 `ERR_CONNECTION_REFUSED`；用户看到的登录页属于浏览器已加载的旧页面状态，当前没有可用后端。
- 修复前红灯：本任务环境身份与浏览器首次直达证据；恢复后再运行 `beauty-industry:local-entry-auth-browser-e2e` 验证是否存在第二层产品缺陷。

## 实现记录

- 真实根因是电脑重启后受控 PostgreSQL/API/Web 没有恢复，旧 runtime 文件并不代表进程在线；本轮没有发现新的登录或深链产品代码缺陷。
- 在三个端口均无 listener、无未知进程可接管的前提下，只使用 `scripts/acceptance/beauty-industry/start.ps1` 恢复同一 AcceptanceRoot。启动脚本重新生成当前源码指纹运行记录，没有复用或事后认领旧 PID。
- 当前 PostgreSQL 55434/PID14652、API3016/PID1484、Web5176/PID6116；API 命令为当前仓库绝对 TSX preflight + `src/server.ts`，Web 命令为当前仓库 Vite，PostgreSQL data dir 属于本 AcceptanceRoot。
- source/runtime 完整指纹 `5C879173F691BB58A787C51A902F07DC84BD8535D43044422364B2EDBFD272EB`，`source_fresh=true`、PID/listener一致、`/ready ok=true/database=true`、Web200。
- 本任务没有修改登录、权限、租户、数据库业务数据或 Provider 配置；运行 profile 为 `safe_default`，文字 `controlled_mock`，媒体 `disabled/max0`。

## 验证

- `pnpm.cmd auth:product-login-smoke`：PASS。
- `pnpm.cmd beauty-industry:local-entry-auth-browser-e2e`：PASS；真实无会话/失效会话、原XHS深链+apiBase、1440/390px、刷新/返回、双租户、主按钮、console/网络均通过；业务提交0、Provider0。
- in-app Chromium 新鲜页面：桌面1440与390px均显示 `图文获客`、主生成按钮、overflow0；新鲜tab console warn/error 0。
- API/Web typecheck：PASS。
- `pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`（含build）：PASS。
- `git diff --check`：PASS。

## 交接

- 残余风险：本入口范围 P0/P1=0；环境已在线。BY-41 的门店场景视觉仍为独立暂停任务，不在本入口结论中冒充完成。
- 用户入口：`http://127.0.0.1:5176/agents/beauty-industry/acquisition/xhs?apiBase=http%3A%2F%2F127.0.0.1%3A3016`；当前浏览器已有有效本机验收会话，无会话会显示仅本机开通并返回原深链。
- 费用：外部 Provider 0，新增费用 ¥0。
- 后续任务：恢复并收口 BY-41；BY-19/BY-20 继续 PAUSED。
- 最后更新日期：2026-09-01。
