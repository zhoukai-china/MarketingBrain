# BY-45 授权素材视频换人底层链路

状态：零Provider底层工程与本地事务专项完成；真实业务接入/页面验收未完成（2026-09-05）

## 归属与边界

- beauty-industry通用核心；品牌仅配置；唯一美业编码任务，不并行。
- 恢复依据：2026-09-05用户及总调度新授权。仅本任务恢复；XHS、经营问答、BY-19/20/43、BY-44外部接入仍暂停。
- 目标：已有授权参考视频+已授权人物照片，固定`wan2.2-animate-mix`异步换人；不宣称换门店背景、新口播、克隆声音、字幕已实现。
- 不调用Provider、不上传真实人物/客户素材、不读取密钥、不建grant/邀请、不部署或改变现有运行进程。
- 不改共享脏schema、现有XHS/Skill/品牌包；新文件与精确差异只属于BY-45。

## 基线与红灯

- `pnpm.cmd content-system:viral-replication-smoke`修复前PASS，但只断言旧字段，未覆盖HTTP/授权/持久化。
- 原服务builder默认`wan-animate-mix/person_image_url/preserve_original_audio`，正式官方协议是`wan2.2-animate-mix/image_url/video_url/mode`及异步头。
- 原callback取得远程URL即标succeeded；未验证本地落盘、可播放或下载授权。原refund为先读后写，未CAS；原confirm无输入指纹冲突保护，提交终态不明被误写为未接受。
- 当前UploadedFile仅tenant归属，缺store/肖像/参考授权持久证据及安全staging；原客户端四boolean不能代替服务端授权。默认执行必须失败关闭。
- 新专项首个正式模型断言在旧builder稳定失败（expected `wan2.2-animate-mix`，actual `wan-animate-mix`）；修复后同一断言通过。测试后补阶段曾因动态/static模块导入使异常class identity不一致误判可恢复poll，统一测试静态导入后PASS，没有放宽运行判定。

## 候选审查

来源：WorkBuddy原型根`C:/Users/book/WorkBuddy/sitong-outsourcing/beauty-xhs-prototype-20260827/`，`handoffs/codex-ready-能力映射与先行开发清单.md`、设计规范6.3、数据打通HTML与`video.html`。

| 建议 | 结论 |
|---|---|
| 两模板/素材引用/状态恢复 | 采纳为版本化产品输入；KOL不可复用老板肖像 |
| 一次换背景+新口播+字幕+任意风格 | 拒绝本模型承诺；结构化gaps阻断，不静默忽略 |
| client consent=true即授权 | 拒绝；必须服务端匹配tenant/store/文件hash/授权期限/肖像角色 |
| 原型PNG当MP4 | 拒绝；最终必须本地验证video stream并持久化 |
| 直接复制契约/Token代表WB用户/last-write-wins | 拒绝；既有身份/账本为准，不接外部WB |
| 候选Skill/代码 | 只读参考；运行引用0 |

官方核对日期2026-09-05：[API](https://help.aliyun.com/zh/model-studio/wan-animate-mix-api)、[价格](https://help.aliyun.com/zh/model-studio/wan2-2-animate-mix)。北京std 0.6元/秒、pro 0.9元/秒，最短2秒；不得沿用历史1元图片授权。

## 验收条件

1. 正式请求/响应、异步头、同地域poll；未知/错模型/额外组合失败关闭。
2. 授权、文件类型/尺寸/时长、跨租户/门店、URL及预算均在Provider/积分前。
3. 单键单任务单预留；重复确认/回调/轮询不重复提交和结算；未知提交保留不可重放终态，不伪称上游取消。
4. 只有原子持久化+视频验证+同租户下载后成功；失败一次释放，Provider成本不伪称退款。
5. 高风险同批3轮；专项、API/Web/Agent typecheck、qa:fast/regression/full含build和diff-check。

## 修改归属（实际）

- `apps/api/src/services/viral-video-replication.ts`：现有协议与输入验证。
- `apps/api/src/routes/viral-video-replication.ts`：既有HTTP入口安全预检/状态与下载。
- 新增`apps/api/src/services/viral-video-replication-runtime.ts`：现有job/reservation事务与状态；`viral-video-replication-assets.ts`：本地MP4/probe/hash/下载，不新建队列或Schema。
- `scripts/viral-video-replication-smoke.ts`更新旧模型断言；新增`scripts/beauty-video-replication-foundation-smoke.ts`与`scripts/fixtures/replication-test-db.ts`，只用合成元数据/本地媒体/注入transport。
- 新增`scripts/acceptance/beauty-industry/video-foundation-db.ps1`和`video-foundation-stop.ps1`：只操作全新BY-45独立root，身份双门禁，保留合成审计数据不删除。
- `package.json`仅新增`beauty-industry:video-foundation-smoke`一条命令；该文件其他既有脏改不归BY-45。
- 本卡、STATUS、TEST_MATRIX、PRODUCT、WORKFLOW、CONTRACTS、tasks README、PRE_FINAL_FOUNDATIONS与docs/BUG_REGRESSIONS新增QA-20260905-001。全部属于通用美业核心/文档，无兰琪品牌硬编码、无候选运行依赖；其他历史未提交文件均保留，不整体提交、不回退。

## 实际测试（2026-09-05）

| 命令 | 结果 |
|---|---|
| `pnpm.cmd content-system:viral-replication-smoke` | PASS（旧基线与正式协议更新后均执行，新增红灯证明旧基线覆盖不足） |
| `node apps/api/node_modules/tsx/dist/cli.mjs scripts/beauty-video-replication-foundation-smoke.ts` | PASS，内存事务夹具3轮；外部fetch陷阱0，真实ffmpeg/ffprobe验证2秒240×320 H264 |
| `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/acceptance/beauty-industry/video-foundation-db.ps1` | 最终PASS，独立PostgreSQL同批3轮；tenant/user/account真实合成记录，HTTP handler→DB→文件→下载 |
| `pnpm.cmd --filter @baolu/api typecheck` | PASS；随后全仓typecheck含Web/Agent/Shared通过 |
| `pnpm.cmd qa:fast` | PASS |
| `pnpm.cmd qa:regression` | PASS（包含相邻能力；不代表恢复暂停模块开发） |
| `pnpm.cmd qa:full` | PASS（含全仓build） |
| `git diff --check` | PASS；未提交文件另经专项编译验证 |

已验证：strict/body伪造拒绝；KOL与owner角色区分；失效/不够权限/跨店/跨租户/尺寸/时长/类型/预算；不安全staging在预留前阻断；同键双确认不重复submit；重复poll单成功；4xx/429/5xx/响应缺task；已知task查询错误恢复；中断submit不重发；Provider失败/落盘失败/终态不明只一次等额释放；Provider已知成本在持久化失败时仍保留；本地下载bytes/hash一致、越权404。

## 隔离环境与审计

- 最终证据root：`F:/思潼AI增长os/test-environments/by45-offline-5cb9e023d27d4a7c9cdd07a9a78d2e90/`，`runtime.json`/`schema.log`/`test.log`/`qa-full.log`/`stop.log`。source=`BC776F6413CE874B4197EF4AC2F570DBB6C26D0E1DC1A29F2234ECBAEF595EA7`。
- 独立PG仅loopback55445/PID15840，同一session记录data目录、绝对postgres命令与listener；完成后已正式stop并核验无listener。55434/3016/5176只读检查无监听，未启动、接管或重写旧AcceptanceRoot。
- 前两次测试启动器暴露Windows PowerShell继承句柄等待与ExitCode为空，未进入DB业务专项；分别保留`by45-offline-878a7d14fc104b1abc7e38d41c63d159`(PID22452)、`by45-offline-b841d199d2e9427780a24029491ad472`(PID16476)。均经独立stop脚本核对provenance/data/绝对命令/listener后停止；未复用旧root或处理未知PID。
- 修复启动器后最终独立root PASS，不用初次失败冒充产品失败。三个root及合成媒体保留审计，无客户/真实人物素材。Provider0/外部业务网络0/费用¥0；仅此前官方文档只读核对，无账户/API调用。
- 后续停止：仅对匹配的BY-45独立root运行`video-foundation-stop.ps1 -AcceptanceRoot <精确root>`；当前均已停止，不应再次stop未知进程。回滚仅按本卡文件/精确hunk恢复，保留其他任务脏改和所有审计/历史DB，不做reset或物理删除。

## 页面与未运行

- browser-skill完整读取；bsk connected browsers=0，未创建session。备用CUA打开本地video.html被URL安全策略阻止，未绕过/改为服务器暴露。只读HTML已定位MODE/storeAssets/templates/style/本地PNG下载语义。
- 正式视频页仍规划中，当前不开发最终UI；后端用真实HTTP inject+事务集成验证。实际1440/390页面受上述限制，不能写PASS。

## 交接

- 结果为部分完成：底层本地工程范围P0/P1=0；端到端视频业务仍有1个接入阻塞（门店素材授权持久证据+安全staging未绑定），默认fail-closed，不提供用户测试入口或邀请。
- 下一唯一建议为“服务端门店素材授权与安全暂存接入”独立原子任务：先做授权记录/hash/撤销/TTL/跨店与租户零费用合同；真实肖像上传和视频调用必须另获明确素材与预算授权。当前不自动启动。
- XHS、经营问答、BY-19/20/43、BY-44外部接入继续PAUSED。未调用真实Provider、未建grant、不部署。用户验收清单：本项尚不开放，不要求用户测试半成品。
