# BY-49 视频私有云暂存适配与接线准备

状态：本地工程完成/idle；开工2026-09-05 10:24，收口10:54 +08:00。真实OSS/视频未验收，不开放页面。

## 归属与用户结果

- 唯一美业任务，产品底层高风险文件/授权子改动；不并行。
- 在BY45/46已有授权、lease、幂等、清理路径接入可替换的实际OSS私有暂存driver；默认关闭，实际云端/视频调用另需受控授权。
- 本轮不恢复XHS/经营问答/BY19/20/43/BY44外部；不改页面、Skill、品牌、视频业务组合，不建桶/改RAM/连接生产或客户桶，不读secret，不发Provider请求/付费/邀请/grant。

## 初始复用证据

- `beauty-video-private-staging.ts`已有driver接口、15分钟lease、版本/hash重验、release/sweep；只有local_only实现。
- `beauty-video-material-integration.ts`已有stage→admission→runtime→撤销/清理组装，route默认未提供driver/execution、maxCostFen=0。
- `clip-asr.ts`是DashScope临时POST policy交换，不是自管私有桶的精确GET签名/删除driver，且其他产品身份不能复用。
- package/源码检索无ali-oss或其他正式OSS SDK；已有阿里媒体地域/结果域策略包含cn-beijing。技术候选选OSS中国北京专用私有桶，地域不自动跟随请求；无真实bucket/key被读取。依官方V4/Put/Delete/签名GET协议确定实现方式，不复用候选运行资产。

## 验收与红绿

1. 修复前缺driver/正式组装可复现；修复后合成凭据与注入transport验证实际协议请求，不能以fixture作为真实mode。
2. 精确bucket/region/prefix/object/短期签名、不可覆写/hash/最小权限、DNS/IP/重定向/编码安全；逐请求业务授权与RAM分开。
3. 上传部分失败、清理失败/到期/撤权、同任务恢复、未知终态不重发，跨用户/店/租户，安全日志；高风险连续3轮。
4. 默认或不全配置在任务/积分/外发前失败关闭。无真实云权限只影响外部验收，不阻止本地工程。
5. BY45/46/47/48相邻、typecheck、qa:full含build/diff；无迁移与UI则不宣称真实PG/Chrome验收。

## 实际红灯与最小修复

- `red.log`：缺少实际provider HTTPS driver/正式route组装，静态准入断言FAIL。新增官方SDK driver及默认关闭的配置factory，不以接口空壳或mock代替真实实现。
- `revoke-red.log`：正式HTTP/同一授权链先stage且未建job，再revoke后仍有2个合成OSS对象，期望0。根因是旧集成只从reserved job寻找lease；修复为同时按tenant+authorizationVersions JSON包含查询全部未释放lease（≤100），继续原release/sweep，不删除原素材、不改账本历史。QA-20260905-005关闭本地范围。
- `sts-red.log`：官方STS.格式的合成AccessKey ID在任何请求前被错误正则拒绝；修复只接受合法STS.前缀临时凭据，长期AK/换行/短值仍拒绝。没有使用真实key或网络验证。
- 正式driver协议、版本控制/私有性、forbid-overwrite、MD5/metadata绑定、签名/删除、DNS/redirect、STS/debug/错误脱敏、默认关闭与恢复详细证据见[VIDEO_PRIVATE_OSS_STAGING](../VIDEO_PRIVATE_OSS_STAGING.md)。

## 精确文件归属（本轮增量）

| 文件 | 本轮行为/归属 |
|---|---|
| apps/api/src/services/beauty-video-oss-staging.ts（新） | beauty core，官方SDK V4/STS+受限HTTPS实际driver/安全审计 |
| apps/api/src/services/beauty-video-staging-config.ts（新） | beauty core，服务端配置组装；不安装或开启付费execution |
| apps/api/src/services/beauty-video-material-integration.ts | BY46既有未提交文件，仅增加未建job授权lease撤销清理 |
| apps/api/src/routes/viral-video-replication.ts | BY45/46既有改动上改用配置factory，保持maxCostFen0/无execution |
| apps/api/src/config/env.ts、.env.example | 只追加专用默认disabled/空值字段；未读取/编辑真实.env |
| apps/api/package.json、pnpm-lock.yaml | 精确ali-oss6.23.0及其锁文件；保留先前所有依赖改动 |
| scripts/beauty-video-oss-staging-smoke.ts（新） | 官方SDK+合成transport+正式HTTP集成三轮，无云/Provider |
| scripts/fixtures/replication-test-db.ts | BY45/46fixture，仅补JSON array_contains查询语义用于撤权红灯 |
| package.json | 追加video-oss-staging-smoke并纳入qa:regression，未删其他门禁 |
| 本卡、STATUS、TEST_MATRIX、PRODUCT、WORKFLOW、CONTRACTS、tasks/README、PRE_FINAL_FOUNDATIONS、BUG_REGRESSIONS、VIDEO_PRIVATE_OSS_STAGING（新） | 仅本项证据/配置/四类恢复边界 |

无Web/Agent/Skill/品牌/Schema/迁移更改；新增模块候选/intake/quarantine/兰琪运行依赖0。工作树大量既有改动均保留，不覆盖、不回退、不提交；不能把整份git diff归为BY49。

## 已执行命令与结果

- `pnpm.cmd view ali-oss version`：6.23.0；`pnpm.cmd --filter @baolu/api add --save-exact ali-oss@6.23.0 --ignore-scripts` PASS。外网仅官方文档/SDK源码与包注册表读取，不是“整个会话网络0”。无安装scripts/业务云请求。
- `pnpm.cmd beauty-industry:video-oss-staging-smoke`：修复前红灯分别exit1，修复后三轮PASS；每轮124次**注入**SDK请求、2次合成execution handoff，真实Provider0、云端HTTP0、测试积分净0。不是124次真实云请求。
- `pnpm.cmd --filter @baolu/api typecheck` PASS；最终`pnpm.cmd qa:full` exit0（最终进程86133已结束），包含`qa:fast`、`qa:regression`、API/Web/Shared/Agent等全仓typecheck和build。BY45真实本地ffprobe、BY46本地私有HTTP/授权、BY47共享ASR阻断、BY48导出、WorkBuddy/OAuth/相邻能力专项均随门禁PASS。
- `git diff --check` PASS；既有LF/CRLF提示不是测试失败。文档链接/路径与只读hash已核对。

## 环境、证据与未运行

- 证据根：`F:/思潼AI增长os/test-environments/by49-oss-offline-20260905`；red/revoke-red/sts-red/green/qa-full-final/diff-check与git状态/统计保留。日志不含真实凭据/客户内容/签名URL。早先qa-full.log为第一轮绿灯，最终以qa-full-final.log为准。
- 本轮无新API/Web/PG/浏览器，没有stop/start或进程认领；10:51只读3016/5176/55434/55445/55446均无监听。没有runtime，不能报告source_fresh。测试进程已结束，不创建用户入口、邀请或grant。
- Schema真实路径`packages/db/prisma/schema.prisma` SHA=`51B667F82598E64EE737AB7DB59933E38DA0B6946AE755FF8024B34A448412E7`，与BY46相同。一次hash命令误指根prisma路径失败，已用真实路径纠正，不作为迁移错误。无Schema变更，本轮未运行新PG迁移/PG集成；新增JSON查询通过事务fixture，尚无本轮真实PG证据。
- 无DOM/导航/页面路径变化，未重复1440/390Chrome；不声称浏览器/真实DNS/TLS/OSS/RAM/视频端到端通过。公开文档与依赖读取之外，业务外网/客户数据/Provider/费用均0，无部署/权限变更。
- 停止/回退：本轮无常驻服务可停；运行配置默认disabled且无execution/预算0。后续若接入，关闭driver不能代替删除云对象，须先用同配置显式sweep确认清理，失败保留记录。源码回退仅针对上表BY49新增文件和精确增量，使用证据/版本对照，不对整份脏文件git checkout/reset，也不删除Schema、历史lease或用户资料。

源码SHA256：

| 文件尾名 | SHA256 |
|---|---|
| beauty-video-oss-staging.ts | C858C6DF56D9179F6957FC158920EDC79354804CEA910998F775C306D1FFC788 |
| beauty-video-staging-config.ts | 6E1E73211927CE08B8F22921B0C019A378087E86BC6AF98E0A44EFD193E9ADF1 |
| beauty-video-material-integration.ts | 76EC812153599DCD782B6AC52C3CFEA1EAA31C12D39B1811157A4614D0839FA2 |
| viral-video-replication.ts | 117398DAD3A57EF33BAB3B9DBA53F7DC3B22CEC0AFEEA8A3D3F4701233A8E906 |
| beauty-video-oss-staging-smoke.ts | E4EB2AC58DAD6F495D107FF224F4A804539AE2F15D90EB7AB0F0715691B6F613 |

## 放行与下一唯一恢复点

本地BY49范围P0/P1=0；实际云存储/视频/用户路径未验收，产品仍不开放，既有暂停P1不因此关闭。云URL删除失败不能立即撤回，生命周期仅后备，已读取副本不能追回；限制已正式记录。

下一唯一建议：零Provider补“受控视频execution安装、单批服务端授权/成本与请求计数接线”，证据为正式route仍无execution且maxCostFen0；这不是让用户选SDK/搬文件或泛称缺云合同。需总调度接力，本卡完成后idle，不自行开启。真实存储/素材/视频测试再核对专用账号范围与独立预算，不沿用旧图文授权。XHS/经营问答/BY19/20/43/BY44外部保持PAUSED。
