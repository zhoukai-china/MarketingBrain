# BY-17 小红书受控验收输出与正式合同闭环

状态：已完成；QA-20260825-009 与 P1-A 已关闭（2026-08-26）

## 归属

- 产品：美业智能体（`productCode=beauty-industry`）
- 层级：产品任务含共享受控文本 Provider 夹具修复
- 风险：高（正式输出合同、AgentRun、积分账本与幂等）
- 预计修改热点：`apps/api/src/services/domestic-chat-provider.ts`、`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx`、新增 P1 专项脚本、`package.json` 与美业产品质量文档
- 是否允许并行：否；同一美业智能体只保留 BY-17 开发中

## 用户结果

用户在受控验收环境提交品牌中立的小红书图文需求时，确定性 mock 能通过与正式模型完全相同的结构、事实与账本门禁并保存/恢复，同时页面明确说明该结果只用于流程验收、不代表真实模型质量或客户最终内容。

## 本次范围

- 以脱敏请求“为夏季基础补水护理做一套面向附近女性顾客的小红书图文”和诊断指纹 `2132b435a0a829a1` 建立修复前红灯。
- 让 `controlled_mock` 补齐正式 `wechat-xhs-content-line@1.0.0` 要求的标题、正文、5–8 个标签、互动与承接、事实与合规待补，以及三套各自的正/负提示词、后期叠字和视觉参数。
- 受控输出继续经过 `beauty-workflow-output-v1`；缺字段、污染、错路由、fallback 或 Provider 失败继续在保存前失败关闭并释放积分。
- 保持 Web/WorkBuddy 共用执行、租户/权限、AgentRun、账本、幂等和历史恢复；验证同 requestId 重复提交只形成一条有效任务/结算。
- 页面明确标识确定性 mock 只验流程、费用为零且不代表真实模型质量或客户最终成品。

## 本次不做

- 不修改选题 P1-B、Get笔记同步 P1-C、视频/直播或其他业务能力。
- 不降低、删除、绕过正式 Skill、Schema、Eval、事实或污染门禁。
- 不调用图片、视频、ASR、媒体或其他 Provider；文本只允许使用用户随后单独批准的 `deepseek-v4-pro` 1 次/¥0.13 授权，且该授权现已耗尽。
- 不修改、停止或替换 3017，不部署生产。

## 验收条件

1. 正常路径：同一脱敏请求在 `controlled_mock` 下固定路由到 `beauty_xiaohongshu_package / acquisition:xhs / wechat-xhs-content-line@1.0.0`，一次生成完整合同输出并返回 200，可保存、刷新与历史恢复。
2. 失败路径：缺字段、标签数量错误、跨行业/品牌污染、无权限、跨租户、超时、取消与重复请求继续失败关闭；不合格结果不保存 AgentRun，预留积分释放。
3. 不应发生：自由文本切 Skill、mock 冒充真实模型质量、自动重试/换模、重复 Provider 调用/扣费、削弱合同或读取其他租户。
4. 可观测结果事件：固定 route receipt、Skill 版本、parser、脱敏 request fingerprint、AgentRun/账本终态和 Provider usage=0 可核验；不记录 Prompt、密钥或客户原文。

## 基线与失败证据

- 基线命令：`pnpm.cmd beauty-industry:xhs-controlled-contract-p1-smoke`
- 修复前失败测试/Eval：`node apps/api/node_modules/tsx/dist/cli.mjs scripts/beauty-industry-xhs-controlled-contract-p1-smoke.ts`（FAIL，2026-08-25）；脱敏指纹 `2132b435a0a829a1` 稳定命中 `missing_contract_terms:后期叠字,待补,互动与承接,事实与合规待补,负向提示词,视觉参数`，并出现 `rubric_fact_retention_weak`、`rubric_not_boss_usable`。该红灯发生在本地 `LLM_MOCK_MODE=true`，Provider 请求与费用均为 0。
- 现象、根因和连带影响：3017 对同一脱敏输入三次返回 502；路由、mock 阶段和账本释放正常，但受控夹具缺少 `后期叠字、待补、互动与承接、事实与合规待补、负向提示词、视觉参数` 等正式合同项。三次请求均发生在前次终态后，不是同一进行中窗口并发穿透。

## 实现记录

- 生产/页面源码归属：`apps/api/src/services/domestic-chat-provider.ts` 补齐受控小红书完整交付；`apps/api/src/products/beauty-industry/media-contract.ts` 让配图 parser 接受正式 `负向提示词` 及旧 `负向视觉提示词`；`apps/web/src/pages/BeautyIndustryAcquisitionPage.tsx` 明示 controlled mock 只验流程、零真实文本调用且不代表客户成品。
- 回归资产归属：新增 `scripts/beauty-industry-xhs-controlled-contract-p1-smoke.ts`、`scripts/verify-beauty-xhs-controlled-browser-e2e.mjs`；更新 `scripts/beauty-industry-controlled-mock-routing-smoke.ts`、`scripts/beauty-industry-workbuddy-mcp-smoke.ts` 和 `package.json`。文档归属为本任务卡、`STATUS.md`、`TEST_MATRIX.md`、`CONTRACTS.md`、`tasks/README.md`、`docs/BUG_REGRESSIONS.md` 的 BY-17/QA-20260825-001 段落。其余 BY-11 至 BY-16 和既有未提交改动均未回退、覆盖或整体提交。
- 数据/接口/配置变化：无数据库 Schema、公开 API、capability/scope/Skill 版本或真实 Provider 路径变化；受控夹具现在输出 3 标题、完整正文、6 标签、互动与承接、事实与合规待补，以及三套各自的正/负提示词、后期叠字和视觉参数。
- 重复提交：用户现场三次同指纹请求均发生在前次 502 终态后，不是进行中并发穿透；因此保留终态后显式重试。同步 `requestInFlightRef` 在 fetch 前锁定，服务端相同 requestId 复放返回同一 run；浏览器真实双击只发 1 个 POST，数据库只存在 1 个 AgentRun、1 个 settled reservation 和 1 条 8 积分 consume。
- 兼容性和回滚点：固定 `beauty_xiaohongshu_package / acquisition:xhs / wechat-xhs-content-line@1.0.0 + beauty-industry-xhs@1.1.0 + beauty-industry-compliance@1.0.0` 不变；3017/PID 8268 原样保留。回滚点为上述小范围 mock/parser/banner/回归增量。

## 验证

- 红绿灯：修复前专项 FAIL；修复后 `pnpm.cmd beauty-industry:xhs-controlled-contract-p1-smoke` PASS，指纹 `2132b435a0a829a1`、正式合同/缺字段/污染/parser/页面同步锁全部通过，Provider=0、费用 ¥0。
- 受影响专项：`beauty-industry:fixed-route-output-p1-smoke`、`beauty-industry:workflow-composition-smoke`（8 workflow × 高风险 3 次）、`beauty-industry:journey-p1-smoke`、`beauty-industry:web-contract-smoke`、受控 routing 全部 PASS。
- Web/WorkBuddy/账本：显式加载受控验收库后 `pnpm.cmd beauty-industry:mcp-database-smoke` PASS；小红书 MCP 正向输出保存并复放同 run，Provider 只进入一次受控夹具，数据库终态为 `succeeded / settled / 8 / AgentRun=1 / consume=1`；权限、跨租户、撤销、失败/取消/超时释放保持通过。
- 类型、全仓与构建：API typecheck、Web typecheck、`pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`（含根 build）全部 PASS。
- 页面/E2E：隔离 API `3018 / PID 9464` + Web 5176 的真实 Headless Chrome PASS：桌面、390px、直接深链、同按钮双击仅 1 个运行请求、200 保存、刷新恢复、跨租户、无横向溢出、console error=0、外部请求=0。浏览器技能扩展未连接（doctor：0 browsers），故按仓库既有 CDP E2E 完成，不冒充技能会话通过。
- 环境：3018 runtime 位于仓库外 `beauty-industry-by17-acceptance-20260825`，PID/runtime/session/绝对 TSX bootstrap/绝对入口/源码指纹 `4613D8E92382B0A0871B61B73FA0A526238E8306B1D05A08621990EEA7233BF9` 一致，`/ready ok=true/database=true`。首次 PID 24984 因 Windows marker 分隔符导致 freshness 回调 fail-closed，经监听/绝对命令连续核验后用正式停止函数清理并确认端口释放；3016/PID31024、3017/PID8268、5176/PID25232、55434/PID12564 全程未处理。
- 零费用阶段 Provider/费用：`domestic_provider_usage=0`、媒体 Provider=0、外部 Provider 请求=0，新增费用 ¥0；未部署生产。后续唯一真实文本终验见下节，不覆盖本条前置证据。
- 最终检查：`git diff --check` PASS；仅出现工作树既有 LF→CRLF 提示，无 whitespace error。新增 BY-17 文件另以 `git diff --no-index --check` 核对。

## 真实模型单次终验（已执行并失败关闭）

- 授权执行：用户批准 `deepseek-v4-pro` 1 次、最高 ¥0.13；实际严格 1 次，thinking disabled，自动重试/质量返工/换模/追加均为 0。输入仍只有脱敏句与全合成空档案租户，文件、图片、视频、ASR 和其他 Provider 调用均为 0。
- Provider 终态：`finish_reason=stop`，prompt/completion/reasoning/total tokens=`3525/1032/0/4557`，Provider 阶段 24.389 秒；按版本化保守单价估算 `¥0.019450`，低于 ¥0.13 硬上限。
- 业务终态：正式 Eval 命中 `rubric_fact_retention_weak`；由于事实保留硬门禁失败，没有生成 route receipt 或可保存结果，`fallbackUsed=false` 的成功证明、Web/WorkBuddy 结果一致性和 succeeded AgentRun 均未达成。合同没有降低或绕过，也没有使用 mock/模板补齐。
- 账本与清理：AgentRun=0；唯一 reservation 为 `released/actualAmount=0/errorCode=runtime_failed`，账本记录一条 8 积分预留 consume 与一条同额 refund，净积分 0。临时 WorkBuddy 凭据、产品 entitlement、Agent entitlement 已撤销，余额归零。
- 环境：真实隔离 API `3019/PID17104`、源码指纹 `4613D8E92382B0A0871B61B73FA0A526238E8306B1D05A08621990EEA7233BF9`，调用后已由双重身份检查的停止脚本关闭并确认端口释放。3018/PID9464 controlled mock 全程未处理且 `/ready database=true`。
- 安全证据：仓库外 `F:\思潼AI增长os\test-environments\beauty-industry-by17-live-20260825\runtime\verification.json` 只记录模型、token、费用、合同规则、账本与环境指纹；未记录 Prompt、模型正文、完整 Provider 原始响应、密钥或客户数据。
- 调用后回归：live postflight、XHS 正式合同、预算、fixed-route、Web contract、8 workflow × 高风险 3 次、`qa:fast`、`qa:regression` 全部在 mock/离线模式 PASS；3019 停止后没有更多 Provider 请求。`qa:full` 未在失败终验后重复运行，因为没有产品实现变更且成功门禁已失败；零费用阶段的既有 `qa:full`/build PASS 仍保留。

## 交接

- 验收链接：`http://127.0.0.1:5176/agents/beauty-industry/acquisition/xhs?apiBase=http%3A%2F%2F127.0.0.1%3A3018`；复用现有 beauty-industry 登录会话。停止 3018 只能执行仓库外 `F:\思潼AI增长os\test-environments\beauty-industry-by17-acceptance-20260825\stop-isolated.ps1`，脚本任一身份不一致即 fail-closed。
- 用户验收（不超过 5 步）：1）打开链接并确认 controlled mock 提示；2）输入脱敏句并快速双击生成；3）核对完整标题/正文/6 标签/互动与事实待补/三套配图字段且只出现一条结果；4）刷新并从历史恢复；5）切 390px 检查无横向溢出。
- 残余 P0/P1：受控验收、合同、页面、租户、权限、账本与幂等范围 P0=0/P1=0；真实模型事实保留 P1=1。本次授权已用尽，不能重试。
- 后续任务：到此停止，等待总调度另行安排 P1-A 根因红灯和零费用修复；P1-B、P1-C 与美业 AI 日报继续排队，不得启动。
- 最后更新日期：2026-08-25

## QA-20260825-009 零费用根因修复（2026-08-25）

- 任务卡处理：不新建 BY-18。本缺陷仍是 BY-17 的同一 P1-A 成功门禁，继续保持 BY-17 打开；真实模型成功前不得把 P1-A 标为关闭。
- 修复前红灯：`pnpm.cmd beauty-industry:xhs-fact-retention-p1-smoke` 首跑 FAIL，稳定记录六项差距：输入没有机器可核验事实清单；prompt/正式 Schema 没有“任务事实回执”；结构完整但事实为空仍能通过产品合同；合理同义表达被通用字面 rubric 误拒；事实矛盾仍能通过产品合同。指纹固定为 `2132b435a0a829a1`，Provider=0、费用 ¥0。
- 根因证据：Web `question: effectiveQuestion` 与 WorkBuddy `input: question` 都完整进入共享 execution，`buildBeautyIndustryRunInput` 也保留用户原句，因此页面、请求合同和归一化没有丢这四项事实。正式组合此前只提供自然语言任务，`wechat-xhs-content-line@1.0.0` 又没有事实回执；通用 rubric 以抽取词元做 `answer.includes`，既不能解释季节/项目/地域人群的逐项保留，也会误拒“夏天/周边女性客群/日常补水护理”等合理表达。三套视觉结构是否造成模型注意力挤压没有 Provider 正文证据，子根因保持未知，不归因模型能力。
- 最小修复：升级主 Skill 为 `wechat-xhs-content-line@1.0.1`；服务端从本轮请求/专业字段提取时点、服务项目、地理范围、目标顾客、平台和交付形式，作为锁定事实清单进入同一个 Web/WorkBuddy 后端输入。正式 Schema 新增“任务事实回执”，prompt 要求标题、正文、标签和相关配图继续使用同一事实；Agent rubric 与产品 postflight 都做逐项、可解释校验，支持季节/地域/人群和不改变项目核心的合理同义表达，漏项与矛盾分别失败关闭。没有重试、第二次修复调用、换模、模板拼接或门禁降级。
- Prompt 预算：XHS 组合只把执行必需的合同字段注入 runtime，完整正式合同仍由 postflight 加载；含事实清单的组合输入实测 `23,434 / 25,000 bytes`，输出仍锁定 `max_tokens=5,120`，没有突破既有预算或扩大模型范围。
- 绿灯覆盖：完整保留成功；漏季节/项目/地理/人群任一失败；事实矛盾失败；餐饮污染失败；结构缺失失败；“夏季→夏天、附近→周边、基础补水护理→日常补水护理”通过；另一条“秋季舒缓护理/周边上班族”证明抽取不只适配本句。controlled mock 根据锁定事实动态输出，不再把本次句子硬编码成所有 XHS 请求的事实。
- Web/WorkBuddy、账本与幂等：两入口仍共用 `executeBeautyIndustryWorkflow`；MCP 隔离库专项 PASS，覆盖 scope、租户、权限、撤销、失败/取消/超时释放、同 requestId 复放和单次结算。第一次数据库专项使用仓库默认账号被 PostgreSQL 鉴权拒绝，未建立测试身份或写入；随后只加载既有 `.env.acceptance`，确认 `127.0.0.1:55434/beauty_industry_acceptance_20260821` 后 PASS 并由脚本清理测试租户。
- 全量门禁：XHS 两专项、text budget、workflow composition、fixed-route、journey、Web contract、MCP platform/database、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含 build）与 `git diff --check` PASS。真实文本/图片/视频/ASR/媒体 Provider=0，新增费用 ¥0，未部署生产。
- 精确文件归属：`apps/api/src/products/beauty-industry/{execution,profile,workflows,output-contract,xhs-task-facts}.ts`、`apps/api/src/services/domestic-chat-provider.ts`、`packages/agent/src/index.ts`、`packages/skills/src/index.ts`、`packages/skills/skills/wechat-xhs-content-line/{prompt.md,contract.json}`、`mcp-skills/skills/wechat-xhs-content-line/SKILL.md`、`scripts/beauty-industry-{xhs-fact-retention-p1-smoke.ts,xhs-controlled-contract-p1-smoke.ts,fixed-route-output-p1-smoke.ts,by09-message-profile.ts}`、`package.json` 及本任务列明的美业/回归文档。工作树其余 BY-11 至 BY-16、平台、FIP、兰琪和既有未提交文件均未回退、覆盖或整体提交。
- 残余 P0/P1：P0=0；代码、离线合同、mock、Web/WorkBuddy、账本/幂等范围 P1=0；P1-A 真实一次调用成功率仍需新的单次真实证据，故产品级残余 P1=1。用户本轮要求修复后休息，本次不请求、不调用新 Provider。
- 未来恢复计划（非当前授权请求）：只有用户未来另行批准后，使用 `deepseek-v4-pro` 1 次、thinking disabled、输入绝对上限 25,000 tokens 且 `maxPromptBytes=25,000`、输出 `max_tokens=5,120`、自动重试/修复/换模/追加=0；预计约 ¥0.02、人民币最坏 ¥0.13。成功须 `fallbackUsed=false`、`wechat-xhs-content-line@1.0.1` 全部结构/事实/污染 Eval 通过、唯一 succeeded AgentRun 和一次结算、Web/WorkBuddy 一致；成功、失败或终态不明均立即停止。
- 下一次恢复点：先核对本节文件归属和全量门禁证据，再由总调度决定是否申请上述单次真实验收；在 P1-A 关闭前不得启动 P1-B、P1-C 或美业 AI 日报。

## 关机安全停点（2026-08-25）

- 3017：停止前 listener、`api.pid`、`api.runtime.json` 与 `by16.session.json` 均为 PID `8268`；仓库根为当前绝对路径，runtime/session 源码指纹同为 `67D89511E42B00E273C9B18D8D2994CB2C9E9C84945A74B7236030A49F83A7AA`。双重身份门禁通过后执行仓库外 `beauty-industry-by16-acceptance-20260825-v2/stop-isolated.ps1`，端口已释放，`by16.stopped.json` 已记录。
- 3018：停止前 listener、`api.pid`、`api.runtime.json` 与 `by17.session.json` 均为 PID `9464`；仓库根为当前绝对路径，runtime/session 源码指纹同为 `4613D8E92382B0A0871B61B73FA0A526238E8306B1D05A08621990EEA7233BF9`。双重身份门禁通过后执行仓库外 `beauty-industry-by17-acceptance-20260825/stop-isolated.ps1`，端口已释放，`by17.stopped.json` 已记录。
- 保持原样：3016/PID31024 的 runtime PID 已知不一致，禁止停止、接管或覆盖；PostgreSQL55434/PID12564 与 Web5176/PID25232 未取得本停点所需的完整正式停止归属，因此保持在线，不清库、不删持久数据。两个仍在的 esbuild helper 分别以 Web25232 和 API31024 为父进程，不是本轮测试残留。
- 工作会话：本轮所有专项、typecheck、`qa:fast`、`qa:regression`、`qa:full`/build 已结束；没有本轮启动的浏览器或临时命令残留。最终 Provider/媒体调用 0、费用 ¥0、未部署生产。用户可以安全执行操作系统关机；本任务未执行 Windows 关机。
- 恢复时：不得把已停止的 3017/3018 当作在线验收入口；先从本任务 QA-20260825-009 文件归属、红绿灯与全量门禁恢复，再由总调度决定是否申请新的单次真实证据。P1-A 未关闭，P0=0、残余 P1=1；不得越过它启动 P1-B、P1-C 或美业 AI 日报。

## 2026-08-26 修复后唯一真实验收

- 授权与预检：用户重新批准 `deepseek-v4-pro` 1 次/最高 ¥0.13。新建仓库外 `beauty-industry-by17-live-20260826`，使用独立 3020/PID4068、绝对 TSX bootstrap、源码指纹 `F54EDA85FDDEBDADE9B48EE9F2CDB5F5FF86098A8C30FC6DBF7E51E526FAD4D2`；数据库为正式验收 `beauty_industry_acceptance_20260821@55434/PID2992`。调用前 prompt `23,434/25,000 bytes`、输出上限 5,120、预算最坏 ¥0.122635≤¥0.13，Provider start/usage=0。
- 调用证据：严格 1 次 `deepseek-v4-pro`，thinking disabled、重试/修复/换模/追加=0；`finish_reason=stop`，prompt/completion/reasoning/total=`3241/1039/0/4280`，Provider 23.146 秒，保守估算 ¥0.018510。只使用登记脱敏句与全合成空档案租户；文件、媒体、图片、视频、ASR 和其他 Provider=0。
- 成功门禁：固定 capability/scope/Skill 三段链；正式栏目、任务事实回执、事实保留与矛盾、污染、标题/标签/三套配图全部 PASS；`fallbackUsed=false`、`providerOutputVerified=true`。Web 历史恢复成功且与 WorkBuddy 持久化结果哈希一致；验证记录只保留 `outputHash=5897fe4de76d4afa`，不保存 Prompt、模型正文或原始响应。
- 账本与清理：唯一 AgentRun=`succeeded`；唯一 reservation=`settled/actualAmount=8`；1 consume、0 refund、无重复结算。临时 WorkBuddy 凭据、产品/Agent entitlement 已撤销，测试余额 0。
- 验收脚本假阴性：第一次本地 postflight 将方法论描述和整句 deliverable 当作客户正文必须逐字出现，发生保存后的假阴性；Provider 计数始终为 1。修复只对齐现有产品 postflight：通用 Eval 保留全部正式栏目和字面术语，描述性交付继续由确定性的标题数、标签数、配图字段和任务事实校验；对同一已持久化输出的零 Provider 审计 PASS，没有模板补齐或合同降级。
- 调用后回归：live offline audit/postflight、XHS controlled contract、事实保留、fixed-route、MCP 隔离数据库（scope/租户/权限/账本/幂等/撤销/失败释放）、完整 `beauty-industry:web-contract-smoke` PASS；昨晚 `qa:full`/build PASS 按批准规则复用，因为本轮没有产品实现变化。`git diff --check` 见最终停点。
- 文件归属：本次只更新 `scripts/beauty-industry-by17-xhs-live-{acceptance,postflight,offline-audit}.mjs` 与本卡、STATUS、TEST_MATRIX、tasks README、BUG_REGRESSIONS 的 BY-17/QA-009 段落；其余 BY-11 至 BY-16 及其他未提交改动均保护不动。
- 结论：QA-20260825-009/P1-A 已关闭，P0/P1=0。本轮授权耗尽；到此停止，不自行启动 P1-B、P1-C 或美业 AI 日报。
- 最终环境停点：3020/PID4068 的 listener、PID 文件、runtime/session 与调用时源码指纹 `F54EDA85…` 全部一致后，正式 `stop-live-isolated.ps1` 于 `2026-08-26T08:01:21+08:00` 成功停止，端口释放并写入 `by17-live.stopped.json`；没有浏览器或临时测试进程残留。共享 PostgreSQL55434/PID2992 保持在线，未处理任何未知或其他环境进程。
