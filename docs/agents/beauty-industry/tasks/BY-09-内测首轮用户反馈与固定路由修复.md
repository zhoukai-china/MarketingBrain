# BY-09 内测首轮用户反馈与固定路由修复

状态：已完成（页面、生产子路径、调用前费用和销售真实输出 P0/P1 均已关闭，可邀请用户验收）

## 归属

- 产品：美业智能体（内部 `beauty-industry`）
- 层级：产品专用
- 风险：高
- 预计修改热点：美业登录与经营工作台页面、美业 WorkBuddy 展示、美业固定工作流适配、产品专项回归、产品文档；不修改外卖或创始人 IP 生产逻辑
- 是否允许并行：否

## 用户结果

邀请制内测用户在公网美业智能体中完成登录、经营档案和五条固定获客任务，不再看到内部术语、客户品牌残留或错误路由；失败可恢复且不误扣费。

## 本次范围

- 产品统一命名为“美业智能体”，首页标题为“美业经营工作台”。
- 登录页删除行业输入，门店/品牌名称使用行业通用占位；经营档案扩展细分赛道与自定义项，渠道改为结构化多选。
- 删除用户可见的“品牌中立”、内部路由说明、开发说明和重复推荐区。
- WorkBuddy 页面只展示用户功能名称与结果说明，内部 scope 仅用于权限校验。
- 复现并修复公网“美业获客服务暂时不可用”；固定验证美业选题、内容十件套、视频数据复盘、直播话术、直播复盘。
- 核对并复用创始人 IP 已验收的四维选题契约，只在美业产品层叠加行业约束。
- 增加 `/beauty-beta/` 子路径回归，完成零费用页面、MCP 和生产回归后受控发布。

## 本次不做

- 不开放视频内容复盘、文生视频或图生视频。
- 不做支付、充值或凭据轮换；用户已单独明确授权撤销截图中泄露的旧凭据，本任务仅执行这一项权限变更。
- 开发期单次真实文本模型测试批次统一以人民币 ¥1 为硬上限；每个工具每批最多一次、调用前必须证明最坏费用不越界，禁止自动重试、并发重复和模型切换。额外重试批次、超过 ¥1、图片/视频/ASR 等付费媒体仍需重新授权。本规则仅是费用边界，不改变产品知识、租户或品牌隔离。
- 不修改外卖、创始人 IP 的产品行为，不覆盖 `/os-v2/`。

## 验收条件

1. 正常路径：桌面与 390px 可完成美业产品登录、经营档案保存/修改/刷新、三大获客分支、五条固定任务、历史恢复与 WorkBuddy 页面；公网子路径与 API base 正确。
2. 失败路径：模型或 Skill 不可用、余额不足、网络失败、取消、重复点击均显示明确终态；失败不扣费、不用模板冒充模型结果。
3. 不应发生：不串任务、不泄露客户品牌/内部 scope/内部路由说明；不开放关闭能力；不产生 Provider 费用或真实支付订单；不影响其他产品。
4. 可观测结果事件：固定 capability/Skill 选择、运行状态、积分预留/结算/释放、幂等键、公网 API 请求路径和失败分类均可脱敏核对。

## 基线与失败证据

- 基线命令：生产 `health` / `ready` 与 `/os-v2/health` 均 PASS；用户失败窗口数据库余额 300、reservation/transaction/AgentRun 均 0。
- 修复前失败测试/Eval：`node scripts/beauty-industry-beta-self-mcp-config-smoke.mjs /etc/systemd/system/beauty-industry-beta.service` 稳定 AssertionError，缺少 beta 自身 MCP 地址；生产 6 个 MCP 生成工具记录 `original_skill_not_found`。
- 现象、根因和连带影响：独立 beta 从共享 env 继承 `127.0.0.1:3002/mcp`，正式美业 Skill 包被错误地从另一发布目录加载。错误发生在积分预留和 Provider 之前，因此没有扣费或模型调用；视频数据复盘的未解析失败不是该 P0。

## 实现记录

- 修改文件：美业经营工作台、登录页、WorkBuddy 展示、经营档案契约、Provider 失败分类、beta systemd、产品登录文案及对应专项回归；未修改外卖/FIP 产品行为。
- 数据/接口/配置变化：无 Schema；beta self-MCP 固定到 3004，文本主超时调整为 180 秒；旧泄露凭据状态改为 revoked 并新增撤销审计，不物理删除。
- UI：产品统一为“美业智能体/美业经营工作台”；删除客户示例、品牌中立和内部路由说明；赛道支持扩展与自定义，渠道改为结构化多选，WorkBuddy 只显示用户功能名。
- 公网子路径：新增生产内容回归并修复 beta 构建只剥离 `/os-v2` 的问题；`/beauty-beta/` 现在按 Vite base 正确路由。生产备份为 `/opt/beauty-backups/by09-basepath-20260824-103751`、`/opt/beauty-backups/by09-product-copy-20260824-104055`。
- 调用前预算：新增版本化 `beauty-text-budget-v1`，固定 `deepseek-v4-pro`、关闭无界 thinking，并按六个 capability 分别限制输入 UTF-8 字节上界和 `max_tokens`；Web 与 self-MCP 在 Provider `started` 之前共用同一预检。六工具批次最坏费用为 ¥0.999456，超限时 Provider 调用、usage 与积分扣费均为 0。
- 销售契约：真实终验发现生活美容顾客效果咨询缺少“当前判断/下一步动作”且可能落入企业项目 fallback；新增修复前红灯后，按当前用户事实识别 B2C 美业效果咨询，完整隔离企业预算、决策人和实施话术，保留疗效、价格、预约和顾客事实边界。零费用回归已通过并部署到独立 beta，回滚备份为 `/opt/beauty-backups/by09-sales-fallback-20260824-120606`。
- 兼容性和回滚点：所有生产修改只落在独立 `/opt/beauty-industry-beta`；既有 `/os-v2/` 未覆盖，发布后健康状态保持 200。

## 验证

- 领域命令：配置回归修复前 FAIL、修复后 PASS；生产 9 个 Skill 包加载 9/9；临时 0 积分 MCP/Web 固定路由 PASS，撤销后 401/401/401。
- 修复前费用红灯：历史选题真实 usage 为 prompt/completion/reasoning 5601/9192/7548，证明仅靠经验报价无法在调用前满足硬上限；专项先稳定 FAIL，再验证六项预算预检、错误模型拒绝、超长请求拒绝和单工具一批一次均发生在 Provider 之前。
- 本批六工具真实终验：只用 `deepseek-v4-pro`，选题、内容十件套、小红书文本包、直播话术、直播复盘、美业销售各 1 次，全部 `finish_reason=stop`、reasoning token=0、账本 settled、无自动重试。按安全 usage 聚合估算分别为 ¥0.030861、¥0.053411、¥0.016168、¥0.060632、¥0.021308、¥0.014508，累计 ¥0.196888≤¥1；临时凭据和 entitlement 已撤销、余额归零。
- 真实业务质量：首批前五项满足各自结构与安全硬门禁；美业销售首次虽 Provider/账本终态成功但缺少“当前判断/下一步动作”，没有用平均通过放行。确定性红灯转绿并部署后，按新授权只复验销售 1 次：`deepseek-v4-pro`、16.707 秒、prompt/completion/reasoning=2859/468/0、`finish_reason=stop`、估算 ¥0.013207≤¥0.12、预留结算 12 积分。结果包含全部销售契约字段、未确认事实和合规边界，不含团购/核销流程或企业采购预算、决策人话术。
- 观测脚本缺陷：旧版日志窗口先后暴露毫秒 ISO 不兼容和 journald 字段使用错误；修复为读取 `__REALTIME_TIMESTAMP`，专项 fixture PASS。该问题只影响测试汇总，不影响六次运行或账本，且修复过程中没有额外 Provider 调用。
- 超时安全：修复前普通 `Error` 超时被错误记为 `unknown`；新增专项先 FAIL，再把 timeout/cancel 归类为稳定终态，并把固定美业选题的主超时与 beta 环境统一为 180 秒。该修复已部署，但授权的单次真实复验已经用完，未再次调用。
- 安全收口：复验临时凭据 1/1 撤销，临时 product/Agent entitlement 停用、余额归零；预留 10 全额释放，实际结算 0，消费/退款各一条且净额 0；无 AgentRun、无重复扣费。
- 页面/E2E：本地受控 mock 环境桌面与 390px 覆盖登录、三分支、刷新恢复、断网、取消晚到、WorkBuddy 专属页，控制台与非预期请求 0；生产登录页桌面与 390px 浏览器均 PASS。
- 公网子路径红灯→绿灯：生产初测 HTTP 200 但实际渲染外卖产品；`beauty-industry-production-web-content-smoke.mjs` 修复前 FAIL，base-path 修复和 beta 重建后 PASS，并核对当前三个用户流程 chunk。
- 质量门禁：销售修复后 `beauty-skill-output-quality-p1-smoke`、`beauty-industry:web-contract-smoke`、`qa:fast`、`qa:regression`、`qa:full`、全仓 build、`git diff --check` 全部 PASS；`fip:self-mcp-timeout-smoke` PASS，未污染 FIP。
- 页面/E2E：本地 Chrome 桌面与 390px 完整路径再次 PASS，控制台与非预期请求 0；生产邀请码页桌面/390px HTTP 200、无横向溢出、无“枕水江南/品牌中立/美业行业智能体”残留。应用内浏览器运行时初始化失败，未用该工具冒充通过；页面证据来自仓库 Playwright 与生产 Chrome 无头验收。
- 未运行项：没有再次调用图片、视频或 ASR；没有充值、发布或投流。WorkBuddy 原生桌面自动写入 secret 未执行，产品 MCP 协议、`tools/list/tools/call`、产品专属连接页和凭据撤销已真实验证，不把桌面自动配置冒充为完成。

## 交接

- 残余风险：截图中旧 secret 已撤销；用户验收时需自行生成新的 WorkBuddy 密钥。产品仍是邀请制内测，正式支付、视频内容复盘、文生视频、图生视频、投流、发布和充值继续关闭。
- 恢复点：BY-09 已完成；用户可从生产邀请制入口重新验收经营档案、三分支、销售和 WorkBuddy 连接。
- 后续任务：无；本任务达线后回到用户业务验收。
- 最后更新日期：2026-08-24
