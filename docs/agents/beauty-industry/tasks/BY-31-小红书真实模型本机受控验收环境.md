# BY-31 小红书真实模型本机受控验收环境

状态：完成（真实文本闭环与本机用户入口已放行）

## 用户结果

在本机隔离验收入口中，小红书图文固定使用 `deepseek-v4-pro` 生成真实文案；图片固定使用 `aliyun_bailian/wan2.7-image`，且只在用户于页面确认积分后创建，最多三张。页面不把 controlled mock 或模板包装成真实结果，刷新后可恢复同租户历史，跨租户不可读取。

## 范围与文件归属

- capability/scope/图片链不变，固定为 `beauty_xiaohongshu_package` / `acquisition:xhs` / `beauty-xhs-image-plan-v2` / `beauty-image-provider-prompt-v1.2` / `beauty-image-safety-v2.6`。真实 smoke 暴露无依据第一人称体验后，文字主 Skill 由 `wechat-xhs-content-line@1.0.2` 最小升级到 `1.0.3`，美业差异与合规约束版本不变。
- 受控环境使用仓库外 `F:\思潼AI增长os\test-environments\beauty-industry-by26-v23-final-live-20260827`；密钥只由 `apps/api/.env` 读取并传给受控启动进程，不复制到仓库、runtime、日志或任务卡。
- 本任务精确文件归属：`scripts/beauty-industry-by17-xhs-live-acceptance.mjs`、`scripts/beauty-industry-by17-xhs-live-offline-audit.mjs`、`scripts/beauty-industry-xhs-customer-delivery-p1-smoke.ts`，本任务卡、`STATUS.md`、`TEST_MATRIX.md`、`BUG_REGRESSIONS.md` 与`tasks/README.md`的BY-31交接段，以及仓库外受控verification JSON。现有BY-11至BY-30及其他未提交改动不回退、不覆盖、不整体提交。

## 不做项

- 不预先替用户生成新图片，不创建第四张，不自动重试、补图、换模或调用其他 Provider。
- 不恢复 PAUSED 的 BY-19/BY-20，不部署生产，不修改 DNS、产品权限、正式 Skill 映射或生产配置。
- 不把邀请码、密钥、Prompt、模型正文、完整 Provider 响应或客户资料写入仓库/日志。

## 修复前红灯（2026-08-28）

- 电脑重启后 PostgreSQL 55434、API 3016、Web 5176 均无监听；`/ready` 与 Web 不可访问，用户没有可测试入口。
- BY-26 验收根目录 source/runtime 指纹均为 `753AC360…`，但 API 离线导致 `source_fresh=false`；不存在 `.env.text-approval` 或 `.env.media-approval`，默认合同为 `text=controlled_mock`、`media=disabled/max0`。
- 因此真实 `deepseek-v4-pro` 文案与“用户确认后生成 wan2.7-image”在 Provider/积分前 fail-closed；此红灯来自受控 `status.ps1`，不是路由、Skill 或页面模板缺失。

## 验收条件

1. 受控启动后 source/runtime/session/listener/CWD/绝对 bootstrap 一致，`source_fresh=true`，`/ready ok=true/database=true`，文本模型只能是 `deepseek-v4-pro`。
2. 一次脱敏、全合成空档案 smoke 恰好调用一次 DeepSeek，无 fallback、无媒体调用；正式 Schema/Eval、事实与污染门禁、唯一 AgentRun、一次积分结算、Web/WorkBuddy 一致全部通过，费用不超过 ¥0.13。
3. 小红书页面不显示 controlled mock/非正式生成；真实文案结果按客户成品、制作说明和折叠审核分层。图片按钮只有报价、授权、存储、租户和预算均通过时可用，且必须由用户主动确认，不在本任务代点。
4. 已有三张 BY-26 合格资产保持可恢复/下载；owner 可读、跨租户 404，刷新与重复操作不新增 Provider 或账本记录。
5. 桌面与 390px、登录、刷新、重复点击、console、账本、Provider 观测及全量质量门禁通过；新邀请码限定 beauty-industry、24 小时、最多一次，明文仅最终反馈一次。

## 失败关闭

- 凭据、模型、报价、源码身份、数据库、预算或 Provider 终态任一不明确，必须在对应外部动作前停止。
- 文案 smoke 成功、失败或终态不明后都不追加文本调用；图片未由用户明确点击确认时 Provider 图片调用必须为 0。
- 图片真实任务每次完整图文任务 Provider 总成本硬上限 ¥1，最多三张；任一失败不自动重试或补图。

## 当前进度

- 受控环境曾以当前源码正式启动，source/runtime=`F901D402…`、fresh/ready/database=true，文本仅 `deepseek-v4-pro`，媒体 real/max3；密钥只进进程，未写 runtime 或文档。
- 唯一一次真实文案 smoke：Provider 恰好1次、finish reason=`stop`、fallback=false，prompt/completion/reasoning/total tokens=`3560/1098/0/4658`，Provider耗时约25.7秒，保守费用¥0.020031。正式保存前以 `beauty_workflow_output_contract_failed:forbidden_我做过` 失败关闭；AgentRun=0，8积分一次预留一次等额释放，净0；图片/视频/ASR Provider=0。
- 根因不是模型连通性或路由，而是 `1.0.2` 没有明确禁止在任何输出层写入无输入事实支持的第一人称体验。已建立修复前红灯并升级 `wechat-xhs-content-line@1.0.3`：禁止“我做过/亲测/做完后”等无事实体验，要求无可核验经历时改用门店中性说明，禁词不能转移到制作/审核层；不降低正式输出合同。
- 新规则保持既有 `maxPromptBytes=25000`，最终真实组合为24997字节；运行资产manifest与MCP正式副本版本/哈希一致，WorkBuddy候选引用0。受控源码指纹现包含API、Web及正式Skill资产，避免Web改动仍被误报fresh。
- 离线专项、Web/API/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）、diff-check全部PASS；真实模式下Chrome桌面1440/390px确认“真实文案已启用（DeepSeek V4 Pro）”、无controlled mock banner、无横向溢出、console0、Provider调用0。
- 修复后按新增精确授权再次调用 `deepseek-v4-pro` 恰好1次：thinking disabled、finish=`stop`、fallback=false、usage event=1、媒体/文件上传/其他Provider=0，保守费用¥0.019404≤¥0.13。执行当时的安全摘要没有持久化精确prompt/completion拆分，因此不猜测该拆分；runner现已补齐后续安全摘要的token字段，避免同类审计缺口。
- API正式链实际返回成功并持久化唯一`succeeded` AgentRun。`wechat-xhs-content-line@1.0.3 + beauty-industry-xhs@1.1.0 + beauty-industry-compliance@1.0.0`的Schema/Eval、任务事实回执、事实矛盾、跨行业污染和第一人称体验门禁全部PASS；8积分一次预留、一次结算、释放0，最终合成验收账户按postflight归零。
- 初版live runner曾把MCP面向客户的`copyMarkdown`当成完整workflow输出，再要求其中包含制作说明与审核回执，产生`missing_contract_terms`假阴性。产品合同没有失败：MCP结构化字段与持久化AgentRun均包含`customerDeliverable / productionNotes / auditReceipt`。runner已改为客户复制稿只验客户层、完整合同从唯一持久化run验三层，并以同一真实结果离线复核PASS；输出hash=`196f926ff0aa5ede`，Web历史恢复与WorkBuddy结构化输出一致。
- 修复后专项、API/Web/Agent typecheck、`qa:fast`、`qa:regression`、`qa:full`（含build）与diff-check全部PASS。真实模式Chrome E2E未点击生成按钮，桌面1440/移动390、双租户、刷新/历史、real banner、横向溢出0、console0且Provider调用0。
- 已创建beauty-industry专属邀请记录`cmtc8zg2f0000115izd4ggljw`，24小时、最多1次、当前usedCount=0，过期时间`2026-08-29 09:03:02 Asia/Shanghai`；仓库和日志只记ID/preview，不保存明文。
- 最终安全停点：PostgreSQL `55434/PID14660`、API `3016/PID18748`、Web `5176/PID14208`；source/runtime=`F901D402…`、`source_fresh=true`、`ready/database=true`、Web=200，API固定configured provider。正式stop/start脚本可按同一AcceptanceRoot双重身份门禁停止；3016/5176/55434以外进程未处理。

## 最终交接

- 本次真实文本授权已消费并停止，不得自动重试、修复、换模或追加调用；用户后续每次点击生成属于其主动验收动作，仍受页面预算、积分和幂等合同约束。
- 本机登录入口：`http://127.0.0.1:5176/login/beauty-industry?apiBase=http%3A%2F%2F127.0.0.1%3A3016`；登录后XHS直达：`http://127.0.0.1:5176/agents/beauty-industry/acquisition/xhs?apiBase=http%3A%2F%2F127.0.0.1%3A3016`。
- 邀请码明文只在本任务最终反馈中显示一次；不写仓库、日志或跨任务消息。环境保持在线供最终用户验收，不部署生产；BY-19/BY-20继续PAUSED。
- BY-31范围内残余P0/P1=0，QA-20260828-001关闭。下一任务由总调度另行安排，本任务不自行启动。

## 实际验证命令与结果

- `node scripts/beauty-industry-by17-xhs-live-acceptance.mjs`：真实授权窗口内执行1次；API成功，原runner假阴性暴露后不追加Provider调用。
- `node scripts/beauty-industry-by17-xhs-live-offline-audit.mjs`：PASS；对唯一持久化真实run完成三层合同、Schema/Eval、事实/污染、Web/WorkBuddy与历史恢复复核。
- `node scripts/beauty-industry-by17-xhs-live-postflight.mjs`：PASS；唯一succeeded AgentRun、一次积分结算、临时凭据/entitlement撤销与余额归零。
- `pnpm.cmd beauty-industry:xhs-controlled-contract-p1-smoke`、`pnpm.cmd beauty-industry:xhs-fact-retention-p1-smoke`、`pnpm.cmd beauty-industry:xhs-customer-delivery-p1-smoke`、`pnpm.cmd beauty-industry:xhs-same-page-image-p1-smoke`：PASS，Provider0。
- `pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`：PASS，`qa:full`含API/Web/Agent typecheck与全仓build。
- `node scripts/beauty-industry-navigation-browser-e2e.mjs`（real XHS只读门禁）：PASS；1440/390、双租户、刷新/历史、console0、Provider0。
- `node --check`两份BY-17验收脚本、verification JSON gate、`git diff --check`：PASS；仅有既有CRLF提示，无空白错误。
