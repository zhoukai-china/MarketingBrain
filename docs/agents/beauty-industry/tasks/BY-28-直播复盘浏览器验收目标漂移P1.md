# BY-28 直播复盘 Web/WorkBuddy 验收目标漂移 P1

状态：已完成（2026-08-27）

## 归属

- 产品：美业智能体
- 层级：产品专用质量门禁
- 缺陷：QA-20260827-002
- 风险：中高（已开放核心路径无法由默认验收命令可靠复核）
- 预计修改热点：直播复盘浏览器 E2E、直播复盘零 Provider 专项、本产品质量文档
- 是否允许并行：否；BY-19/BY-20 继续 PAUSED，BY-26 真实三图不并行

## 用户结果

当前受控美业环境中，直播复盘的默认浏览器验收命令直接指向正式记录的 API，并在桌面与 390px 真实完成数据解析、八模块结果、刷新、返回和跨租户验证；验收脚本不能因遗留端口误报产品失败或绕开当前运行时。

## 本次范围

- 将直播复盘 E2E 的默认 API 目标与仓库正式美业验收端口统一，同时保留显式环境变量覆盖。
- 在创建合成租户前检查目标 `/ready` 与数据库状态，失败时报告安全 endpoint/stage，不进入浏览器或业务写入。
- 把默认目标加入直播复盘零 Provider静态门禁，防止再次回退到已停止的隔离端口。
- 运行真实 API+隔离数据库+Chrome 桌面/390px、刷新/返回、跨租户、console 和 external Provider 0。

## 本次不做

- 不修改 `live_review_workflow_v1`、Skill、Schema/Eval、权限、租户、账本或产品页面。
- 不调用任何真实 Provider，不访问外网，不产生费用，不部署生产。
- 不恢复 BY-19/BY-20，不执行或申请真实三图终验。

## 验收条件

1. 正常路径：不设置 E2E API 覆盖时，默认命令在当前 3016 受控环境完整 PASS，连续三次稳定。
2. 失败路径：目标 API 不可用或数据库未就绪时，在创建租户前以明确 preflight 错误停止，不伪报页面或 workflow 失败。
3. 不应发生：不得默认访问旧 3017，不得调用外部 Provider，不得创建跨租户可见结果或重复账本记录。
4. 可观测结果：输出 API preflight、桌面/390px、数据解析、八模块、刷新、返回、租户隔离、console 与外部请求结果。

## 基线与失败证据

- 当前正式环境：PostgreSQL 55434、API 3016、Web 5176，`source_fresh=true`、text=controlled_mock、media=disabled。
- 修复前默认命令 `pnpm.cmd beauty-industry:live-review-browser-e2e` 连续 3 次均在 `createTenant` 前 `fetch failed`；同一源码显式设置 `BEAUTY_E2E_API_URL=http://127.0.0.1:3016` 后连续 3 次全路径 PASS。
- 根因：直播复盘的浏览器与 WorkBuddy 两条 BY-16 验收 runner 都遗留默认端口 3017；其他当前结果型矩阵已使用3016。固定 workflow、页面、WorkBuddy合同与 API 本身未失败。

## 实现记录

- 修改文件：`scripts/verify-beauty-live-review-browser-e2e.mjs`、`scripts/beauty-industry-live-review-workbuddy-p1-smoke.ts`、`scripts/beauty-industry-live-review-workbench-p1-smoke.mjs` 与本产品质量文档。
- 实现：浏览器与 WorkBuddy 验收默认目标统一为当前正式 3016；两条 runner 均在创建租户/浏览器/业务运行前核验 `/ready ok=true` 和 `database=true`，不可用时返回稳定、有限的 endpoint/stage 分类。WorkBuddy 仅允许显式本机 HTTP 端点，未扩大到外网。
- 数据/接口/配置变化：无产品接口或数据库变化；仅验收入口和质量门禁。
- 兼容性和回滚点：显式 `BEAUTY_E2E_API_URL` 仍优先；回滚为本任务测试文件改动。

## 验证

- 领域命令：直播复盘 workbench/runtime/WorkBuddy、MCP platform 与美业完整 Web 合同矩阵 PASS；错误端口夹具在租户创建、浏览器和 Provider 前 fail-closed。
- 全量门禁：API/Web/Agent typecheck、`pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`（含 build）与 `git diff --check` PASS。
- 页面/E2E：默认命令连续 3 次 PASS；每次均覆盖 API preflight、桌面1440、390px、数据解析、八模块、刷新、返回、第二租户隔离、console0、external Provider0。相邻全结果矩阵、视频内容复盘与导航 E2E 亦 PASS。
- 未运行项：真实 Provider、生产部署按范围禁止。

## 交接

- 残余风险：QA-20260827-002范围P0/P1=0；产品运行代码与直播合同未修改。BY-19/BY-20继续PAUSED，BY-26真实三图仍是独立付费门禁。
- 后续任务：由总调度选择；BY-26 真实三图仍需新的精确费用授权。
- 最后更新日期：2026-08-27
