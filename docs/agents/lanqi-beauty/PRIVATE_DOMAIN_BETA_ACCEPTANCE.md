# 兰琪「私域营销」内测验收清单（交付 WorkBuddy · 2026-09-13）

一页说明：**入口 + 账号口径 + 逐条检查项 + 通过标准**。通过后由老板本人复验，再发内测码给用户。

## 一、范围

只验**私域营销**板块。其余 7 个板块显示「开发中」是刻意收口，不算缺陷，请勿作为问题提交。

## 二、入口

| 环境 | 入口 |
|---|---|
| 生产 | `https://api.lcppch.top/os-v2/lanqi/moments`（微信群话术：`…/moments/wechat-group`） |
| 测试实例 | `https://api.lcppch.top/lanqi-test/lanqi/moments`（免登录直通，自动化友好，同一份产物） |

## 三、账号口径

- 生产：**真人微信扫码登录**，且账号已开通「兰琪」；未开通会停在登录/开通提示，属预期。
- 测试实例：免登录直通，不用扫码。
- 一期口径：**一个账号 = 一家门店**；出现门店切换器/多店对比才是缺陷。
- 数据纪律：**不得使用真实客户资料**；截图不得外带经营数据、手机号、客户姓名。

## 四、逐条检查项与通过标准

| # | 检查项 | 操作 | 通过标准 |
|---|---|---|---|
| 1 | 私域首页可达 | 打开 `/lanqi/moments` | 正常渲染，能看到朋友圈与微信群入口；无白屏、无「页面不存在」 |
| 2 | 快速模式生成 | 填内容 → 生成 | 结果卡片文案完整；卡片上**同时有「📋 复制文案」与「🔄 重新生成」** |
| 3 | 专业模式（七柱）生成 | 选类型 → 填内容 → 生成 | 结构完整；同样有「复制文案 / 重新生成」 |
| 4 | 素材信息不够的分支 | 只填一两句就生成 | 出现「这条素材信息还不够」+「补充一个关键信息…再生成」（明说不编造），并给「重新生成」出口；只提示不给出口即缺陷 |
| 5 | 复制真的可用 | 点「📋 复制文案」 | 出现「文案已复制」提示；粘贴内容与页面一致；被浏览器拒绝时提示「请手动选中复制」 |
| 6 | 重新生成 | 点「🔄 重新生成」 | 有加载态；生成后内容变化；不叠加旧结果 |
| 7 | 微信群话术：主题留空 | 微信群页，主题不填、只填场景 → 生成 | 能生成（主题选填）；不出现「填了却生成不了」 |
| 8 | 微信群话术：信息不够 | 只填很少 → 生成 | 与第 4 条同口径：明确提示 +「重新生成」 |
| 9 | 失败 / 超时 / 断网 | 生成中断网或刷新 | 人话错误提示 + 可重试；**输入不丢**；不长时间空白、不假装成功 |
| 10 | 刷新与历史恢复 | 生成后刷新 / 返回再进 | 历史与结果能恢复（该路径历史上出过 500） |
| 11 | 顶栏「多端实时同步」 | 点它 | 必须有可见反馈（曾是点不动的死按钮） |
| 12 | 移动端 | 390×844（真机微信更佳） | 无横向滚动、按钮可点、文字不重叠；桌面端同步检查 |
| 13 | 模型中立 | 通读页面与错误提示 | 不得出现任何模型名 / 厂商名 |
| 14 | 合规文案 | 检查生成结果 | 无「加微信 / 私聊 / 留个电话」等违规引导词、无绝对化疗效承诺 |
| 15 | 租户隔离 | 两个账号各生成一次 | A 看不到 B 的结果与历史 |

## 五、已知口径（请先看，避免误报）

1. 朋友圈「配图建议」目前是**本地生成的占位示例图**（图上写明"占位示例，正式 AI 配图待能力开通后替换"），属设计内状态。
2. 其余板块「开发中」、爆款复刻/一键成片出片受控未开，都不在本次验收范围。
3. 私域本期不做计费，页面不出现积分/价格是口径。

## 六、不通过时的提交格式

编号；环境（生产/测试 + 完整 URL）；账号（不要写手机号）；步骤 1) 2) 3)；预期；实际；证据（打码截图 + 时间戳 + 控制台报错）；复现率（必现/几次中几次）。

## 七、验收通过后的动作（老板本人执行）

1. 老板本人在**生产**真人微信走一遍第 2–8 条。
2. 生成**内测码**发给内测用户。
3. 发**体验额度 ≤ ¥100**（1 元 = 20 积分 → **≤2000 积分/人**）。
   注意：人工体验额度发放入口当前默认停用、单笔上限 800 积分；发 2000/人 需分 3 笔或先调上限，属平台侧动作。
