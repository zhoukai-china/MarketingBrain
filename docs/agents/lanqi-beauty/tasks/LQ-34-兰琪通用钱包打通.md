# LQ-34 兰琪通用钱包打通（充值入口在兰琪、钱包与思潼 AI 通用）

状态：**①②③④⑤ 已切完 + 测试实例灰度通过（真实 HTTP + 真实库 38/0）；生产未发布——待老板确认迁移 dry-run 报告后再走发布流程**

## 用户口径（2026-09-16 老板拍板，逐条可追溯）

1. 「**兰琪的用户只使用兰琪智能体**，也就是说需要在兰琪智能体里充值；未来在兰琪智能体里加一个『更多智能体』的接口，点击之后进入思潼 AI，用户可以继续在思潼 AI 里使用其他智能体。**在兰琪智能体充值的积分，可以支持同时在思潼 AI 里使用其他智能体**。也就是说钱包是通用的。未来用户进入思潼 AI 之后，也可以在思潼 AI 充值。」
2. 多人门店的付费主体：**选选项 1** —— 「兰琪里不管谁操作，都扣**租户老板（owner）的钱包**，流水里记『谁操作的、扣的是老板的钱』；思潼 AI 侧保持『谁登录扣谁的钱包』。」
3. 迁移方式：**选 Phase 2（正式，不做双轨过渡）** —— 「把历史租户额度一次性迁进钱包（或按门店发一笔等额），下线回退分支，只留一本账；补对账报表和退款一致性。」
4. 「**不显示人民币消耗**，兰琪的积分计费逻辑和思潼 AI 保持一致。」

## 现状（问题定位，2026-09-16 核实）

| 环节 | 现状 |
|---|---|
| 兰琪充值入口 | 已接通用钱包：顶栏「我的 · 充值」→ `/recharge`，充的钱进**用户钱包 `wallet`**（LQ-33b 已上线） |
| 思潼 AI 侧扣费 | 扣的就是这本钱包（`readWallet` / `consumeWalletCredits`） |
| **兰琪侧扣费** | 扣的是**租户积分账户 `creditAccount`**——两本账不一致，导致「在兰琪充了钱、兰琪仍提示积分不足」 |

> 2026-09-16 收口后：上表第 3 行的主路径（文案十件套 / 图片 / 视频确认 / 爆款复刻 / 视频执行 / 出片许可预算，即 ①②③④⑤）**已改扣 owner 钱包**，与 `/recharge`、思潼 AI 同一本账；只剩 ⑥⑦（美业单品线共用的预留模型）仍走租户账户，见第 2 节。

兰琪/美业专业线当前直接读写 `creditAccount` 的位置（切换范围，共 7 处）：

1. `apps/api/src/routes/acquire.ts`（文案十件套，LQ-33 新增）
2. `apps/api/src/routes/lanqi-media-generation.ts`（图片 / 视频确认扣费 + 失败退款）
3. `apps/api/src/services/viral-video-replication-runtime.ts`（爆款复刻出片扣费 / 退款）
4. `apps/api/src/services/beauty-seedance-execution.ts`（视频执行扣费）
5. `apps/api/src/services/beauty-video-execution-permit.ts`（出片许可预算校验，读余额）
6. `apps/api/src/routes/lanqi-business-qa.ts` + `apps/api/src/services/credit-reservations.ts`（经营问答预留 / 结算 / 退款）
7. `apps/api/src/products/beauty-industry/execution.ts`、`beauty-industry-media.ts`（美业单品预留 / 结算）

## 用户结果

门店老板在兰琪里充值后，**所有兰琪能力立刻可用**（图片 / 视频 / 爆款复刻 / 文案十件套 / 经营问答）；同一笔钱未来进思潼 AI 也能直接花；系统里**只有一本钱包账**，有可对账的流水（谁操作、扣谁的）。

## 实施计划（按顺序，每步都有验证）

### 1. 计费主体解析（已完成）

- 新增 `apps/api/src/services/lanqi-wallet.ts`：按 `tenantId` 解析**租户 owner**，命中失败即 **fail closed**（`lanqi_wallet_owner_missing`，不扣费、不放行）。
- 统一收口三个动作：`precheckLanqiWallet`（余额校验）/ `chargeLanqiWallet`（扣费）/ `refundLanqiWallet`（退款）。
- 幂等与审计口径：
  - 幂等键统一为 `lanqi:{tenantId}:{requestId}`，避免不同门店共用同一 owner 时撞键；
  - 扣费走 `consumeWalletCredits`（paid → bonus、Serializable、条件更新、同键幂等）；
  - **退款按原扣费流水回退到原桶**（读该 requestId 的 consume 流水，paid/bonus 各退多少就退多少），调用方不用记账；
  - 操作人写进 `WalletLedger.source`（`lanqi:operator=<userId>`），付费人是 owner，二者分开可审计。

### 2. 切换 7 个扣费点（①②③④⑤ 已完成 2026-09-16；⑥⑦ 本轮不做）

- 逐个把 `creditAccount` 读写换成上面的三个函数；失败关闭语义保持（余额不足 → 402、模型失败 → 退款或不扣）。
- 页面余额显示改读**owner 钱包余额**（兰琪工作台与「我的 · 充值」一致）。

#### 逐点切换配方（2026-09-16 复核源码后写死，避免下一次重新踩坑）

已完成（2026-09-16）：**① `routes/acquire.ts`（文案十件套）** —— `precheckLanqiWallet` → `chargeLanqiWallet`，回归 28/0 + 131/0 已验证（提交 `88f6f82`）。**② `routes/lanqi-media-generation.ts`（图片 / 视频确认）** —— 两阶段：先 `chargeLanqiWallet` 再建任务，建任务失败按同一 requestKey `refundLanqiWallet` 补偿（提交 `f1b8057`）。**③ `services/viral-video-replication-runtime.ts` + ⑤ `services/beauty-video-execution-permit.ts`** —— 许可预算改读 owner 钱包余额（找不到 owner 直接 fail closed），扣费 / 退款同 ② 的两阶段（提交 `16d4a17`）。**④ `services/beauty-seedance-execution.ts`** —— `submit` 分支两阶段化：事务外扣钱包，事务内只做许可提交 + 建 job（`billingStatus` 承担原 `CreditReservation` 的角色），任一步失败按同一 requestKey 补偿退款（提交 `d3bb128`）。**③④⑤ 一次切完**（同一套预留-结算模型，拆开切会留下半套逻辑）。

剩余 6 点，按"改动风险从低到高"排序，逐点单独提交、单独回归：

| # | 文件 | 现状 | 切换要点（易错处） |
|---|---|---|---|
| ② | `routes/lanqi-media-generation.ts`（图片 / 视频确认） | 在**建任务的同一个事务里**扣 `creditAccount`；退款在 `refund()` 事务里 `increment` | ⚠️ 两阶段语义：`consumeWalletCredits` / `refundWalletCredits` 各自开事务，**不能嵌进现有事务**。做法：先 `chargeLanqiWallet`（requestId = 现有 `requestKey`）→ 再建任务（`billingStatus: "reserved"`）；建任务抛 `P2002`（并发同键）时**不要重复扣**（钱包侧同键本就幂等），返回既有任务；建任务抛其它错 → 调 `refundLanqiWallet` 补偿。退款侧：claim 事务只改状态（`refund_processing`），**退出事务后**再 `refundLanqiWallet`（幂等键 = 同一 requestKey），最后置 `refunded`；这样「退到一半崩了」可安全重试 |
| ③ | `services/viral-video-replication-runtime.ts` | 条件更新 `creditAccount` + `creditTransaction`（含许可 budget） | 许可预算校验（`beauty-video-execution-permit.ts`）先改读**owner 钱包余额**，否则会出现"许可说可以、扣费说没钱"；扣费/退款同样走 ② 的两阶段 |
| ④ | `services/beauty-seedance-execution.ts` | 事务内 `updateMany` 条件扣 `creditAccount`，失败 `reject("insufficient_credits")` | 扣费移到执行**之前**（钱包侧先扣），失败退款走 `refundLanqiWallet`；`reject("insufficient_credits")` 文案保持 402 语义 |
| ⑤ | `services/beauty-video-execution-permit.ts` | 读 `creditAccount.balance` 判预算 | 改 `readLanqiWalletBalance(tenantId)`；找不到 owner → 直接判 `quota_exhausted`（fail closed） |
| ⑥ | `routes/lanqi-business-qa.ts` + `services/credit-reservations.ts` | 预留 / 结算 / 退款全部基于 `creditAccount` + `CreditReservation` | 这一处被**美业单品复用**，属公共路径：要么把 `credit-reservations` 改成钱包实现（影响面最大，需回归美业线），要么给兰琪单独走 `lanqi-wallet` 的"先扣 + 原桶退"。**建议后者**：兰琪问答改成先 `chargeLanqiWallet`（幂等键用现有 requestId），失败 `refundLanqiWallet`；美业单品的 `credit-reservations` 只在它自己那条线保留，等它切的时候一起收口 |
| ⑦ | `products/beauty-industry/execution.ts`、`beauty-industry-media.ts` | 同上，预留 / 结算 / 退款 | 与 ⑥ 同一决策：本轮**不动**（它们是美业单品线），只在任务卡登记"待随美业线一起切"，避免为了整齐把影响面扩大 |

> 口径提醒：**②③④ 是兰琪用户主路径，必须在同一批次切完再发版**；⑤ 是 ②③ 的前置；⑥⑦ 属美业/公共线，若要一起切必须单独立卡（跨产品回归）。

#### 已踩过的坑（避免重复）

- 钱包幂等键是 `(walletId, refRequestId, type)`：兰琪侧统一用 `lanqiWalletRequestId(tenantId, requestId)` 加租户前缀，否则同一 owner 服务多门店时会撞键。
- `refundWalletCredits` 要求 `breakdown: {paid, bonus}`；**不要自己记账**，用 `refundLanqiWallet`（它读原 consume 流水按桶退回）。
- `lanqi-wallet-smoke` 是库级回归，会建临时租户/用户/钱包并自动清理；本机 `.env` 指向 `localhost/sitong_os_v2`（dev 库），**不要**把它指向生产跑。

#### ③④⑤ 复核结论（2026-09-16 读源码后补，下次直接照做）

这三处不是"扣一笔钱"，而是**预留-结算模型**，必须一起重构、一起回归：

- ⑤ `beauty-video-execution-permit.ts`：`claim()` 在 `atomic(tx => …)` 里读 `tx.creditAccount.balance` 跟 `a.creditCost` 比。改法：把余额读提前到 `atomic` 之外（`readLanqiWalletBalance(a.tenantId)`），原子块里只做比较；找不到 owner → 直接 `reject("insufficient_credits", 402)`（fail closed）。
- ④ `beauty-seedance-execution.ts` 的 `submit` 分支：**同一个事务**里做了四件事——条件扣减租户账户、建 `CreditReservation`、写 `credit_transaction`（`reason=reservation:beauty-industry:seedance`）、提交许可以及建 `viralVideoReplicationJob`（`billingStatus="reserved"`）。改法（两阶段）：① 事务外 `chargeLanqiWallet(requestId = 该次 requestKey)`；② 事务内只做许可提交 + 建 job +（可选的内部审计行），把 `CreditReservation` 的角色由 **job.billingStatus** 承担（`reserved` / `refund_processing` / `refunded`）；③ 任一步失败 → `refundLanqiWallet(同一 requestKey)` 补偿。
- ③ `viral-video-replication-runtime.ts`：与 ④ 同一套（`CreditReservation` + 条件扣减 + 退款），照 ④ 的两阶段改；退款一律 `refundLanqiWallet`，**按原桶退回、同一 requestId 只退一次**，不要自己写 increment。
- 共同注意：`credit-reservations.ts` 仍被美业线使用，**本任务的兰琪路径不要复用它**；兰琪侧一律走 `lanqi-wallet` 的三个函数，等美业线自己切时再收口公共实现。

### 3. 历史额度一次性迁移（dry-run 已出；**口径已修正为「只迁兰琪租户」**，等老板确认后才 apply）

**口径修正（2026-09-16，提交 `9c61771`）**：初版迁移脚本按「全库 `CreditAccount.balance > 0`」取数，会把思潼 AI 侧 190 个 `founder-ip,takeaway` 租户、4 个 `beauty-industry` 租户一并搬进钱包——**这是跨产品越权改账，不能用**。现口径收窄为：**只迁持有 `lanqi` 产品权益、或存在 `LanqiStoreProfile` 的租户**；范围判定函数与离线口径回归 `lanqi:wallet-migration-scope-drill`（20 断言，已挂 `qa:regression`）随该提交落地。

**dry-run 结论（2026-09-16，测试实例 + 生产只读，未执行任何写入）**：

- 测试实例（schema `lanqi_test`）：兰琪口径 **42 个账户 / 12,420 积分**可迁（全库 68 / 20,220，正确排除 26 / 7,800），`ownerMissing=[]`、`alreadyMigrated=0`。
- 生产：兰琪口径 **2 个账户 / 508 积分**可迁（全库 201 / 2,001,160,483，正确排除 **199 / 2,001,159,975**），`ownerMissing=[]`、`alreadyMigrated=0`。
- 生产两条候选（都只有 `lanqi` 权益、`LanqiStoreProfile` 行数 0）：

| tenantId | 名称 | 余额 | ownerUserId | 活跃成员 | owner 钱包 |
|---|---|---:|---|---:|---|
| `cmt6idd1c04v62hgb86gvh7pz` | 兰琪 | 300 | `cmt6idd1g04v72hgbue4moh86` | 1 | 无 Wallet 行（ledger 0） |
| `cmt6wlw7w0517pou3005wvk8o` | 兰琪 | 208 | `cmt6wlw850518pou3jyxodf6g` | 1 | paid 0 / bonus 0 |

- 两条都是 2026-08-24 `grant welcome_credits` 300；后者随后有 `reservation:beauty-industry:*` 的消费 / 退款流水，净剩 208。
- 被排除的 199 个（**不得迁移**）：`founder-ip,takeaway` 190 个 / 2,001,154,675；`beauty-industry` 4 个 / 3,800；无兰琪权益 5 个 / 1,500。TOP：`WB外卖测试` 2,000,000,000、`保禄` 998,277、`QA BBQ` 2,992。
- 旧 v1 报告的 `ownerMissing=6` **是非兰琪租户的噪音**，新口径下 `ownerMissing=[]`。
- ✅ **老板已确认「口径 + 候选清单」并授权执行（2026-09-16 晚）**，要求顺序：**先测试实例 apply → 再生产 apply**，且都要出「迁移前 CSV + 迁移后对账」证据。
- **测试实例已 apply（2026-09-16 21:01，schema `lanqi_test`）**：
  - 命令：`node apps/api/node_modules/tsx/dist/cli.mjs scripts/lanqi-wallet-migrate.ts --apply --backup /opt/baolu-backups/lq34-migration-test-20260916-2102/`（exit 0）
  - 迁移前 CSV：`/opt/baolu-backups/lq34-migration-test-20260916-2102/lanqi-credit-migration-1789563700534.csv`（3,616 B，43 行，列 `tenantId,tenantName,balance,ownerUserId`）
  - 实测范围 **43 个账户 / 12,520 积分**（比白天 dry-run 的 42 / 12,420 多一条 100 积分租户，属后续平台活动新增；全库 69 / 20,320，排除 26 / 7,800 不变），`ownerMissing=[]`。
- **测试实例迁移后对账（只读 SQL，必须先 `SET search_path TO lanqi_test`）**：
  - `WalletLedger` `refRequestId LIKE 'lanqi-migrate:%'` → **43 行 / 12,520**，全部 `type=admin`、`bucket=paid`；
  - `CreditTransaction` `reason='lanqi_credit_migration_to_wallet'` → **43 行 / 12,520**（`direction=adjust`）留痕；
  - 已迁租户 `CreditAccount.balance` 全为 **0 且行仍在**（无 `nonzero_migrated_accounts`）；
  - **非兰琪 26 个账户 / 7,800 积分一分未动**；owner 钱包 `paid ≥ 迁入额`（0 人不足）；无重复租户行。
- **幂等证据**：同日再跑一次 `--apply`（备份目录 `lq34-migration-test-idem-20260916`），迁移流水仍为 **43 行 / 12,520**，未重复发放。
- ⚠️ **生产迁移、生产发布、老板门店实跑本轮均未执行**（下一轮继续，见「交接」）。

- 迁移对象：`CreditAccount.balance > 0` 的兰琪租户。
- 迁移目标：该租户 **owner 的钱包**，进 `paid` 桶（历史额度是购买 / 授予所得，不是营销赠送，不设到期）。
- 幂等与留痕：每租户一条 `WalletLedger`（`type=grant`、`source=lanqi_credit_migration:<tenantId>`、`refRequestId=lanqi-migrate:<tenantId>`），重复执行不重复发放；迁移前逐租户导出 CSV 备份，迁移后把 `creditAccount.balance` 置 0 并保留行（不删数据）。
- 回滚：备份 CSV + 「反迁移」脚本（把钱包里的额度扣回租户账户）。

### 4. 对账与退款一致性（退款一致性已落地并验证；对账报表待做）

- 出「充值订单 / 钱包流水 / 兰琪消费流水」三方对账口径，至少覆盖：充值、扣费、退款、迁移、推荐奖励。
- 退款规则只有一条：**原路退回钱包（原桶）**，且同一 requestId 只退一次。**已落地**：`refundLanqiWallet` 按原扣费流水分桶退回，离线演练 25 断言 + 实例演练均已覆盖「跨桶扣回原桶 / 重复退款不写第二笔 / 已退款 requestId 重放被拒（`status:"refunded"`，fail closed）」。

### 5. 测试与发布（门禁全绿 + 测试实例灰度与历史额度迁移已通过；生产未发布）

- 新回归（均已落地并进 `qa:regression`）：`lanqi:wallet-smoke`（owner 解析、余额不足、扣费幂等、退款回原桶且只退一次、跨租户隔离、操作人留痕）、`lanqi:wallet-refund-drill`（离线失败退款演练 25 断言）、`lanqi:wallet-migration-scope-drill`（迁移口径 20 断言）。
- **实例灰度（2026-09-16，新增 `scripts/lanqi-wallet-instance-drill.mts`）**：真实 HTTP + 真实 prisma + 真实模型，**38 断言 / 0 失败**——充值前 402 且不扣费 → `applyRecharge` 充 100 后同一接口 200 / 扣 40 / 流水记操作人 → 跨租户 404 且 B 店钱包不动 → 无 owner fail closed 409 → 跨桶扣费 / 原桶退回 / 重复退款幂等 / 已退款 requestId 重放被拒 → 全程旧账（`creditAccount` / `creditTransaction` / `creditReservation`）零写入；跑完自动清理，服务器残留核查全 0。
- 既有回归：`lanqi:media-generation-smoke`、`lanqi:video-oss-transport-smoke`、`lanqi:xcq-*`、`lanqi-business-qa-smoke`、`lanqi:copy-kit-smoke`、`qa:lanqi-foundation`、`qa:fast`。
- **发布顺序（老板 2026-09-16 晚确认）**：**生产迁移 → 生产发布 → 只读核对老板门店余额与流水 → 老板门店实跑一次低价生成**（验证「充值 → 兰琪可用 → 思潼货架同样可用」）。测试实例侧已先行跑完「发布 + 迁移」当作预演。
- **测试实例重新发布（2026-09-16 20:59，`20260916-lq34-wallet-test1`）**：白天那份 `lq34-wallet-full-20260916-1335` 在 20:42 被平台线 `plat67` 发布**叠加覆盖**过（`apps/api/src/services/sitong-wallet.ts` 被换回主线版本，缺 `withWalletTransaction` / `WalletDb`），导致迁移脚本首次 apply 直接报 `withWalletTransaction is not a function`（未写任何数据）。用基于 `ff88530` 的发布包重发后 `DEPLOY_OK` + `VERIFY_OK`（health/ready 200），再跑迁移成功。**教训：跨任务共用同一台测试实例时，发布前必须核对钱包相关文件的 sha256，不能只看「上次发布成功」。**
- 灰度：先老板门店，确认「充值 → 兰琪可用 → 思潼货架同样可用」三件事都成立后再放开。

## 本次不做

- 不做双轨过渡（用户已选 Phase 2）；不保留「钱包不足回退租户账户」的分支。
- 不改思潼 AI 侧「谁登录扣谁」的现状。
- 不显示人民币；不在兰琪里加第二个充值入口（继续用 `/recharge`）。

## 风险

- **钱的路由切换**：7 个扣费点必须一次性切干净，否则会出现「一半扣钱包、一半扣租户账户」的裂账；合并 / 发布需串行。
- 历史额度迁移不可逆（有 CSV 备份与反迁移脚本兜底）。**本轮新增一条风险并已止血**：迁移初版扫全库，会把思潼 AI / 美业租户的积分一起搬进钱包；现口径已收窄到兰琪租户，见 QA-20260916-011。
- 多人门店：员工操作扣老板钱包，必须在页面与流水里说清（避免员工以为花的是自己的）。
- `credit-reservations.ts` 目前是租户账户的预留 / 结算模型，切钱包时要一起改成「钱包扣费 + 原桶退款」，否则经营问答会出现半套逻辑。

## 交接

- 已完成：任务卡、口径冻结、现状定位；`lanqi-wallet.ts` 计费主体收口；**①②③④⑤ 五个扣费点切换 + 页面余额改读 owner 钱包**；离线退款演练（25 断言）、迁移口径离线 drill（20 断言）、**测试实例真实灰度演练（38 断言）**；测试实例全量发布（`lq34-wallet-full-20260916-1335`，`DEPLOY_OK` + health/ready 200 + `No pending migrations`，备份 200M）。
- 本轮不做：**⑥ `routes/lanqi-business-qa.ts` + `services/credit-reservations.ts`、⑦ `products/beauty-industry/execution.ts` / `beauty-industry-media.ts`** —— 属美业单品线共用的预留模型，要为它单独立卡 + 跨产品回归，不能为「目录整齐」捎带切。
- **下一步（老板已授权，无需再拍板）**：① 生产 dry-run 复核（应为 **2 个 / 508 积分**）→ ② scp 迁移所需文件到生产并 `--apply --backup`（先出迁移前 CSV）→ ③ 生产迁移后对账（2 / 508、非兰琪 199 / 2,001,159,975 不动）→ ④ 生产发布 → ⑤ 只读核对老板门店余额与流水 → ⑥ 老板门店实跑一次低价生成。
- ⚠️ 注意：**生产迁移与生产发布本轮均未执行**，生产仍跑平台线 `plat67`（未含 LQ-34）。

### 断点与交接物（2026-09-16 夜）

- 代码：worktree `C:\Users\book\.codex\worktrees\lq34\baolu-os-v2-source`，分支 `codex/lq34-wallet`，**HEAD `ff88530`，工作区 clean**（本轮未改任何代码，只动了测试实例与证据）。
- 发布物（已上传服务器，可直接复用）：`/tmp/release-20260916-lq34-wallet.tar.gz`，**1,584 文件 / 9,978,440 B / sha256 `2d4fa183e2bb478576d4061180101f98e67b1004078455b5cc74484739b52ff1`**（本地同一份在 `%TEMP%\release-20260916-lq34-wallet.tar.gz`）。
- 文件清单：`%TEMP%\lq34-rel-files.txt` = 平台线 plat67 清单 + 8 个 LQ-34 文件（清单口径：排除 `.env.example`、`__pycache__`/`*.pyc`、`dev.db`、`scripts/tmp/**`、`videos/**`）。
- 服务器脚本/证据：`/tmp/lq34-chain-test.sh`（测试实例发布链）、`/tmp/lq34-recon-test.sql`、`/tmp/lq34-recon-test2.sql`、`/tmp/lq34-recon-after-idem.sql`、`/tmp/lq34-test-apply.json`、`/tmp/lq34-test-idem.json`、`/tmp/deploy-chain-20260916-lq34-wallet.log`；备份保留 `/opt/baolu-backups/lq34-wallet-full-20260916-1335-before-baolu-os-v2-test/`（回滚兜底，勿删）。
- **生产环境事实（本轮踩坑，务必记住）**：测试实例与生产**共用同一个物理库 `baolu_os_v2`**，靠 PostgreSQL **schema** 隔离——`baolu-os-v2.env` 的 `DATABASE_URL` 不带 schema（= `public`，生产数据），`baolu-os-v2-test.env` 是 `...?schema=lanqi_test`。**用 psql 对测试实例对账必须先 `SET search_path TO lanqi_test`**，否则读到的就是生产数据（首次对账就踩了这个坑，只读无副作用）。
- 白天那份测试实例备份 `/opt/baolu-backups/lq34-wallet-full-20260916-1335-before-baolu-os-v2-test/` 已被后续平台线发布覆盖，**不能作为当前测试实例的回滚基准**；当前基准是 `20260916-lq34-wallet-test1` 的备份目录。
- 最后更新日期：2026-09-16
