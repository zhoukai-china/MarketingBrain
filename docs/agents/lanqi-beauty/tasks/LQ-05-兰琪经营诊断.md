# LQ-05 兰琪经营诊断

状态：自动验收完成

## 归属

- 产品：兰琪美业经营增长智能体平台
- 层级：产品专用
- 风险：高
- 预计修改热点：兰琪专用诊断服务、API、诊断结果页面和专项 Eval
- 是否允许并行：否

## 用户结果

门店可从本店经营档案生成一份可追溯的经营诊断，明确区分事实、估算、资料缺口和可执行的下一步补充项。

## 本次范围

- 基于当前门店档案生成资料完整度诊断与优先补充顺序。
- 每个结论标出资料来源，缺资料时明确拒绝输出项目和内部定价建议。
- 提供独立的兰琪诊断结果页面，作为当前可见的产品入口。
- 建立资料不足、来源冲突和跨租户隔离的专项回归。

## 本次不做

- 不编造任何兰琪方法论、案例、价格、效果或已执行动作。
- 未有已授权且生效的兰琪知识前，不生成项目、价格或成交方案。
- 不调用外部投放、支付、内容发布或客户触达。

## 验收条件

1. 正常路径：可读出当前租户档案，并输出资料来源、资料缺口和优先补充顺序。
2. 失败路径：资料为空时返回明确的资料不足结果，不伪造诊断结论。
3. 不应发生：不展示项目或定价建议；租户 A 不能获得租户 B 的事实、估算、待补或报告。
4. 可观测结果：返回明确 `knowledgeStatus`，标识当前是否有经授权的兰琪方法论可用于下一层诊断。

## 基线与失败证据

- 基线命令：`pnpm.cmd qa:lanqi-foundation`
- 修复前失败测试/Eval：新模块，无既有实现。

## 实现记录

- 修改文件：新增 `lanqi-diagnosis` 服务/API、独立诊断结果页面、专项 smoke，并纳入兰琪质量命令。
- 数据/接口变化：新增 `GET /lanqi/diagnosis/current`，只基于服务端可信当前租户的 `LanqiStoreProfile` 生成资料型诊断。
- 兼容性和回滚点：独立路由和页面，不改变既有档案记录；移除本任务新增路由、页面和 smoke 即可回滚。

## 验证

- 领域命令：`pnpm.cmd lanqi:diagnosis-smoke` 与 `pnpm.cmd qa:lanqi-foundation`，通过。
- 接口实测：写入合成门店资料后，返回“基础可诊断”、3 条资料依据、1 项优先补充与 `pending_authorized_knowledge` 方法论状态。
- 页面：Web 类型检查与生产构建通过。
- 页面/E2E：Browser/Computer Use 运行环境初始化因本机路径错误失败，未完成真实点击验收。

## 交接

- 残余风险：当前是资料型诊断，不含兰琪真实方法论、案例和内部定价；收到授权材料并完成知识上线后，才能开启该层能力。
- 后续任务：LQ-06 项目、获客与成交。
- 最后更新日期：2026-08-13。
