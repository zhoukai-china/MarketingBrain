# LQ-14 同页真实图片生成任务链路

状态：真实三图与图片工作室 P1/延迟回归完成；本轮全量门禁 PASS

## 归属

- 产品：兰琪美业经营增长智能体
- 层级：产品专用
- 风险：高（异步媒体、积分、租户文件）
- 开发任务标签：`reasoning_high`；异步任务、租户隔离、计费幂等、Provider 联调和安全根因必须使用高能力编码模型 + high 推理。纯 UI/机械回归可使用 `reasoning_standard`，但不能降低 E2E 和门禁。
- 预计修改热点：兰琪图片工作室页面、兰琪媒体 API/服务、媒体任务数据模型、兰琪专项测试与产品文档
- 是否允许并行：否；不修改公共 Agent、Skill、模型路由注册热点

## 用户结果

用户在同一兰琪图片工作室确认 LQ-10 专业提示词后，创建受控图片生成任务，看到进度、成功图片或明确失败，并可取消、重试、选择和保存结果，无需复制提示词到其他页面。

## 本次范围

- LQ-10 正向提示词、负向提示词、后期中文叠字、画面比例、知识状态和事实边界原样进入任务预览。
- 复用现有 `media.image.generate` 与百炼异步提交/查询能力；新增显式执行闸门，默认禁止真实付费调用。
- 建立报价、确认、幂等、取消、刷新恢复、超时、失败退款、租户隔离、文件归属与生命周期契约。
- 提供不产生供应商费用、不扣积分的受控模拟任务，用于桌面端与 390px 页面验收；页面必须明确标注模拟结果不等于真实成图。
- 真实模式只在模型、密钥、持久文件存储、积分结算和单次预算批准全部就绪时放行。

## 本次不做

- 不充值、不修改生产配置或部署；2026-08-20 在最高 ¥1 的明确授权下，仅于隔离测试环境执行 3 次 `wan2.7-image` 真实调用，未执行第四次或自动重试。
- 不开发 LQ-11 文生视频、LQ-12 图生视频或 LQ-13 直播话术。
- 不重复开发 LQ-09/LQ-10 产品能力；仅允许把真实 P1 失败沉淀为提示词增强 Skill 的最小结构与稳定性修复。
- 不把通用美业方法论冒充兰琪知识，不激活尚未提供的兰琪视觉资料。

## 验收条件

1. 正常路径：选择专业提示词方向 → 查看费用与执行模式 → 确认创建 → 排队/处理中 → 成功图片 → 选择并保存 → 刷新后恢复；重新生成产生新任务。
2. 失败路径：缺授权/缺预览/执行未放行、网络失败、供应商失败、持久化失败、超时、取消非排队任务、余额不足均给出明确结果；未交付结果不收费或只退一次。
3. 不应发生：不因页面打开或报价创建媒体任务；重复点击不重复任务/扣费；租户 A 不能读取、取消、选择、保存或下载租户 B 的任务/图片；页面不泄露模型供应商、密钥、内部错误或临时供应商 URL。
4. 可观测结果事件：`lanqi_image_generation.quoted/requested/submitted/processing/succeeded/failed/canceled/retried/selected/saved/restored`。

## 基线与失败证据

- 基线命令：`node -e <LQ-14 permanent-disabled assertion>`
- 修复前失败测试/Eval：2026-08-20 实际 FAIL：`LQ-14 baseline FAIL: same-page generation button is permanently disabled and no generation workflow is rendered`。
- 现象、根因和连带影响：LQ-10 按零付费范围永久禁用第二步；已有通用媒体接口未绑定专业预览，也没有同页任务 UI、显式预算闸门、文件持久化/归属、选择保存与完整取消恢复闭环。
- 模型策略修复前 Eval：`lanqi:runtime-model-policy-eval` 首次因缺少运行时模型策略服务 FAIL；随后只读审计发现本地 Agent 与远程 MCP 的小红书/图片提示词能力均默认 `standard`，媒体任务也没有独立持久化实际 Provider 与 prompt 版本。

## 模型与推理分工

| 任务 | 运行时模型 | 推理标签 | 硬门禁 |
|---|---|---|---|
| 兰琪知识综合、小红书策略/文案 | `deepseek-v4-pro` | `reasoning_high` / `deep` | 非精确 Pro 配置拒绝专业生成；降级只能返回明确标识的受控草稿 |
| 专业文生图提示词、多轮视觉修改、事实/授权复核 | `deepseek-v4-pro` | `reasoning_standard` / `standard` | `thinking=disabled`、`max_tokens=4096`、严格 JSON Output；结构/安全硬失败不得放行 |
| 低风险分类、标签、格式转换、候选初筛 | 当前仍使用 `deepseek-v4-pro` | `reasoning_high` | `deepseek-v4-flash` 仅为候选；通过兰琪同套 Eval 并显式批准后方可切换为 `reasoning_standard` |
| 图片生成 | 百炼 `wan2.7-image` / `media.image.generate` | 媒体异步任务 | DeepSeek 不生成图片；任务记录实际 Provider/model、prompt 版本、负向提示词、参数、状态、费用预览与失败原因 |

## 实现记录

- 修改文件：`apps/web/src/pages/LanqiImageStudioPage.tsx`、`apps/web/src/styles/lanqi-image-studio-enhancer.css`、`apps/api/src/routes/lanqi-media-generation.ts`、`apps/api/src/services/lanqi-media-generation.ts`、`apps/api/src/services/lanqi-media-assets.ts`、`apps/api/src/services/lanqi-runtime-model-policy.ts`、`apps/api/src/services/structured-delivery.ts`、`apps/api/src/config/env.ts`、`packages/agent/src/index.ts`、`packages/db/prisma/schema.prisma`、迁移、LQ-14 smoke/Eval/contract 与产品文档。
- 数据/接口/配置变化：媒体任务新增专业预览、负向提示词、结构参数、资产、选择/保存/取消时间；新增报价、显式确认、任务列表/刷新/取消、资产选择/保存/下载；新增 `disabled/mock/real` 执行闸门、真实执行二次批准与存储闸门。
- 模型策略：专业文本请求在 API 提交前验证精确 `deepseek-v4-pro`；小红书保持 deep，图片提示词在真实同套 Eval 后使用有界 standard/non-thinking/JSON Output，本地 Agent、远程 MCP 与审计标签一致。模拟或 Provider 降级结果在页面标为“受控草稿（专业模型未完成）”。媒体确认只能使用服务端恢复的本租户预览。
- 安全修复：成功与取消/退款用条件事务互斥；退款只允许一个事务返还；取消竞态的孤立文件清理；供应商临时 URL 不进入用户结果；受控模拟不调用外部文本或图片模型。
- 兼容性和回滚点：真实执行默认关闭；迁移尚未应用到生产；关闭 LQ-14 执行模式即可退回 LQ-10 纯预览，不影响 LQ-09。

## 验证

- 领域命令：`lanqi:image-studio-smoke`、`lanqi:image-generation-workflow-smoke`、`qa:lanqi-foundation`、`qa:fast` PASS；提示词 Eval 8 类 × 3 轮、192/192、硬失败 0。专项第一次在沙箱内因 `esbuild spawn EPERM` 失败，沙箱外原命令复跑 PASS，属于工具进程权限而非项目失败。
- 全仓门禁：2026-08-20 原始 `qa:regression`、`qa:full` 曾因共享 Agent smoke 将 FIP 获客任务错误路由到 `agent_acquisition / baolu_content_creator` 而 FAIL；底层网关补齐 FIP 专属 Skill 运行时激活后，2026-08-21 本任务再次原样执行 `qa:regression`、`qa:full` 均 PASS，`qa:full` 包含 `qa:fast`、全仓回归和 7 个工作区构建。
- 页面/E2E：`127.0.0.1:5175` + API `127.0.0.1:3015`、`DATA_MODE=database`，桌面与 390×844 实际完成受控邀请登录、门店事实恢复、普通需求、专业提示词、费用确认、异步任务、成功图片、选择、保存、刷新恢复、断网 3999 与恢复；移动端三项任务和三项已保存均恢复，无页面错误提示。
- 真实三图：租户指纹 `b517c775278d`，3 个任务均为 `aliyun_bailian / wan2.7-image / prompt 1.0.0 / succeeded / charged / persisted / selected / saved`；任务指纹 `01eef63e5d78`、`9496cb265567`、`fb466c05ca48`，Provider 任务指纹 `25f8ca3d6c2f`、`c17ad910452b`、`2d88c8847cbe`。比例依次为 `3:4`、`16:9`、`1:1`，端到端落库用时依次 582 秒（包含修复前白名单阻塞窗口）、17 秒、4 秒。
- 2026-08-21 持久验收重跑：在独立根目录 `F:\思潼AI增长os\test-environments\lanqi-acceptance-20260821`、仅监听 `127.0.0.1:55433` 的新 PostgreSQL 集群上再次完成 26 个迁移并验证二次部署无待迁移；两个合成租户均为有效 `lanqi` entitlement，A 租户 0 任务/300 测试积分，B 租户完成三图后 3 任务/0 测试积分。
- 2026-08-21 P1/延迟回归：Champion 第 1 例 81.734 秒后 `length` 硬失败（4096 reasoning tokens）；修复为 Pro standard、显式 non-thinking、4096 上限和 JSON Output，并把 Skill/提示词版本升到 1.0.1。真实 Challenger 8 类×3 为 24/24、硬失败 0、P50 17.118 秒、P95 20.935 秒、24 次 `stop`、reasoning tokens 0、估算文本成本 ¥0.4458。
- 页面 P1：阶段/耗时/取消/晚到保护、旧预览保留、断网恢复、合成标签剥离、护理合规改写及所有付费入口统一禁用均 PASS；真实页面保存 1.0.1 预览，3 张历史图片完整恢复。本轮图片 Provider 任务、媒体任务、图片积分交易均未增加。
- 本次重跑三图：B 租户指纹 `e73e6b272594`，任务指纹 `2660c69a2ffa`、`1a691914c2dd`、`e2743a7d7114`，Provider 任务指纹 `568cd64d3600`、`b20a0a414264`、`843460c6f96a`；均为 `aliyun_bailian / wan2.7-image / prompt 1.0.0 / succeeded / charged / persisted / selected / saved`，比例 `3:4`、`16:9`、`1:1`，落库用时 550.8、20.0、17.0 秒。第一项耗时包含解析器修复与服务重载窗口，同一 Provider 任务恢复，没有付费重试。
- 本次资产指纹：`f49a93f1d396`、`1750db2e8ae1`、`cd1ee291e7b7`；3 PNG + 3 元数据均为 `tenant_owned`，选择和保存时间存在。公开价仍按 2026-08-20 核验的 ¥0.20/成功图片估算，累计预计 ¥0.60；未查询供应商账户最终账单，不把估算冒充实际账单。
- 费用与积分：公开价核验为 ¥0.20/张，3 张累计预计供应商成本 ¥0.60，不超过 ¥1；隔离测试租户 3 笔任务各消费 100 测试积分，余额 300 → 0，任务数 3、媒体交易数 3，无第四笔、无退款、无重复任务。
- 文件归属：三张结果只保存到租户哈希目录 `uploads/lanqi-media/b517c775278d16500585a139`，3 个图片文件 + 3 个元数据文件；用户侧只返回租户资产 URL，不暴露 Provider 临时签名 URL。
- 持久恢复：当前重跑资产保存到验收根目录下的租户哈希资产区；逻辑备份 `backups/lanqi-acceptance-20260821.dump` 已由 `pg_restore --list` 验证可读（555 项），15 项 SHA-256 清单全部匹配。`stop.ps1` 只在 PID、Node 命令和监听端口同时匹配时停止本环境；实际停机后 3015/5175/55433 均不监听，再由 `start.ps1` 恢复，状态检查与新浏览器页面仍恢复 3/3 资产、控制台 0 错误/告警。`README.md` 记录同一恢复方式，不修改生产配置。
- 租户与幂等：页面新建隔离工作区 B 后，任务 0、预览 0、图片 0，未出现 A 的门店名或三条独有需求；接口专项证明同键同输入只创建一项、同键变更 409、跨租户资产 404，失败与取消路径不重复计费。
- 人工成图评分（1–5，硬失败必须为 0）：

| 样例 | 意图 | 主体 | 构图 | 风格 | 品牌边界 | 文字处理 | 人物/手部/面部 | 虚假事实 | 可用性 | 硬失败 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 夏季补水小红书封面 | 5 | 5 | 4 | 4 | 5 | 5 | 5 | 5 | 4 | 0 |
| 无人物护理空间/项目介绍 | 5 | 5 | 4 | 4 | 5 | 5 | 5 | 5 | 4 | 0 |
| 无品牌滴管瓶护理场景 | 5 | 5 | 5 | 5 | 5 | 5 | 5 | 5 | 5 | 0 |

三图均无顾客、员工、可识别门店、商标、价格、疗效、案例或营销文字；仅保留供应商要求的“AI 生成”水印，不视为模型生成的营销叠字。

## 交接

- 已关闭阻塞：隔离 PostgreSQL 迁移 26/26、二次无待迁移，真实页面三图全部落库；原数据库认证阻塞不再成立。
- 本轮真实 Bug：百炼成功结果返回 `dashscope-*.oss-accelerate.aliyuncs.com`，旧国内出站白名单只包含北京 OSS，导致首图已成功但页面无法落库。修复前 contract FAIL；默认白名单新增官方 OSS 加速域名，隔离测试 env 仅增加同域名且未改生产配置；同一 Provider 任务恢复成功，没有付费重试。
- 共享放行阻塞已解除（2026-08-21）：底层网关补齐 FIP 专属内容 Skill 的运行时激活集合，并把 FIP 与门店获客 smoke 固定到各自产品上下文/Agent；当前主检出区原始 `qa:regression`、`qa:full` 均 PASS。兰琪无需修改或覆盖共享运行时热点。
- 残余风险：本地租户资产存储已验证，但生产对象存储、生产迁移、生产部署未执行；真实三图覆盖无人物安全样例，未覆盖经授权真人图片。兰琪视觉知识版本仍未加载，页面已明确只使用门店已确认事实和通用视觉表达。
- 真实验收授权：2026-08-20 总调度批准三图总预算最高 ¥1；北京 `wan2.7-image` 公开价核验为 ¥0.20/张，实际成功 3 张、预计 ¥0.60。官方页面没有单列价格生效日，本次记录核验日期 2026-08-20。
- 后续任务：LQ-14 已达到一次最终用户验收线；文生/图生视频本周不开发，保持未开放。
- 共享热点：本轮未修改 `mcp-client`、`routes/mcp`、`agent-runtime` 或 `agent-definitions`；对 `packages/agent`、`packages/skills` 和结构化 Provider 仅做图片提示词 standard/non-thinking/JSON 与 Skill 1.0.1 的最小改动，FIP `enabled/high/16384` 回归不变。完成后热点继续释放给底层网关/Skill 治理。
- 最终验收入口：当前持久测试服务保持运行，使用 `http://127.0.0.1:5175/lanqi/image-studio?apiBase=http%3A%2F%2F127.0.0.1%3A3015`；页面已恢复 B 租户三张历史资产，最终新页面控制台错误/告警为 0。电脑关机后链接失效，可按验收根目录 `README.md` 运行 `start.ps1` 恢复；不使用通用 dev-login 绕过产品授权。
- 最终验收清单（不超过 5 步）：①受控登录后确认门店档案；②输入普通图片需求并检查专业提示词、负向提示词和费用预览；③查看三项已完成图片任务及结果；④选择、保存后刷新确认恢复；⑤切换隔离租户确认看不到原租户任务。无需再次产生付费图片。
- 最后更新日期：2026-08-21。
