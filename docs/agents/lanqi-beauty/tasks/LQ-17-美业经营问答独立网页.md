# LQ-17 兰琪美业经营问答独立网页

状态：PAUSED（代码与受控验证已完成；用户要求停止对外验收，等待新原型）

暂停停点（2026-09-03）：不继续真实模型、页面扩展、WorkBuddy外部接入、邀请或部署。既有代码、测试和费用审计全部保留；本次暂停没有新增Provider调用或费用。下一恢复点只能是用户提供新原型后重新确认的独立任务。

## 归属

- 产品：兰琪美业经营增长智能体
- 层级：产品任务含美业通用核心子改动
- 风险：中
- 预计修改热点：美业 Agent 固定 capability、兰琪薄 API、通用会话历史、兰琪独立网页与样式、受控输出、测试和产品文档
- 是否允许并行：否

## 用户结果

兰琪已授权用户在独立网页提出美业经营问题，获得基于本门店已确认事实的可执行答复，并能继续追问、查看历史和刷新恢复。

## 本次范围

- 固定 `beauty_business_qa -> general_qa@0.2.0`，网页与 WorkBuddy 入口共用同一后端执行、质量和账本合同。
- 只把当前租户经营档案中的已确认事实送入问答；估算和缺失资料不得冒充事实。
- 复用 `Conversation / Message / AgentRun` 历史、租户、权限、幂等和失败语义。
- 新增独立稳定页面 `/lanqi/business-qa`，覆盖桌面与 390px。
- 默认交付环境保持 controlled mock；真实模型只用于本任务获批的限额验收，媒体始终为 0。

## 本次不做

- 不恢复 BY-19 Get笔记、BY-20 AI 日报或小红书图文开发。
- 不恢复小红书图文、BY-19/BY-20，不部署生产，不创建新的生产入口。
- 不编造兰琪知识、价格、疗效、案例、业绩或已执行动作。

## 验收条件

1. 正常路径：有权租户可新建问答、继续追问、查看历史并刷新恢复；回答含可执行动作和事实边界。
2. 失败路径：空问题在 API 前拒绝；超时/取消/服务失败可恢复；跨租户会话返回 404；缺产品权限拒绝。
3. 不应发生：不出现跨租户数据、不引用 WorkBuddy 候选、不把估算/缺失字段写成确认事实、不重复 AgentRun 或扣积分。
4. 可观测结果事件：请求只记录 requestId、固定 capability/Skill/版本、阶段、耗时、结果码和计数，不记录问题或回答正文。

## 基线与失败证据

- 基线命令：`node apps/api/node_modules/tsx/dist/cli.mjs scripts/lanqi-business-qa-smoke.ts`
- 修复前失败测试/Eval：当前缺少 `beauty_business_qa` 固定 capability、`/lanqi/business-qa` API、独立页面和 capability 专属 controlled fixture，基线应失败。
- 现象、根因和连带影响：正式 `general_qa` 与通用会话底座已存在，但兰琪产品没有固定映射与独立用户旅程；直接调用会落入通用路由/旧受控 fallback，无法证明品牌、事实和历史边界。

## 实现记录

- 美业通用核心：`apps/api/src/products/beauty-industry/business-qa.ts`、`apps/api/src/services/agent-definitions.ts`、`apps/api/src/services/domestic-chat-provider.ts`、`packages/agent/src/index.ts`。
- 兰琪产品薄层：`apps/api/src/routes/lanqi-business-qa.ts`、`apps/api/src/server.ts`、`apps/web/src/pages/LanqiBusinessQaPage.tsx`、`apps/web/src/styles/lanqi-business-qa.css`、`apps/web/src/main.tsx`、`apps/web/src/pages/LanqiDiagnosisPage.tsx`、`apps/web/src/pages/LanqiContentStudioPage.tsx`。
- WorkBuddy 薄适配与计费：`apps/api/src/products/beauty-industry/mcp-adapter.ts`、`apps/api/src/routes/workbuddy-mcp.ts`、`apps/api/src/products/beauty-industry/text-budget.ts`；身份仍由各入口的可信凭据推导，不能由请求体切换。
- 回归与文档：`scripts/lanqi-business-qa-smoke.ts`、`scripts/lanqi-business-qa-api-smoke.ts`、`scripts/lanqi-business-qa-live-contract-smoke.ts`、`scripts/lanqi-business-qa-live-acceptance.ts`、`package.json`、本任务卡及兰琪 `PRODUCT/WORKFLOW/CONTRACTS/STATUS/TEST_MATRIX/tasks README`、`docs/BUG_REGRESSIONS.md`。
- 数据/接口/配置变化：不新增表；复用通用会话、消息与 AgentRun。
- 兼容性和回滚点：删除新增固定 capability、路由注册和页面路由即可回滚；既有数据模型与页面不变。

## 验证

- 领域命令：`pnpm.cmd lanqi:business-qa-smoke`、`pnpm.cmd qa:lanqi-foundation`，PASS。专项同时验证安全拒绝中的“无法提供”不会再被误判为 AI 身份套话，而显式“我是一个 AI”仍被拒绝。
- 全量门禁：`pnpm.cmd qa:fast`、`pnpm.cmd qa:regression`、`pnpm.cmd qa:full`（含 build），PASS。
- 页面/E2E：当前受控环境 `127.0.0.1:5176 -> 127.0.0.1:3016` 完成新问题、追问和刷新恢复，console warn/error 0；本轮无 DOM/CSS 改动，390×844 沿用同一页面既有真实 E2E：`scrollWidth=375 <= innerWidth=390`。
- 租户/权限/幂等：Web 入口要求 `lanqi` entitlement，WorkBuddy 入口要求绑定凭据的 `beauty-industry` entitlement；空问题 400、顺序 replay 只恢复一个结果、同 key 异输入 409、跨租户会话 404。
- 真实限额审计：`deepseek-v4-pro` 共 3 次、媒体 0、保守费用约 ¥0.01754。一次普通 WorkBuddy 问答通过并仅产生一条 succeeded AgentRun/一次 5 积分结算；另两次暴露“合同校验晚于保存”和安全拒绝误报，失败积分均释放/补偿，不保留伪成功。根因修复后未追加付费调用。
- 当前运行：source/runtime 短指纹 `450056B9`、`source_fresh=true`、API ready/database=true、Web 200；已恢复 `controlled_mock` 与 media disabled。
- `git diff --check`：PASS（见最终收口记录）。
- 未运行项：修复后的高风险真实 Provider 重复样例未再调用；原因是授权调用已用完且本任务以确定性红灯/绿灯关闭代码缺陷。真实答案质量或生产激活仍需新的独立授权与至少 3 次高风险样例。

## 交接

- 残余风险：LQ-17 受控流程范围内 P0/P1=0；真实 WorkBuddy 外部 Connector/OAuth 和生产真实答案质量尚未验收，不能把本机流程预览称为正式模型质量。
- 后续任务：完成后由总调度重新排期；不自行恢复暂停任务。
- 最后更新日期：2026-09-03
